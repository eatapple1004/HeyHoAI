# 세션 킥오프 붙여넣기 문구 (워커 11 + 총지휘 1)

> 운영 원칙: **워커 세션은 git 안 건드림(파일만 저장).** 총지휘 세션 1개가 git 전담(commit/push).
> 이유: 11세션이 같은 클론에서 동시에 git 건드리면 push 레이스/충돌. 각자 다른 파일이라 파일 충돌은 없으므로 write-only가 안전.

## 워커 (각 새 세션에 1개씩)
1. influencer: `~/HeyHoAI에서 docs/섹션명령서/01_influencer.md 읽고 수행. v2 검증·확정+산출물스펙 → src/recipes/seeds/recipes.influencer.v2.js (스키마·비용). 이름 전역고유. git commit/push 금지, 파일만. 한국어 스펙+keep/cut/add 요약.`
2. fashion: `~/HeyHoAI에서 docs/섹션명령서/02_fashion.md 읽고 수행. v2 검증·확정+스펙 → recipes.fashion.v2.js. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
3. beauty: `~/HeyHoAI에서 docs/섹션명령서/03_beauty.md 읽고 수행. v2 검증·확정+스펙 → recipes.beauty.v2.js. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
4. jewelry: `~/HeyHoAI에서 docs/섹션명령서/04_jewelry.md 읽고 수행. v2 검증·확정+스펙 → recipes.jewelry.v2.js. 'Unbox ASMR Reel'은 tech와 겹치니 Jewelry Unbox ASMR로 고유화. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
5. food: `~/HeyHoAI에서 docs/섹션명령서/05_food.md 읽고 수행. v2 검증·확정+스펙 → recipes.food.v2.js. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
6. home: `~/HeyHoAI에서 docs/섹션명령서/06_home.md 읽고 수행. v2 검증·확정+스펙 → recipes.home.v2.js. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
7. tech: `~/HeyHoAI에서 docs/섹션명령서/07_tech.md 읽고 수행. v2 검증·확정+스펙 → recipes.tech.v2.js. 'Unbox ASMR Reel' → Tech Unbox ASMR로 고유화. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
8. pet: `~/HeyHoAI에서 docs/섹션명령서/08_pet.md 읽고 수행. v2 검증·확정+스펙 → recipes.pet.v2.js. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
9. ugc: `~/HeyHoAI에서 docs/섹션명령서/09_ugc.md 읽고 수행. v2 검증·확정+스펙 → recipes.ugc.v2.js. 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약.`
10. general (신규): `~/HeyHoAI에서 docs/섹션명령서/10_general.md 읽고 수행. net-new로 6~8개 설계+스펙 → src/recipes/seeds/recipes.general.v2.js (스키마·비용). 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약. 철저히=ultracode.`
11. headshot (신규): `~/HeyHoAI에서 docs/섹션명령서/11_headshot.md 읽고 수행. net-new로 6~8개 설계(정장·스튜디오·전문가 톤)+스펙 → src/recipes/seeds/recipes.headshot.v2.js (스키마·비용). 이름 전역고유. commit/push 금지, 파일만. 한국어 스펙+요약. 철저히=ultracode.`

## 총지휘 (마지막·git 전담)
12. orchestrator: `~/HeyHoAI에서 docs/섹션명령서/00_총지휘_종합관리.md 읽고 총지휘 맡아. node scripts/consolidate_recipes.js로 11섹션 시드 수거·검증, _STATUS.md로 관리. 이슈·중복 이름 정리, general·headshot 들어오면 통합 뷰(_overview.html·한국어카탈로그) 갱신 + studio.html VERTICALS 와이어링, 전부 commit·push. ultracode.`

---

# 🆕 프롬프트 정밀화 세션 (死필드 이관) — main 재시작본 (워커 11 + 총지휘 1)

> **작업 브랜치 = `feat/prompt-negatives` (= origin/main 기준).** main의 live `/api/recipes`가 이 시드를 직접 읽음. 시작 전 `git fetch` + origin/main 충돌 확인(CLAUDE.md 규칙).
> **임무**: main 전 93개 네거티브가 죽은 필드 `look.negative`에 있음(extra_negative 0/93) → `look.extra_negative`(엔진이 읽는 필드)로 이관 + SAFETY 중복 제거 + text_overlay는 text/logo 제외 + 섹션 특화 정제. **11섹션 全(beauty 포함 — main에선 beauty도 死16).**
> 표준 = `docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md`. 워커=파일만 저장, 총지휘=git 전담. 각 워커는 다른 시드 파일만 건드려 충돌0.

## 워커 (각 새 세션에 1개씩 · 괄호=死필드 이관 수)
1. influencer (死6): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = influencer. 명령서대로 죽은 필드 look.negative(6개)를 look.extra_negative로 이관+정제(SAFETY 중복 제거·text_overlay는 text/logo 제외). extra_positive는 identity/hand 정밀화. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.influencer.v2.js =0 확인. docs/섹션명령서/01_influencer_작업기록.md 기록. 파일만 저장, commit/push 금지.`
2. fashion (死8): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = fashion. 명령서대로 죽은 필드 look.negative(8개)를 look.extra_negative로 이관+정제. extra_positive는 identity/wardrobe 정밀화. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.fashion.v2.js =0 확인. docs/섹션명령서/02_fashion_작업기록.md 기록. 파일만 저장, commit/push 금지.`
3. beauty (死16): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = beauty. main에선 beauty도 死필드(16개)다. look.negative(16개)를 look.extra_negative로 이관+정제. On-Model Glow Drop·Before/After·On-Skin Patch에 wardrobe 추가. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.beauty.v2.js =0 확인. docs/섹션명령서/03_beauty_작업기록.md 기록. 파일만 저장, commit/push 금지.`
4. jewelry (死8): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = jewelry. 명령서대로 죽은 필드 look.negative(8개)를 look.extra_negative로 이관+정제. extra_positive는 reflection/reference lock 정밀화. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.jewelry.v2.js =0 확인. docs/섹션명령서/04_jewelry_작업기록.md 기록. 파일만 저장, commit/push 금지.`
5. food (死7): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = food. 명령서대로 죽은 필드 look.negative(7개)를 look.extra_negative로 이관+정제. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.food.v2.js =0 확인. docs/섹션명령서/05_food_작업기록.md 기록. 파일만 저장, commit/push 금지.`
6. home (死7): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = home. 명령서대로 죽은 필드 look.negative(7개)를 look.extra_negative로 이관+정제. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.home.v2.js =0 확인. docs/섹션명령서/06_home_작업기록.md 기록. 파일만 저장, commit/push 금지.`
7. tech (死7): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = tech. 명령서대로 죽은 필드 look.negative(7개)를 look.extra_negative로 이관+정제. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.tech.v2.js =0 확인. docs/섹션명령서/07_tech_작업기록.md 기록. 파일만 저장, commit/push 금지.`
8. pet (死12): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = pet. 명령서대로 죽은 필드 look.negative(12개)를 look.extra_negative로 이관+정제. extra_positive는 identity/hand 정밀화. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.pet.v2.js =0 확인. docs/섹션명령서/08_pet_작업기록.md 기록. 파일만 저장, commit/push 금지.`
9. ugc (死7): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = ugc. 명령서대로 죽은 필드 look.negative(7개)를 look.extra_negative로 이관+정제. extra_positive는 identity/hand/lip-sync 정밀화. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.ugc.v2.js =0 확인. docs/섹션명령서/09_ugc_작업기록.md 기록. 파일만 저장, commit/push 금지.`
10. general (死8): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = general. 명령서대로 죽은 필드 look.negative(8개)를 look.extra_negative로 이관+정제. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.general.v2.js =0 확인. docs/섹션명령서/10_general_작업기록.md 기록. 파일만 저장, commit/push 금지.`
11. headshot (死7): `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 워커 맡아. 담당 섹션 = headshot. 명령서대로 죽은 필드 look.negative(7개)를 look.extra_negative로 이관+정제. extra_positive는 얼굴 일관성 정밀화. 끝나면 grep -c '"negative":' src/recipes/seeds/recipes.headshot.v2.js =0 확인. docs/섹션명령서/11_headshot_작업기록.md 기록. 파일만 저장, commit/push 금지.`

## 총지휘 (git 전담)
12. orchestrator: `~/HeyHoAI에서 docs/섹션명령서/PROMPT_프롬프트정밀화_명령서.md 읽고 프롬프트 정밀화 총지휘 맡아. 시작 전 git fetch + origin/main 충돌 확인. 11워커가 look.negative→look.extra_negative 이관한 결과 수거·검증: ① for k in influencer fashion beauty jewelry food home tech pet ugc general headshot; do echo -n "$k: "; grep -c '"negative":' src/recipes/seeds/recipes.$k.v2.js; done → 전부 0 ② node scripts/consolidate_recipes.js 이슈0·중복0 ③ text_overlay에 text/logo 없음. 구조화 export(scripts/export_recipe_prompts.js, CSV/Excel) 생성. _STATUS.md 갱신, Chief 보고. git 전담, push는 사용자 승인 후. ultracode.`
