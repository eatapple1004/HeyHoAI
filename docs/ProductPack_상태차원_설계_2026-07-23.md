# Product Pack — 상태(state) 차원 + 20컷+ 안전 스케일링 (2026-07-23)

## 왜 (문제)

YSL 립스틱처럼 **한 제품이 여러 모습**인 경우가 있다 — 뚜껑 닫힘(레오파드 케이스·로고가 주인공) vs 뚜껑 열림(누드 총알·컬러가 주인공). 파는 스토리가 아예 다르므로 각각 컷 세트가 필요하다.

그런데 기존 파이프라인은 **"제품 1개 = 정체성 1개 → 대표 레퍼 1장 → 컷 N개"** 구조였다. 급소 2개:

1. **모든 스틸이 `refs[0]` 한 장만 참조** (`pack.service.js` 구 96·105행). 닫힘/열림 사진을 2장 넣어도 레퍼는 첫 장만 쓰여 **전 컷이 한 상태로만** 나왔다. 세트(변형)일 때도 개별 스틸은 첫 변형만 쓰고, 변형들은 로우 합성에만 들어갔다.
2. **컷 개수 천장 ~12.** 플래너가 "8–12컷"을 지시받고, 컷마다 **긴 영어 프롬프트를 통째로** 한 JSON에 담아 뱉는다(max_tokens 8000). 여기서 그냥 "20컷 만들어"로 바꾸면 JSON이 잘려(→파싱 실패→중립 폴백 6컷) **오히려 더 적고 밋밋해진다.** ← 지난 오분류 참사와 같은 truncation 함정.

두 문제의 뿌리가 같다: **상태라는 차원이 없다.**

## 설계

### 1. 상태(state) = 변형(variant)과 별개의 축

- **variants** = 서로 다른 SKU (빨강/파랑, MON/TUE 병) — 기존 축
- **states** = **같은 하나의 제품**을 다르게 보여준 모습 (뚜껑 닫음/열음, 접힘/펼침, 포장/개봉) — 신규 축

v1은 **둘 중 하나**만 (단일 SKU × 상태). 색상변형 × 상태 동시 매트릭스는 후속.

🔑 **상태는 사진에 실제로 보이는 것만.** 안 찍힌 상태는 캐논 레퍼를 구울 수 없다 → classify 프롬프트에서 "attached 사진에서 보이는 것만, 다 같은 상태면 정확히 1개" 로 못박음.

### 2. 스케일링 = 상태별로 플래너를 나눠 호출

"압축 스펙 + 템플릿 전개"도 후보였으나 **플래너의 제품 맞춤 프롬프트(강점)를 희생**한다. 대신:

- 상태마다 `planPack({state})` 를 **따로 호출** → 호출당 출력이 8~10컷로 유지 → **truncation 원천 차단**
- 총 컷 수가 상태 수만큼 **선형 증가** → 2상태 × 8~10 + 합성 = **20컷+가 억지 복제 없이** 나옴
- 단일 상태 제품은 기존 단일 호출(8~12컷) 그대로 — 밋밋한 단품을 20컷으로 억지로 불리지 않는다

### 3. 구현상 비자명한 3가지

| # | 함정 | 대응 |
|---|---|---|
| ① | 두 상태가 똑같이 `pdp_front`를 뱉으면 `assetId`(`kind:cut_key`)가 충돌해 카드가 겹침 | 컷 키를 **상태로 네임스페이스** — `closed__pdp_front` |
| ② | depth는 앞에서 N개를 자르므로, 상태별로 뭉쳐 있으면 "표준 8컷" = 상태A 8 + 상태B 0 | 계획 단계에서 **상태 인터리브**(번갈아 배치) → 슬라이스 로직 변경 0으로 해결 |
| ③ | 재굽기로 레퍼 버전이 쌓이면 합성에 옛 버전까지 들어감(닫힘v1·열림v1·닫힘v2 3장) | `composeRefPaths()` = **상태별 최신 1장씩**만, plan 순서대로 |

## 바뀐 것

| 파일 | 변경 |
|---|---|
| `planner.service.js` | classify가 `states[]`(key·label·photoIndex) 감지 + variants와 구분 지시 · `planPack({state})` 상태 포커스 · max_tokens 600→900(classify) |
| `suites.js` | `STATE_COMPOSITES` = 「상태 비교 · 나란히」 |
| `refBake.service.js` | `bakeOne({state})` — 레퍼 사진의 상태를 그대로 재현하도록 못박음(모델이 임의로 뚜껑을 도로 닫지 않게) |
| `pack.service.js` | 상태별 플래너 호출·인터리브·네임스페이스 · **상태마다 그 상태 사진으로 레퍼 베이크** · 🔑`refForCut()` 로 컷→자기 상태 레퍼 바인딩 · 상태 합성 · `plan.states`·`cut.state` 저장 |
| `pack.route.js` | `states` 수용(≤4) · `latestRefPathFor(state)` · `composeRefPaths()` · 재생성이 자기 상태 레퍼 사용 · 상태 재굽기는 그 상태 사진으로 |
| `pack.html` | 확인 카드 상태 칩(토글) · 게이트가 **상태별 레퍼**를 각각 보여주고 각각 재굽기 · 깊이 티어 `핵심/표준/넉넉/풀` · 모달 높이 가두기 |

## 검증 (2026-07-23)

- **백엔드 로직** — 플래너·이미지 provider 전부 스텁, API 호출 0
  - 상태 모드 **17건 통과**: 상태별 호출 2회(각 그 상태 사진 1장만) · 키 유니크·접두어 · 인터리브(`closed,open,closed,…`) · depth=4가 두 상태 모두 커버 · 상태별 레퍼를 그 상태 사진으로 · **모든 스틸이 자기 상태 레퍼 사용(어긋남 0)** · 합성 입력=상태 레퍼 2장 순서대로
  - 회귀 **18건 통과**: 상태 없음/1개는 기존 단일 경로 그대로(플래너 1회·`bakeRefs`·레퍼 `main`·네임스페이스 없음·합성 없음)
- **UI** — 목서버(:8096)로 전 구간
  - 확인 카드에 상태 2칩·토글(끄면 단일 모드로 떨어짐) · `states` JSON이 서버까지 도달 확인
  - 게이트 = 상태 2블록·각자 레퍼/교정입력/재굽기 · **닫힘만 재굽기 → 열림 무영향** 확인
  - 완료 = 스틸 10개가 `닫열닫열닫열닫열닫열` **인터리브** + 「상태 비교 · 나란히」 합성 + 레퍼 2 · 실패카드 0 · 콘솔 에러 0
  - 🔴 육안 검증에서 잡은 것: 모달 높이가 뷰포트 **237%**(1691px)라 깊이 다이얼·진행 버튼이 화면 밖 → 모달 `max-height:90vh` + 레퍼 영역만 스크롤로 **86%(612px)** 로 수정 / 최대 티어가 「넉넉」으로 표시되던 것(`min(12,total)==total` 중복) → 최대는 항상 「풀」

> ⚠️ 실제 비전(classify·planPack)·이미지 생성은 real 키가 필요해 **로컬에선 배선만** 검증. 실동작(립스틱 실사진)은 **prod 배포 후** 확인 필요.

---

# 입력 4형태 기능 감사 + 후속 수정 (같은 날)

목표를 "어떤 제품이든 분석해서 맞는 콘텐츠를 대량·다양하게"로 놓고, 입력 4형태를 **실제로 돌려** 감사했다
(`provider.generate`만 스텁 → bakeOne/genStill의 **실제 프롬프트**를 캡처).

## 감사에서 드러난 결함 2개

### 결함 1 — 세트(여러 개 제품)의 스틸이 한 종만 나왔다 ❌

레퍼는 `red·pink·nude` 3장을 다 굽는데 **스틸 9장 전부가 `red` 하나만** 참조했다. 나머지 변형은 굽는 크레딧만
쓰고 로우 합성에만 들어갔다 — 즉 3종 세트를 넣으면 **빨강 컷 9장 + 라인업 1장**, 핑크·누드 개별 컷은 0장.

상태에서 고친 `refs[0]` 고정 버그가 **변형(SKU) 축에는 그대로 남아 있던 것.**

**수정**: 컷→레퍼 바인딩 필드를 `state` → **`refSku`** 로 일반화(상태든 변형이든 같은 필드)하고,
세트일 때 컷을 변형에 **라운드로빈 배분**한다(`cut1=red, cut2=pink, cut3=nude, cut4=red…`).
> 상태처럼 N배로 뽑지 않는 이유: 변형은 "같은 그림, 색만 다름"이라 N배는 지루한 중복이 된다.
> 컷 수는 유지하고 전 변형이 고루 나오게 하는 쪽이 같은 비용으로 다양성이 높다.

### 결함 2 — "한 단위"가 항상 단품으로 가정됐다 ⚠️

베이크에 실제로 나가던 지시: `ONE single product only (never duplicate).` + negative `two or more products`.
그래서 **귀걸이(한 쌍)** 나 **향수+박스**는 첫 베이크부터 틀렸고, 사용자가 게이트에서 손으로 교정해야 했다.

**수정**: classify가 **`unit`** 을 자동 판별한다 — `single | pair | with_package | group`.
`bakeOne`이 이 값으로 단위 문구를 바꾸고, 개체가 둘 이상이 정상인 경우 이를 막던 negative를 뺀다.
우선순위 = **수동 hint > 자동 unit > 기본 단품** (게이트의 "한 쌍으로" 수동 교정은 그대로 최종 권한).

## 부수 수정

- classify 프롬프트에 **각도 ≠ 상태** 명시 — 같은 구성의 정면/측면/클로즈업을 상태 2개로 오판하던 여지 제거.
- 확인 카드의 상태 1개 안내가 `"예: 뚜껑 연 사진"` 이라 **모든 제품에** 립스틱 표현이 나가던 것 → 중립 문구로.
- 확인 카드 `한 단위` 표시에서 조사 오류(`한 쌍`+`로` = "한 쌍로") → 조사 없이 끊어 쓰기.

## 감사 결과 (수정 후, 실측)

| 입력 형태 | 레퍼 | 스틸이 쓴 레퍼 | 판정 |
|---|---|---|---|
| A. 단일 제품 | 1 `main` | `main` | ✅ |
| B. 제품+박스 한 사진 (unit 미판별) | 1 `main` | `main` | ⚠️ unit 판별에 의존 |
| B2. 포장/개봉 따로 촬영 | 2 | 둘 다 | ✅ |
| C. 세트 3종 | 3 | **red·pink·nude 전부** | ✅ (수정됨) |
| D. 한 제품 상태 2개 | 2 | 둘 다 | ✅ |
| E. 한 쌍(unit=pair) | 1 | `main` · 프롬프트가 **PAIR** | ✅ (수정됨) |
| F. 제품+박스(unit=with_package) | 1 | `main` · 프롬프트가 **with packaging** | ✅ (수정됨) |

회귀: 상태 모드 17건 · 단일 경로 18건 전부 통과. UI: 확인 카드 `한 단위`·상태 칩 표시 + `unit`·`states` 전송 확인.

---

# 컷 천장 상향 — 플래너 차수(round) 분할 (같은 날, 후속)

## 왜

감사에서 남은 최대 갭: **단일 상태·단일 SKU 제품의 천장이 ~12컷**이었다. 상태나 변형이 있어야만 20컷+가 나오는데,
**가장 흔한 입력은 단품 사진 1장**이다. 목표가 "대량으로 다양하게"인데 정작 기본 케이스가 못 미쳤다.

한 호출에 "20컷 만들어"라고 하면 **JSON이 잘린다**(오분류 참사의 원인). 그래서 호출을 키우면 안 된다.

## 방법 — 상태 분할과 같은 트릭을 "차수"에 적용

한 계획 단위(상태 1개, 또는 제품 전체)에 대해 플래너를 **여러 차수**로 호출한다.
2차부터는 **앞 차수에서 뽑은 컷 라벨을 넘겨** "이것들 말고 진짜 다른 것"을 요구한다.
호출당 출력 크기는 8~10컷 그대로 → truncation 위험 없이 총량만 늘어난다.

- `TARGET_CUTS=20`, `PER_ROUND=10`, `MAX_ROUNDS=3`
- `roundsFor(nUnits) = ceil(20 / nUnits / 10)` → 단위 1개면 2차수, 2개면 1차수(상태가 이미 총량을 늘리므로)
- **차수 간 키 충돌**: 2차가 1차와 같은 `key`를 뱉으면 `assetId`가 겹쳐 카드가 덮인다 → `_r2` 접미사
- **순서**: 각 단위 안에서 1차 컷이 앞 → depth로 잘라도 **가장 좋은 컷부터** 남는다
- **부분 실패 내성**: 2차가 실패/빈응답이면 거기서 멈추고 **1차 결과는 그대로 쓴다**. 1차부터 실패해야 중립 폴백.
- 프롬프트에 "근접 중복 금지, 아이디어가 떨어졌으면 억지로 채우지 말고 적게 반환" 명시

## 🔴 1차 구현의 결함 — "차수 수를 미리 계산"이 틀렸다 (prod 테스트에서 발견)

첫 구현은 `roundsFor(nUnits) = ceil(20/nUnits/10)` 로 **차수를 미리 정했다.** 이건 "한 차수당 10컷이 온다"는
가정인데, 실제로 플래너는 요청보다 적게 준다. prod 테스트(차 제품, 상태 `박스 포장`/`티백 낱개` 2개)에서
**상태당 1차수 × 6컷 = 총 12컷**에 멈췄다 — 목표 20에 못 미쳤는데도 더 돌지 않았다.

**수정**: 차수 수를 미리 정하지 않고 **목표에 닿을 때까지** 돈다.
- `targetPerUnit(nUnits) = max(6, ceil(20/nUnits))` — 상태 2개면 각 10컷 목표
- 매 차수 `want = min(10, max(4, target - got))` — **부족한 만큼만** 요청(한 호출이 12를 넘지 않아 truncation 안전)
- `MAX_ROUNDS=4` 상한, 새로 추가된 게 0이면 조기 종료

**동시에 넣은 백스톱 — 라벨 중복 드롭.** 차수를 더 돌릴수록 모델이 "10개 더" 요청에 억지로 채워 보낼
위험이 커진다. 프롬프트의 "중복 금지"는 소프트 지시라 못 막는다 → **정규화한 라벨이 이미 있으면 코드가 드롭**한다.
(기존 키 충돌 방어는 `_rN`으로 **살리는** 쪽이라 근접 중복을 못 걸렀다.)

## 결과 (실측)

| 입력 | 최초 | 차수 1차 구현 | **목표 도달형(현재)** |
|---|---|---|---|
| 단일 제품 | 10컷 | 20컷 | **20컷** |
| 상태 2개 | 10컷 | **12컷(미달)** | **20컷** |
| 세트 3종 | 9컷 | 18컷 | **27컷** |
| 한 쌍(귀걸이) | 6컷 | 12컷 | **24컷** |

> 대가: 플래너 호출이 늘어난다(단위당 최대 4회). 상태 2개면 최악 8회 — 이미지 20장 생성 비용보다는 작지만
> 공짜는 아니다. `added===0` 조기 종료가 실제로는 대부분 2~3회에서 끊는다.

깊이 사다리도 이제 의미가 산다: **핵심4 / 표준8 / 넉넉12 / 풀20**(기본 선택=표준8).

검증 14건 통과: 2차 호출·exclude 전달·1차 우선 배치·키 충돌 접미사·빈 차수 조기종료·2차 실패 시 1차 보존·
1차 실패 시 중립 폴백·상태 모드는 상태당 1차수로 인터리브 유지. 기존 회귀(상태 17·단일 18)도 통과.

> 플래너 호출이 1회 → 2회로 늘지만, 이미지 20장 생성 비용에 비하면 무시할 수준이다.

---

# prod 테스트에서 나온 버그 3건 (김스낵 6종 세트)

## ① 게이트가 레퍼를 1장만 보여줬다 — 세트 축 누락

레퍼는 6종이 정상적으로 구워졌는데 게이트엔 1장만 떴다. 원인은 `gateUnits()` 가 **`plan.states` 만** 본 것:

```js
return st.length > 1 ? st.map(...) : [{ key: 'main' }];   // 세트(변형)는 states 가 비어 있다
```

상태 축에만 게이트를 붙이고 **변형 축에는 안 붙인 누락.** `plan.refSkus` 에 6종이 다 있으므로
거기까지 폴백하게 고쳤다 → 6장이 각각 뜨고 각각 재굽기된다. 문구도 상태면 "상태별", 세트면 "종류별"로.

## ② 풀이 6컷 — 개수 문제가 아니라 **플래너 실패의 지문**

`NEUTRAL_STILLS` 가 정확히 6컷이다. 즉 "풀 6컷" = **비전 플래너가 실패해 제품 중립 폴백으로 떨어진 것**이고,
차수 로직이 안 돈 게 아니다. 실제 결과물도 전부 회색 무배경 일반 제품컷이었다(김스낵에 맞춘 컷 0개).

**진짜 문제는 이게 조용했다는 것** — 실패했는데 화면엔 "완료"로 보인다(오분류 참사와 같은 모양).

수정: `runPack` 이 폴백 여부와 첫 실패 사유를 `plan.fallback` / `plan.fallbackReason` 으로 실어 보내고,
**게이트에 경고 배너**를 띄운다("자동 분석이 실패해 기본 컷으로 진행합니다" + 사유). 근본 원인(왜 실패했나)은
`pm2 logs | grep '"stage":"plan"'` 의 `error`/`stop_reason` 으로 개별 판정한다.

## ③ 같은 상태가 두 번 감지됐다

확인 카드에 `봉지 열림` / `봉지 열림` 두 칩이 떴다(키만 다르고 뜻은 동일). 중복 제거가 **아예 없었다** —
`filter(s => s && s.key)` 로 키 존재만 봤다. 그대로 두면 같은 모습의 레퍼를 두 장 굽고 컷도 2배가 된다.

수정: 정규화한 **키·라벨 둘 다**로 접는다. 프론트(확인 카드)와 라우트(서버) 양쪽에.

## 곁들인 것

레퍼가 3장 이상이면 게이트 썸네일을 축소(300→143px)해 훑기 쉽게. 6장이어도 모달은 90vh 안에 갇히고
레퍼 영역만 스크롤되며 깊이 다이얼·CTA는 항상 보인다.

검증 10건 추가(폴백 플래그·사유·부분 실패·세트 refSkus) + 기존 회귀 전부 통과.
UI 실측: 게이트 6장·재굽기 6개·폴백 배너·중복 상태 1개로 접힘·"종류별로 고루 나눠 만듭니다".

---

# 🔴 "자동 분석 실패"의 근본 원인 — media_type 불일치 (경고 배너가 잡아냄)

배너를 넣자마자 사유가 화면에 찍혔다:

```
400 {"type":"invalid_request_error","message":"messages.0.content.0.image.source.base64: The ima…"}
```

truncation이 아니라 **Anthropic이 이미지를 거부한 것.** 결정적 단서는 **분류는 성공했는데 플래너만 실패**한 것 —
두 경로가 media_type 을 다르게 얻고 있었다.

| 경로 | media_type 출처 | 결과 |
|---|---|---|
| classify | `req.files[i].mimetype` (multer가 준 진짜 MIME) | ✅ 정상 |
| planPack | `mimeOf(path)` — **확장자로 추측** | ❌ 400 |

`multer.diskStorage` 는 파일명을 `crypto.randomBytes(16).toString('hex')` 로 만든다 — **확장자가 없다.**
그래서 durable 복사가 `path.extname(sp) || '.jpg'` 로 **전부 `.jpg`** 가 되고, `mimeOf()` 는 항상 `image/jpeg` 를
선언한다. PNG 바이트에 `image/jpeg` 라벨을 붙여 보내니 Anthropic이 400으로 거부한다.

**즉 PNG·WebP를 올리면 플래너가 100% 실패했다.** 앞서 차·립스틱이 됐던 건 그게 JPEG였기 때문.
(`nanoBanana.provider` 도 확장자로 추측하지만 **Gemini가 관대해서** 통과했다 — 그래서 레퍼는 정상적으로 구워지고
플래너만 죽는, 진단하기 어려운 모양이 됐다.)

**수정**: 확장자를 믿지 않고 **매직바이트로 실제 포맷을 판정**한다(`sniffMime`).
Claude 지원 4종(jpeg·png·gif·webp) 밖이면 sharp로 JPEG 변환해서라도 보낸다(아이폰 HEIC 등).
변환마저 실패하면(손상 파일) **던지지 않고** 예전처럼 보낸다 — `b64` 는 `planUnit` 의 try 바깥이라
여기서 던지면 팩 전체가 죽는다. 플래너가 실패하고 배너로 사유가 드러나는 편이 낫다.
durable 파일명도 실제 바이트 기준으로 붙여 정직하게 만들었다.

검증 10건: 매직바이트 판정 4종 · **이름이 `.jpg`인데 내용이 PNG → `image/png` 로 선언**(제보 재현) ·
반대 경우 · WebP · 미지원(TIFF) → JPEG 변환.

> 🔑 교훈: **경고 배너를 넣지 않았으면 이 버그를 못 찾았다.** "조용한 폴백"은 버그를 숨긴다.
> 그리고 같은 파일을 두 경로가 각자 해석하면(하나는 MIME, 하나는 확장자) 반드시 어긋난다.

---

# 다양성 엔진 — 렌즈 로테이션(동적) (같은 날, 후속)

## 왜

"컷을 더 다양하게, 많이(40~100)". 단순히 상한만 올리면 근접 중복이 늘어 "40개인데 절반이 비슷".
사용자 통찰: **무지 텀블러도 씬·컨셉 축을 열면 100개도 가능**(배경×무드×조명×맥락 = 사실상 무한).
→ 개수 상한은 사실상 없음. 진짜 제약은 "유효성 곡선"(앞쪽=핵심 씬, 뒤로 갈수록 옅어짐)뿐.

## 핵심 = 렌즈(축)를 차수마다 회전

기존 차수 분할은 매 차수 "이것들 말고 다른 것"(exclude=부정 지시)만 줬다. exclude는 공간을 닫기만 해
30컷쯤에서 마른다. **렌즈는 긍정 지시**("이번엔 배경 질감으로")라 넓은 새 공간을 연다.

- 차수마다 다른 렌즈: 핵심→라이프→아트→배경→시즌→실험(폴백 `ROUND_LENS` 6개)
- 축이 다르니 차수끼리 안 겹침 → 무지 단품도 판매·라이프·아트·배경 고루

## 🟣 렌즈는 두 겹으로 유동적 (고정 배열이면 안 됨)

| 유동성 | 무엇이 다른가 | 구현 |
|---|---|---|
| **제품별** | 축의 *종류* (음료=성분, 주얼리=착용) | classify가 `lenses[]`를 제품 보고 뽑음(유효순, 6~10개). 고정 `ROUND_LENS`는 폴백 |
| **생성량별** | 축의 *개수·깊이* (4장 vs 100장) | `PER_ROUND` 10→**5**(렌즈당 소량) → `target`이 몇 개 렌즈를 쓸지 자연히 결정 |

각 축의 깊이가 다르다(핵심=얕음 4~5, 배경=거의 무한) → **렌즈당 적게 × 렌즈를 많이** 가 다양성 최적.
- 4장 → 핵심 렌즈에서 4장(렌즈 1개) · 20장 → 렌즈 4~5개 · 100장 → 전 렌즈 + 깊은 축 재순환
- `MAX_ROUNDS` = 렌즈수+2(동적). 렌즈 다 쓰면 재순환(exclude 쌓여 계속 새로) → 무한
- 컷에 `lensIdx` 기록 + `plan.lenses` 저장 → **"더 뽑기"(Phase 2)가 이 축을 이어서 순환**

## 브리프(#135)와 조화

유저 브리프가 있으면 briefClause(PRIMARY) + lensClause(이번 각도) 공존 — Claude가 "그 브리프를
이 렌즈로 변주"한다. 둘 다 프롬프트에 들어가되 브리프가 우선.

## 바뀐 것

- `planner.service.js`: classify에 `lenses[]`(label+압축 brief, max_tokens 900→1400) · `planPack({lens})` lensClause
- `pack.service.js`: `ROUND_LENS` 폴백 + `activeLenses`(동적) + `lensPick` · `PER_ROUND` 5 · `MAX_ROUNDS` 동적 · `plan.lenses` 저장
- `pack.route.js`: `lenses` 파싱(≤12)·config 저장·runPack 전달
- `pack.html`: classify 렌즈를 `confirmState`에 담아 POST 전송(사용자엔 안 보임 — "알아서" 유지)

## 검증

- 동적 렌즈 9건: 제품 렌즈 순서대로 순환 · 고정 ROUND_LENS 안 씀 · 렌즈당 ≤5 · 20컷=렌즈4개 · 렌즈 없으면 폴백 · plan.lenses 저장 · 얕은 축이어도 렌즈 더 돌아 채움
- 렌즈 로테이션 9건 + 기존 회귀(mime/fallback/target/rounds/states/single) 전부 통과
- UI: classify 6렌즈 → confirmState → POST 전송 확인, 콘솔 0

## 컨셉 × 렌즈 조화 (같은 날, 후속)

병합된 #135(컨셉 브리프 주도) + 이번 동적 렌즈가 부딪히는 지점을 사용자가 짚었다.

**문제**: classify는 컨셉(hint)을 **제품 식별에선 무시**한다(컨셉 "우주 느낌"이 제품을 우주제품으로
오인하는 것 방지). 그래서 렌즈도 컨셉을 모른 채 **제품 카테고리 축**(카페/성분/착용)으로 뽑혔다.
그러면 planPack에서 briefClause(컨셉=PRIMARY)와 lensClause(제품 축)가 매 차수 충돌 —
"카페 아침" 컨셉인데 렌즈가 "배경질감·시즌"으로 끌어 어색한 조합("카페 아침의 대리석")이 나오고,
concept_preview의 "실제 컷들은 이 방향으로 생성됩니다" 약속을 어긴다(씬 고정형 컨셉에서 특히).

**수정**: 렌즈의 성격을 컨셉 유무로 가른다.
- 컨셉 있음 → 렌즈 = **그 컨셉의 변주**(같은 카페 아침을 라떼 옆·창가 햇살·책과 함께·김 나는 컵…),
  제품 축으로 새지 말라 명시. → briefClause(컨셉 전체)와 lensClause(이번 변주)가 자연히 조화.
- 컨셉 없음 → 렌즈 = 제품 카테고리 축(현행, 다양성 엔진).
- 🔑 **제품 식별은 두 경우 다 컨셉 무시 유지**(오분류 방지) — 렌즈 뽑기만 컨셉 반영.

classify 프롬프트를 `buildClassifyPrompt(nImgs, hint)`로 추출해 hint 분기를 테스트 가능하게.
검증 12건: 컨셉 있으면 "REALIZE THE CONCEPT"+원문인용+제품축금지, 없으면 제품축, 제품 식별은 둘 다 사진 기준.

# 🔴 prod 실측 P0 3건 (금향다원 홍차 27컷)

상태 3개(포장 봉투/찻잎 접시/우린 차)로 실제 27컷을 뽑아 눈으로 본 결과.

## P0-1 — 상태 이름이 이미지에 **텍스트로 인쇄**됨

찻잔에 한글 **"우린 차"가 그려져** 나왔다(캐논 레퍼부터 오염 → 하류 컷 전파).
원인: 베이크 프롬프트의 `state: 우린 차` 를 모델이 **그려야 할 글자**로 해석.
어제 "한국어 라벨이 영어 프롬프트에 박힌다"고 적어둔 것이 최악의 형태로 터진 것.

**수정**: classify가 상태마다 **영어 `desc`**(장면 설명)를 뽑고 베이크·플래너가 그걸 쓴다.
+ 프롬프트에 "이 설명을 제품·컵·배경에 글자로 새기지 말 것" 명시 + negative에 `added text/caption/
watermark/invented lettering` 추가. 한국어 `label`은 **UI 표시 전용**으로 분리.

## P0-2 — 상태와 컷 내용 불일치

`건조 찻잎 텍스처 매크로 · **우린 차**` 처럼 우린 차 상태인데 마른 찻잎 그림이 나왔다.
원인: `lensClause`(찻잎 축)가 `stateFocus`를 이겼다. 어제 컨셉×렌즈 충돌은 고쳤는데 **상태×렌즈**는 안 고쳤다.

**수정**: stateFocus를 **HARD constraint**로 격상 — "렌즈가 다른 상태를 요구하면 렌즈를 상태에 맞춰 굽히고,
절대 다른 상태를 주제로 한 컷을 계획하지 말 것". lensClause에도 "충돌 시 STATE가 이긴다" 명시.

## P0-3 — 브랜드 없는 컷이 과반 (27컷 중 브랜드 8컷)

찻잎·찻물 컷엔 봉투가 없어 **"어느 회사 홍차인지 모르는 일반 스톡사진"** 이 됐다. 상세페이지 가치 낮음.
원인: 파생 상태 레퍼가 포장과 독립적으로 구워져 브랜드 연결이 없음.

**수정 두 겹**:
- classify가 상태마다 `showsPackage`(브랜드 보이는 상태인가)를 판별 → `derived = !showsPackage`
- **프롬프트**: 파생 상태 라운드엔 "대부분의 컷에 브랜드 포장을 함께 프레임에(옆/뒤/따르는 중), 순수 매크로는 1~2컷만"
- **레퍼**: 파생 상태 컷 생성 시 `genStill`에 **포장 레퍼를 함께** 넘긴다(`brandRefPath`) → 프롬프트가 포장을
  부를 때 실제 라벨을 그릴 수 있다. `plan.brandSku`에 포장 상태 key 저장. 두 생성 경로(runPack·generatePack) 모두.

## 검증 14건

레퍼 3장 · **베이크 프롬프트에 한글 0건**(영어 desc 사용) · 각인 금지 문구/negative 존재 ·
플래너에 desc 전달 · classify가 showsPackage 요구 · brandSku=포장 · **파생 컷은 레퍼 2장(상태+포장),
포장 컷은 1장** · 2번째 레퍼가 실제 포장 레퍼. 기존 회귀 9스위트 전부 통과.

> ⚠️ 실제로 각인이 사라지고 브랜드가 들어가는지는 **prod 재실측**에서만 확정된다.

## 🟡 실측에서 남은 것(미수정)

- **P1 다양성**: 찻잎 매크로류가 6컷+ 반복. 소재가 "찻잎/찻물"뿐이라 렌즈를 돌려도 시각적으로 비슷.
- **P1 합성 잘림**: `composeRow`가 중앙 세로 슬라이스만 떼어 3장을 붙여 봉투·찻잔이 잘림.
- **P2 라벨 왜곡**: "봉투에서 쏟는" 등 동적 컷에서 워드마크 깨짐(생성 모델 근본 한계). 정면 계열은 양호.

---

## 🔴 남은 것 — Phase 2 "더 뽑기"(무제한)

렌즈 인프라(순환·lensIdx·plan.lenses)는 다 깔렸다. 남은 것:
- `planUnit` 모듈화(seed=기존 라벨/키로 이어뽑기)
- `POST /:id/expand` — max(lensIdx)+1 렌즈부터 N개 더
- pack.html "20개 더 뽑기" 버튼 + 진행표시
- 🔴 prod 실측: 동적 렌즈로 씬이 진짜 갈라지는지(로컬은 배선만·real 비전 아님)

## 남은 것 / 알려진 갭

- **「컨셉 추가」(add-cut)는 refSku 미인식** — 라이브러리 컷엔 refSku가 없어 최신 레퍼를 쓴다. v1 허용.
- **상태 × 변형 동시 매트릭스 미지원** (v1은 둘 중 하나).
- ~~단일 상태·단일 SKU 제품의 천장 ~12컷~~ → **해결됨**(위 "차수 분할" 절 — 어떤 제품이든 20컷). 다만 20을 넘기려면 `TARGET_CUTS` 상향 + 실제 다양성이 유지되는지 확인 필요.
- **B(제품+박스 한 사진)는 classify의 `unit` 판별에 달렸다** — 오판하면 박스가 떨어진다. 게이트에서 `박스와 함께` 로 교정 가능.
- 🔴 **prod 실검증 필요**(로컬은 전부 스텁·목): ①립스틱 2장 → 상태 2개·각자 제 상태 ②귀걸이 1장 → unit=pair 자동 판별 ③세트 → 변형별로 고루
