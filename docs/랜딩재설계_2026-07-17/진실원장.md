# Doppia 진실 원장 — 코드 검증 완료 (11 도메인)
> 이 문서 밖의 사실 주장은 랜딩에 쓸 수 없다. 모든 항목은 file:line으로 검증됨.

## 1. 팔 수 있는 참인 주장 (107)

### engines
- **[TRUE·headline]** Doppia's ad video is built by a four-engine chain: Claude writes the script, Gemini renders each scene, Kling animates it, ElevenLabs voices and scores it.
  - 근거: `src/ugc/ugcVideo.service.js:25-30 requires all four in one service (generateUgcScript from ugcScript.service, renderClips from clipPipeline.service, music.service, tts.service); cl`
  - ⚠️ This is the single strongest engine claim on the page and the landing does not make it — the strip lists engines without saying what any of 
- **[TRUE·headline]** Kling powers every motion clip and the sound-effects track.
  - 근거: `Two independent live paths: (1) src/ugc/clipPipeline.service.js:97-107 klingProvider.submit + pollUntilDone (Ad Video); (2) src/generate/videoJob.service.js:89-102 raw fetch to htt`
  - ⚠️ Kling is the ONLY configured video vendor — RUNWAY_API_KEY and MINIMAX_API_KEY are both empty in prod .env, so the fallback chain at videoGe
- **[TRUE·headline]** Google's Gemini image models (Nano Banana / Nano Banana Pro) render every photo and every Ad Video scene frame.
  - 근거: `src/images/providers/nanoBanana.provider.js:1 require('@google/genai'), :11 new GoogleGenAI({apiKey: env.GEMINI_API_KEY}), :93 and :138 model gemini-2.5-flash-image. src/tools/regi`
- **[TRUE·headline]** ElevenLabs generates the AI voice-over and the background music.
  - 근거: `src/ugc/audio/tts.service.js:16 EL_TTS endpoint, :74-84 synthElevenLabs, :153-161 with-timestamps (drives caption sync + ugcVideo.service.js:330 scene-length recalc). src/ugc/audio`
  - ⚠️ config/index.js:92 notes music_v2 = '라이선스 클리어' (license-cleared training), which is a real commercial-safety asset for paid ad output — but 
- **[TRUE·headline]** Claude writes the script and scene direction, and enhances every prompt.
  - 근거: `@anthropic-ai/sdk in package.json. src/ugc/ugcScript.service.js:7,12 client, :100 and :275,:319 messages.create with env.CLAUDE_MODEL_SCRIPT; config/index.js:10 CLAUDE_MODEL_SCRIPT`
  - ⚠️ config/index.js:11 comment says Enhance is '결과 이미지 품질 직결' — Claude is not a side character here, it sits on the quality path of BOTH product
- **[TRUE·fine-print]** OpenAI is configured and reachable, and the landing does not disclose it.
  - 근거: `prod .env OPENAI_API_KEY is SET (len=164). Two paths: (1) src/ugc/audio/tts.service.js:15,22,93 — OpenAI TTS is the standby vendor and tts.service.js:22 selects it the moment ELEVE`
  - ⚠️ Cuts both ways: privacy.html:189 also states OpenAI is '현재는 이용하고 있지 않으며', which is not accurate either — the key is live and TTS silently fa
- **[TRUE·fine-print]** The landing names seven third-party trademarks with no attribution or non-affiliation notice anywhere on the page.
  - 근거: `public/landing.html:581 carries bare wordmarks for Google (Nano Banana, Gemini), Kuaishou (Kling), MiniMax, Runway, ElevenLabs and Anthropic (Claude). grep -i for 'trademark|respec`
  - ⚠️ The trademark use itself is mostly defensible under nominative fair use — you may truthfully name a vendor you actually use. But that defens
- **[TRUE·support]** Nano Banana Pro supports 2K output and 1-8 images per run.
  - 근거: `src/tools/registry.js:36 controls: { aspectRatio: IMG_ASPECTS, imageSize: ['1K','2K'], count: [1,2,3,4,5,6,7,8], negative: true } with the comment '2K=Pro 전용'; enforced at generate`
  - ⚠️ This is a TOOL capability, not a plan entitlement — nothing in entitlements.js gates it. Sell it as 'what the engine does', never as 'what y

### entitlements_enforcement
- **[TRUE·headline]** Private Mode — keeping your generated results out of Doppia's public Community feed — is a paid-plan feature, enforced on the server.
  - 근거: `src/generate/generate.route.js:52-60 canUsePrivate() loads users.plan from DB and returns entitlementsFor(...).privateMode; :88 `const resultVisibility = (wantsPrivate(req.body) &&`
  - ⚠️ Gate is fail-closed by design: generate.route.js:59 catches a DB error and returns false → defaults to PUBLIC. So a transient DB failure pub
- **[TRUE·headline]** On the Free plan every result you generate is published to the public Community feed, and you cannot opt out — not at generation time, and not afterwards.
  - 근거: `src/generate/generate.route.js:88 forces visibility='public' for anyone failing canUsePrivate. The same gate is repeated on every live generation path: :871 (/video/async), :999 (/`
  - ⚠️ Legacy sync POST /video (generate.route.js:1152, insert at :1415) passes no visibility → result.repository.js:5 defaults to 'private' for ev
- **[TRUE·headline]** Your paid plan includes a fixed credit allowance, and generating spends credits; when the balance runs out, generation stops.
  - 근거: `Grant: src/subscription/subscription.service.js:121 `const credits = (PLANS[plan].monthlyCredits || 0) * m;` → :123 creditService.addCredits(). Spend gate: src/teams/team.credit.js`
  - ⚠️ 'Credits / month' is a rate description, not a monthly refill event. subscription.service.js:97-99 is a PREPAID PASS: credits are granted in
- **[TRUE·headline]** Free signup includes 1,500 credits with no credit card.
  - 근거: `src/credits/credit.service.js:7 `const SIGNUP_BONUS = 1500;` → :193 addCredits(userId, SIGNUP_BONUS, {type:'signup_bonus'}). Matches src/lib/entitlements.js:12 free monthlyCredits:`
  - ⚠️ Landing:560/721/759 already state this correctly. No change needed.
- **[TRUE·support]** Failed generations are refunded automatically — you only pay for what actually renders.
  - 근거: `src/generate/generate.route.js:419 `if (okCount === 0) await charge.refund();` and :420 partial refund per failed image via teamCredit.refundGeneration(). Response :442 reports `ch`
  - ⚠️ Refunds are best-effort — src/teams/team.credit.js:150,:153 swallow errors with .catch(() => {}). Sell as the billing policy, not as an acco
- **[PARTIAL·support]** A commercial-use license is granted by plan tier (currently Premium and Enterprise only).
  - 근거: `Declared in three places but enforced in none. src/lib/entitlements.js:12-16 commercial:false for free/starter/standard/pro, true for premium. src/pricing/pricing.config.js:26-32 l`
  - ⚠️ PARTIAL because it is a CONTRACTUAL axis, not a technical one — that is legitimate (a license needs no runtime gate), but it means the landi
- **[TRUE·fine-print]** Your subscription auto-downgrades to Free when the pass expires, and the entitlement check honors that immediately.
  - 근거: `src/lib/entitlements.js:26-29 planKey() returns 'free' when user.plan_renews_at is in the past. Callers pass it through: generate.route.js:56 selects plan_renews_at alongside plan;`
  - ⚠️ entitlements.js:25 warns the expiry check is SKIPPED if the caller omits plan_renews_at (backward-compat), so it holds only on the paths tha

### credit_economy
- **[TRUE·headline]** Start with 1,500 free credits. No credit card.
  - 근거: `src/credits/credit.service.js:7 `const SIGNUP_BONUS = 1500;` → granted by grantSignupBonus() (credit.service.js:192-197), called at src/auth/auth.service.js:39 (email signup) and s`
  - ⚠️ ONE-TIME, forever. grantSignupBonus has exactly 2 call sites, both inside account-creation. There is no second grant anywhere in the repo. D
- **[TRUE·headline]** 1,500 credits = 5 template photos.
  - 근거: `src/credits/credit.service.js:12 `pro: [150, 300]` = [custom, template] per photo; pro is the studio default. Charged at src/generate/generate.route.js:240 `imageCost(model, genera`
  - ⚠️ Exactly 5 — no range. The '16' upper bound the landing advertises is unreachable (see the ≈5–16 claim).
- **[TRUE·support]** A custom-prompt photo costs half of a template photo (◈150 vs ◈300).
  - 근거: `src/credits/credit.service.js:12 `pro: [150, 300]` — index 0 = custom, index 1 = template. Selected by src/generate/generate.route.js:240 via `billingMode !== 'custom'`; public/stu`
  - ⚠️ Counterintuitive and currently unsold on the landing (0 mentions). It means free users get 10 custom photos vs 5 template photos from the sa
- **[TRUE·support]** Bigger plans cost less per credit.
  - 근거: `src/pricing/pricing.config.js:25-31. Computed: Starter $19/7,300 = $0.002603/cr · Standard $49/20,300 = $0.002414 · Pro $99/44,000 = $0.002250 · Premium $199/95,000 = $0.002095. Mo`
  - ⚠️ True but unsellable in practice right now — no plan can be purchased (501). See the purchase claim.
- **[TRUE·fine-print]** Pricing is consistent between the server config and the client — USD and KRW both.
  - 근거: `src/pricing/pricing.config.js:24-32 and public/js/pricing.js:15-23 are field-for-field identical: free $0/₩0 · starter $19/₩31,000 · standard $49/₩81,000 · pro $99/₩164,000 · premi`
  - ⚠️ One outlier: starter $19 × 1503.60 × 1.10 = ₩31,425 but config says ₩31,000 (−1.35%, rounded down). Harmless (in the customer's favor), but 
- **[TRUE·fine-print]** 1,500 credits buys zero Ad Videos.
  - 근거: `At ◈625/scene (credit.service.js:18, custom rate forced by generate.route.js:1020/:1043), a 3-scene video = 1,875 > 1,500. Only a 2-scene video (1,250) fits, which is not an ad. La`
  - ⚠️ Doubly true: even with unlimited credits, Ad Video is blocked at public/studio.html:2927. The honest framing is 'Photos free · Ad Video from
- **[TRUE·headline]** You are only charged for photos that actually come out — failures are refunded automatically, per photo.
  - 근거: `src/generate/generate.route.js:415-421: `if (charge && failCount > 0) { if (okCount === 0) await charge.refund(); else await teamCredit.refundGeneration(req.user, creditService.ima`
  - ⚠️ Applies to the photo path. I found no equivalent per-scene refund on the Ad Video render path — but that path is dead anyway.
- **[TRUE·support]** You can never be surprise-billed — running out of credits fails the request instead of charging you.
  - 근거: `src/credits/credit.service.js:79-81 — inside a `FOR UPDATE` transaction, `if (after < 0) throw insufficientError(balance, -delta);` → credit.service.js:42-46 returns HTTP 402 namin`
  - ⚠️ 'Cancel anytime' is a different and false claim — there is no recurring charge to cancel.
- **[TRUE·support]** Every credit movement is logged to a transaction ledger you can inspect.
  - 근거: `src/credits/credit.service.js:83-87 writes an INSERT into credit_ledger inside the same transaction as every balance change (atomic — no unlogged mutation is possible). Schema at s`
  - ⚠️ Landing mentions this zero times.

### billing_subscription
- **[TRUE·headline]** Plans never auto-renew — you pay once for the term and it simply ends. There is no subscription to trap you in.
  - 근거: `src/subscription/subscription.service.js:115 computes `expires = base + m*30d` and :118 writes plan_renews_at once — nothing ever re-charges. src/lib/entitlements.js:26-29 silently`
  - ⚠️ This is the TRUE statement the landing currently replaces with a FALSE one ('Cancel anytime'). It has a shelf life: docs/결제창_개발명세_포트원_2026-0
- **[TRUE·headline]** Start with ◈1,500 free credits. No credit card.
  - 근거: `src/credits/credit.service.js:7 `const SIGNUP_BONUS = 1500;` granted at :193 via addCredits. No card can be entered anywhere: plan purchase 501s at src/subscription/subscription.ro`
  - ⚠️ 'No credit card' is true but trivially so — no card field exists in the product at all. It stays true after payment opens only if signup rem
- **[TRUE·support]** If a generation fails, your credits come back automatically — no ticket, no ask.
  - 근거: `src/generate/generate.route.js:419 `if (okCount === 0) await charge.refund();` and :420 refunds partial failures at failCount × per-image cost; further refund call sites at :499, :`
  - ⚠️ Verified as code, not just policy. This is one of the few billing claims where doc and code agree.
- **[TRUE·support]** Credits don't expire while your account is active.
  - 근거: `No expiry logic exists: `grep -riE 'expire|소멸' src/credits/` returns zero hits, and no migration adds credit expiry. public/refund.html:99 states '별도 명시가 없는 한 회원 자격이 유지되는 동안 유효합니다'`
  - ⚠️ src/subscription/subscription.service.js:120 carries a comment claiming '만료 시 잔여 소멸' that is implemented NOWHERE. Putting this in copy makes
- **[TRUE·support]** Upgrade mid-term and you lose nothing — the higher tier starts instantly and your unused days carry over.
  - 근거: `src/subscription/subscription.service.js:114 `const base = (cur !== 'free' && curExpMs > nowMs) ? curExpMs : nowMs;` extends from the existing expiry rather than discarding it; :11`
  - ⚠️ Real, working, fair logic — but currently unreachable: no non-admin can purchase (subscription.route.js:31 → 501). Sellable only once paymen
- **[TRUE·fine-print]** detectCurrency resolves ko → KRW, everything else → USD.
  - 근거: `public/js/pricing.js:58-63 — localStorage 'doppia_currency' override (:59), then doppia_lang==='ko' → KRW (:61), then navigator.language starting with 'ko' → KRW (:62), else USD (:`
  - ⚠️ True but display-only. It selects no PG and no charge currency — the 'pg' field it drives (pricing.js:67-68) is decorative. See the KRW/KCP 
- **[PARTIAL·fine-print]** Buy a credit pack anytime to top up.
  - 근거: `The Eximbay rail is real and reasonably hardened: src/billing/eximbay.service.js:28 ready() inserts a pending billing_orders row, :89-98 verifies via /v1/payments/verify, :101-106 `
  - ⚠️ Whether packs are purchasable AT ALL is an env-var question unresolvable from the repo — ask before selling it. Eximbay also hardcodes `curr

### output_formats
- **[TRUE·headline]** Photos generate in 10 aspect ratios: 4:5, 1:1, 9:16, 3:4, 4:3, 16:9, 2:3, 3:2, 5:4, 21:9.
  - 근거: `src/tools/registry.js:28 — `const IMG_ASPECTS = ['4:5','1:1','9:16','3:4','4:3','16:9','2:3','3:2','5:4','21:9']`, attached to both enabled image tools (registry.js:36 nano-banana-`
  - ⚠️ generate.route.js:104 — an unrecognized ratio silently becomes null and is simply not sent to Gemini (provider default), rather than errorin
- **[TRUE·headline]** The default photo ratio is 4:5 — the Instagram feed ratio.
  - 근거: `src/tools/registry.js:27 comment '순서=노출 우선(첫 값=기본). IG 피드 4:5 우선' + :28 first element = '4:5'. public/studio.html:1854 renderRatioGrid falls back `cur=_ratioVals[0]`; :1136 aspectS`
  - ⚠️ None. This is one of the few product decisions where code, recipes and resolver all independently agree.
- **[TRUE·support]** 4:5, 1:1 and 9:16 are surfaced inline; the other seven ratios sit behind a 'Custom' panel.
  - 근거: `public/studio.html:1840 — `const RATIO_PRIMARY=['4:5','1:1','9:16']; // 인라인 우선 노출(IG 피드·정사각·스토리; 나머지는 Custom 패널)`. Fold logic at :1858-1863: if more than 3 values, render RATIO_PRI`
  - ⚠️ 3:4, 4:3, 16:9, 2:3, 3:2, 5:4, 21:9 are one extra click away, not absent. Landing copy should not imply only three ratios exist.
- **[TRUE·headline]** The ratio is chosen per generation, not baked into the template — the same template can be re-run at any of the 10 ratios.
  - 근거: `public/studio.html:1740 — template modes (influencer/shopping) get `c={ aspectRatio: gpt?[...]:GEMINI_ASPECTS, ... }`, i.e. the full ratio picker, not a fixed value. :4534-4535 — b`
  - ⚠️ Recipes do declare `output.aspect_ratio: '4:5'` (recipes.accessories.v2.js:55) but recipeResolver consumes it only for its own return metada
- **[TRUE·support]** Photo resolution is 1K or 2K, and 2K requires the Nano Banana Pro engine.
  - 근거: `src/tools/registry.js:36 — nano-banana-pro carries `imageSize: ['1K','2K']` with comment '2K=Pro 전용'; :43 nano-banana (flash) has NO imageSize key, comment 'flash=1K 고정'. Server gu`
  - ⚠️ The gate is the ENGINE, not the plan — see the separate claim below. Also note 'null' means the field is simply omitted from the Gemini imag
- **[TRUE·support]** Photos deliver 1 to 8 images per run, defaulting to 4.
  - 근거: `public/studio.html:1731 `const COUNT_OPTS=[1,2,3,4,5,6,7,8]`; :1750 `fill('countSel',COUNT_OPTS,null,4)` sets default 4; :1707 curCount() falls back to 4. Registry agrees: src/tool`
  - ⚠️ Not plan-gated. Cost scales with count (studio.html:1648 `curCount()*(isTpl?it[1]:it[0])+cap`), so the real limiter is credits, not entitlem
- **[TRUE·headline]** Ad videos and Kling clips export in 9:16, 1:1 or 16:9, defaulting to 9:16.
  - 근거: `src/tools/registry.js:74 — kling `aspectRatio: ['9:16','1:1','16:9']`. Ad Video picker: public/studio.html:874-876 three ugc-ar-btn (9:16 marked `active`, labelled 'Reels · Shorts'`
  - ⚠️ Video has NO resolution control — 1K/2K applies to photos only. There is no video ratio beyond these three (no 4:5 video, unlike photos).
- **[TRUE·headline]** Ad videos render at exactly 1080×1920 (9:16), 1080×1080 (1:1) or 1920×1080 (16:9), 30fps, H.264 MP4.
  - 근거: `src/ugc/renderPlan.js:79 — `const ASPECT_DIMS = { '9:16':{w:1080,h:1920}, '1:1':{w:1080,h:1080}, '16:9':{w:1920,h:1080} }`, consumed by the assembler at src/ugc/assembler/ffmpeg.as`
  - ⚠️ This is the single hardest, most provable output spec in the codebase — these are ffmpeg canvas dimensions the code actually enforces, not a
- **[TRUE·headline]** Ad video output at 1080×1920 / 30fps / H.264 MP4 meets the Reels and TikTok upload spec.
  - 근거: `renderPlan.js:79 (1080×1920) + ffmpeg.assembler.js:47 (30fps) + :101 (libx264, yuv420p — the universally-accepted web/social profile) + ugcVideo.service.js:83 (.mp4 container). stu`
  - ⚠️ True as a FORMAT/spec claim only. It is NOT true as a distribution claim — see 'publish straight to Instagram & TikTok' below. Phrase it as 
- **[PARTIAL·support]** 'Reels & TikTok ready' is true in the format sense — 9:16 is available for both photos and video.
  - 근거: `Photos: registry.js:28 includes '9:16', and it is one of the three INLINE ratios (studio.html:1840 RATIO_PRIMARY). Video: registry.js:74 + renderPlan.js:79 → 1080×1920 mp4. In-prod`
  - ⚠️ PARTIAL because 'ready' is doing ambiguous work. Format-ready = TRUE and well-evidenced. Publish-ready = FALSE (zero integration; studio.htm
- **[TRUE·fine-print]** Downloads are one file at a time, via a plain browser download link — PNG for photos, MP4 for video.
  - 근거: `Photos are always PNG: generate.route.js:360 `const filename = `${imageId}.png``, no format option anywhere. Video is always MP4: ugcVideo.service.js:83/:350/:583. Download UI is a`
  - ⚠️ Three real limitations the landing must not paper over: (1) NO bulk download / ZIP / 'download all' — grep for zip/archiver/jszip returns no
- **[PARTIAL·fine-print]** The pixel dimensions shown in the UI (e.g. 4:5 2K = 1824×2288) are Doppia's own estimate, not a provider-verified guarantee.
  - 근거: `public/studio.html:1888 `const TIER_PX={'1K':1024,'2K':2048,'4K':4096}` and :1889-1894 `dimsFor()` distributes a base² pixel budget across the ratio and rounds to /16. The code's o`
  - ⚠️ PARTIAL: the tier (1K/2K) is really requested (generate.route.js:326 `imageConfig.imageSize`), but the exact px figures are a client-side pr
- **[PARTIAL·headline]** A seller can produce feed, thumbnail and Reels crops of the same look from one upload, and post the results to a commercial storefront.
  - 근거: `Ratio breadth + per-generation ratio choice are real (registry.js:28; studio.html:1740, :4535). Reels-spec video is real (renderPlan.js:79). The commercial half is NOT currently tr`
  - ⚠️ ⚠️ DEPENDENCY, stated explicitly per instruction: this claim is only sellable AFTER the approved commercial:false→true change lands for star

### landing_falsehood_sweep
- **[TRUE·headline]** Doppia has no prompt box — you pick a ready-made template and it renders.
  - 근거: `src/recipes/recipeResolver.js:152-165 buildPrompt() assembles the positive prompt server-side from recipe config (subj + preset.prefix + attrFrags + shot.pose + shot.scene + look.e`
  - ⚠️ A 'Custom' text-to-image mode exists in Studio (studio.html:811 modeCustom, not flag-hidden), which IS a prompt box. So 'there is no prompt 
- **[TRUE·headline]** One product photo plus a template produces catalog-ready product photos.
  - 근거: `src/tools/registry.js:33-45 nano-banana-pro (gemini-3-pro-image-preview) and nano-banana (gemini-2.5-flash-image) both enabled:true; registry.js:91 DEFAULT_IMAGE_TOOL='nano-banana-`
- **[TRUE·headline]** Signing up gives 1,500 free credits, no credit card.
  - 근거: `src/credits/credit.service.js:7 SIGNUP_BONUS = 1500; :193 addCredits(userId, SIGNUP_BONUS). public/js/pricing.js:15 free cr:1500. Landing :560/:721/:759 bind to data-dp='plans.free`
- **[TRUE·support]** 1,500 free credits ≈ 5 template photos (300 credits each) or up to 16 at flash tier (90 each).
  - 근거: `src/credits/credit.service.js:12 pro:[150,300] (custom, template); :11 flash:[45,90]; :7 comment '가입 시 무료 크레딧 = 템플릿 5장(300×5)'. Landing's own :839-840 pconv script computes this.`
  - ⚠️ 1,500 credits is 5 photos on the DEFAULT tool. Saying '1,500 free credits' without the conversion oversells; the landing already renders the
- **[TRUE·headline]** Pick a house model and a background — no casting.
  - 근거: `public/js/models.roster.js:1 '/* AUTO-GENERATED · window.MODELS(80) */' — verified 80 entries by executing the file. public/js/backgrounds.roster.js:1 window.BACKGROUNDS(40) — veri`
  - ⚠️ Underclaimed at :641. The specific numbers (80 models across 9 ethnic groups × both genders × ages 22-29; 40 backgrounds) are the sellable f
- **[TRUE·support]** Photos export at 10 aspect ratios, defaulting to 4:5 for the Instagram feed.
  - 근거: `src/tools/registry.js:29 IMG_ASPECTS = ['4:5','1:1','9:16','3:4','4:3','16:9','2:3','3:2','5:4','21:9'] with comment '순서=노출 우선(첫 값=기본). IG 피드 4:5 우선.'; wired into nano-banana-pro:3`
  - ⚠️ Zero mentions on the landing. This is a real, shipped, seller-relevant fact the page never uses.
- **[TRUE·fine-print]** Commercial license is included with Premium.
  - 근거: `src/lib/entitlements.js:17 premium: { commercial: true } — the only plan with it. public/js/pricing.js:22 premium license:'Commercial'. public/terms.html:210 table: '상업용(Commercial`
  - ⚠️ TRUE ONLY UNTIL THIS SESSION'S APPROVED CHANGE LANDS. Opening commercial:false→true for starter/standard/pro (entitlements.js:14-16) destroy
- **[TRUE·support]** Paid plans keep your results out of the public Explore feed (private mode).
  - 근거: `src/lib/entitlements.js:14-18 privateMode false for free, true for starter/standard/pro/premium. src/generate/generate.route.js:58 entitlementsFor({role,plan,plan_renews_at}).priva`
  - ⚠️ This is the ONLY entitlement in the entire codebase that is actually enforced. It is also the only real paid-vs-free difference — and the la
- **[TRUE·headline]** Pricing is available in KRW at ₩31,000 / ₩81,000 / ₩164,000 / ₩329,000 per month, VAT included.
  - 근거: `public/js/pricing.js:15-23 priceKRW 0/31000/81000/164000/329000; :41 currency:{krwPeggedFx:1503.60, krwVatIncluded:true, peggedOn:'2026-07-13'}; :75 priceOf() switches on CURRENCY=`
  - ⚠️ The landing's price cells (:720,:726,:732,:738,:744) hardcode '$19/$49/$99/$199' as fallback text but DO carry data-price, so pricing.js ove
- **[TRUE·support]** Annual billing cuts the monthly price ~16-17% (e.g. Starter $19→$16, Standard $49→$41).
  - 근거: `public/js/pricing.js:16-23 priceY 16/41/82/165 and priceYKRW 26000/68000/136000/273000; :75-78 priceOf(o, annual) reads them.`
  - ⚠️ Zero mentions on the landing. A shipped discount lever the page never pulls.
- **[PARTIAL·support]** Every image on the landing is a real Doppia output.
  - 근거: `14 of 16 gallery tiles use /img/producthero/*, /img/accessories/*, /img/productcut/* — real engine outputs. But :668 uses /img/models/f-east_asian-2.jpg and :677 uses /img/models/f`
  - ⚠️ Both offending tiles are labeled 'Apparel · Sample · On-model'. They are roster assets, not template outputs, and contain no apparel product
- **[TRUE·fine-print]** Free includes 'All photo modes'.
  - 근거: `Vacuously true: requirePlan (src/lib/entitlements.js:53) has 0 call sites, so no photo mode is gated by plan anywhere. Free genuinely reaches every photo mode.`
  - ⚠️ True but self-defeating as a Free bullet — it silently admits paid plans add no photo capability. The only real free-vs-paid delta in code i
- **[PARTIAL·support]** Official templates are available for Apparel, Cosmetics, Accessories and Nail.
  - 근거: `Apparel/Cosmetics/Accessories are real: src/recipes/seeds/recipes.fashion.v2.js, recipes.beauty.v2.js, recipes.accessories.v2.js, plus recipes.productcut.v2.js and recipes.producth`
  - ⚠️ The nav (:524) and gallery/demo chips (:597,:660) correctly mark Nail 'Coming soon'/disabled. But :697 lists Nail as a plain live link (/stu
- **[TRUE·headline]** Photos is structurally ahead of Ad Video and should lead the page.
  - 근거: `Photos: src/tools/registry.js:35,42 both enabled:true; :91 DEFAULT_IMAGE_TOOL='nano-banana-pro'; no flag hides it. Ad Video: public/js/flags.js ugc:{launch:false} AND reels:{launch`
  - ⚠️ Proposal only, per session constraint — both stay alive. But the page currently orders them Ad Video FIRST (:608-627, badged '✦ Flagship · N

### models_backgrounds
- **[TRUE·headline]** 80 house models — 40 women, 40 men.
  - 근거: `public/js/models.roster.js:1 header 'window.MODELS(80)'; parsed at runtime = 80 entries, gender {female:40, male:40}. Built by scripts/build_model_roster.js:117-142 (8 groups × 5 s`
  - ⚠️ Real count, real files. Nothing blocks this.
- **[TRUE·headline]** 8 ethnic groups and 40 nationalities — East Asian, Southeast Asian, South Asian, Black/African, White/European, Hispanic/Latino, MENA, Mixed.
  - 근거: `scripts/build_model_roster.js:16-73 defines exactly 8 GROUPS, each with 5 'descents' → 40 unique descents (Korean, Japanese, Filipino, Nigerian, Scandinavian, Lebanese …). Runtime `
  - ⚠️ 'Nationality' is a prompt descriptor, not a verified provenance. Say 'ethnicities/looks', not 'models from 40 countries' — no one is from an
- **[TRUE·headline]** Every Doppia house model is AI-generated. Not a real person. No casting.
  - 근거: `scripts/generate_model_images.js:64 calls provider.generate({prompt, negativePrompt, width, height}) — no reference image passed. src/images/providers/nanoBanana.provider.js:88-89:`
  - ⚠️ TRUE as an engineering fact, but do NOT extend it into a legal guarantee. public/terms.html has zero coverage of the house roster's likeness
- **[TRUE·headline]** 40 backgrounds across 8 categories — Studio, Home, Cafe/Retail, Urban, Architectural, Nature, Golden Hour, Seasonal.
  - 근거: `public/js/backgrounds.roster.js:1 header 'window.BACKGROUNDS(40)'; parsed = 40, exactly 5 per each of 8 categories. All 40 /img/backgrounds/*.jpg exist (0 missing, 0 orphans, 4.6MB`
  - ⚠️ See the 'backgrounds are text, not plates' claim below — what they are is not what a reader assumes.
- **[TRUE·headline]** Pick a house model and a background — available on the Model Cut and Jewelry On Model templates.
  - 근거: `Exactly 2 of 170 cards carry picker:'model' — public/js/recipes.generated.js:3543 (model-cut, section studiomodel) and :3850 (jewelry-on-model, section accessories, with 4 cuts: ea`
  - ⚠️ '2 of 170 templates' is the honest denominator. Don't let copy imply every template has a model picker — 168 do not. Also: the rosters load 
- **[TRUE·fine-print]** Filter the model roster by gender.
  - 근거: `studio.html:2621 modelsFor(f) filters on m.gender only; :2632-2634 renders exactly three chips — All / Women / Men. That is the entire filter set.`
  - ⚠️ Thin, and the trap is the inverse: ethnicity, nationality and age are in the DATA but have NO filter UI. Any copy implying 'filter by ethnic
- **[TRUE·support]** Filter backgrounds by category (8 category chips + All).
  - 근거: `studio.html:2662 renders All + one chip per category from bgCats(); :2647 bgsFor() filters by category_key and always keeps Studio visible.`
- **[PARTIAL·fine-print]** 80 × 40 = 3,200 combinations.
  - 근거: `Mechanically there are no exclusions — studio.html:2621 (gender only) and :2647 (category only) are independent filters, setModalModel/setModalBg (:2638/:2666) carry no cross-const`
  - ⚠️ Arithmetically defensible but rhetorically dishonest. 3,200 counts INPUT PERMUTATIONS a user could select — not 3,200 pre-made, verified ass
- **[PARTIAL·support]** The same house model keeps the same face across every shot.
  - 근거: `The intent is wired: studio.html:4546 attaches the roster image as a reference; nanoBanana.provider.js:35 says 'keep the SAME face, hair and features'. But the Photos path does NOT`
  - ⚠️ Sell as intent/design ('the model you pick is carried into the shot'), never as a guarantee. scripts/export_recipe_cards.js:94 states outrig
- **[TRUE·support]** Ad Video's 'With a model' format uses the same 80-model roster.
  - 근거: `studio.html:4849 shows the picker only when fmt==='model-editorial'; :4859 reads window.MODELS; :4904 sends modelImage; backend routes it as a person reference — src/ugc/clipPipeli`
  - ⚠️ Ad Video's picker is weaker: gender-only with no 'All' option (studio.html:864-865, default 'female' at :4844), and no background picker. Bu
- **[TRUE·headline]** No casting, no studio rental, no location fee.
  - 근거: `Structural: no human is photographed (see the AI-generated claim) and no physical location is used — backgrounds are text descriptors (studio.html:2667). The seed itself frames it `
  - ⚠️ TRUE only as 'costs you don't incur'. Any WON/USD figure ('replaces a ₩2M shoot', 'save X%') is UNFOUNDED — there is zero cost/benchmark dat
- **[PARTIAL·support]** Use the generated on-model shots in your paid ads and storefront (commercial use).
  - 근거: `DEPENDENCY, not a code finding: this rests entirely on the approved-but-unapplied change opening commercial:false→true for starter/standard/pro in src/lib/entitlements.js. As of HE`
  - ⚠️ Do not ship this line until the entitlements change AND terms.html:210 land together. If the table still says '상업적 이용 불가' while the landing 

### privacy_data
- **[TRUE·headline]** You keep the rights to the product photos you upload. Doppia's license covers only what is needed to generate, store, and display your content — nothing more.
  - 근거: `public/terms.html:200 (제12조④) "회원은 회사가 서비스 제공(콘텐츠 생성, 저장, 표시, AI 처리수탁자에 대한 전송 등)을 위하여 필요한 범위 내에서 업로드 콘텐츠를 이용·복제·처리하는 것에 동의합니다. 회사는 이 범위를 넘어 회원의 업로드 콘텐츠를 이용하지 아니합니다." — 약관 전체에 소유권 양`
  - ⚠️ 약관에 "회원이 소유권을 보유한다"는 **명시 문구는 없다**. 소유권 양도 조항 부재 + 라이선스 범위가 서비스 제공으로 한정된 점에서 도출한 것. 따라서 카피는 "we don't take rights beyond running the service
- **[PARTIAL·support]** Doppia does not train AI models on your uploads.
  - 근거: `public/terms.html:200 "회사는 이 범위를 넘어 ... 이용하지 아니합니다" + public/privacy.html:198 (제7조) "오직 이용자가 요청한 콘텐츠 생성 및 재생성 목적으로만 이용되며, 광고·마케팅 학습 등 다른 목적으로 이용하지 않습니다." 코드측: `grep -rniE "fine.?tu`
  - ⚠️ PARTIAL인 이유 = **Doppia는 학습 안 하지만, 수탁자에 대해선 아무 약속이 없다**. privacy.html:170-173 국외이전 표의 보유·이용기간 칸이 4개 벤더 전부 "처리 목적 달성 시까지(**수탁사 정책에 따름**)" — 즉 
- **[TRUE·headline]** We publish exactly which AI vendors receive your data, which country it goes to, what is sent, and each vendor's privacy contact.
  - 근거: `public/privacy.html:161-190 (제6조 국외이전). 표에 이전받는 자·연락처·이전 국가·이전 항목·목적·시기/방법·보유기간 7개 칸 완비: Anthropic(privacy@anthropic.com, 미국), Google LLC(policies.google.com/privacy, 미국), Kling AI`
  - ⚠️ 이건 진짜 차별점이다 — 국내 AI SaaS 경쟁사 다수가 이 표를 안 갖고 있다. 다만 자랑하는 순간 Kling=중국이 같이 노출된다(아래 항목 참조). 트레이드오프 인지하고 쓸 것.
- **[TRUE·fine-print]** Photos go to US vendors only (Google, Anthropic). Ad Video additionally sends your image frame to Kling AI (Kuaishou) in China.
  - 근거: `public/privacy.html:171 Google LLC=미국, 이전항목 "업로드된 얼굴 사진·제품 사진, 프롬프트 텍스트", 목적 "AI 사진 생성" / public/privacy.html:172 Kling AI(Kuaishou)=**중국**, 이전항목 "업로드 또는 생성된 시작 프레임 이미지(얼굴·제품 포함 가능`
  - ⚠️ **Photos vs Ad Video 구조 질문에 대한 근거 있는 답이 여기 있다.** 한국 브랜드·대행사·엔터프라이즈 구매 심사에서 '중국 이전'은 단독 탈락 사유가 되는 경우가 흔하다. Photos 경로는 미국 벤더만 타므로 프라이버시 심사를 통과
- **[TRUE·support]** Doppia never receives or stores your card number — the payment processor handles it. We keep only the order ID, amount, and approval result.
  - 근거: `public/privacy.html:80 "카드번호·유효기간·CVC 등 결제수단 상세 정보는 회사가 수집·보관하지 않으며, ... 회사는 주문번호·결제 결과·승인 토큰만을 보관합니다." + public/terms.html:160 (제8조⑤) 동일 취지 + public/refund.html:148 (제8조) 동일.`
- **[TRUE·support]** Your data is hosted on AWS in the Seoul region (ap-northeast-2), and generated media currently stays on the Seoul server — not in overseas object storage.
  - 근거: `public/terms.html:293 호스팅 제공="Amazon Web Services (서울 리전)" / public/privacy.html:149 수탁사 표 "AWS(서울 리전) ... 데이터 저장(서울 리전 보관)" / public/privacy.html:236 "AWS(서울 리전)의 보안이 확보된 클라우드 환경"`
  - ⚠️ ⚠️ prod EC2의 .env는 레포에 없다 → **코드로 prod 실제 상태를 확증할 수 없음**. 만약 prod에서 MEDIA_S3_BUCKET이 R2로 켜져 있다면 privacy.html:189의 "현재는 이용하고 있지 않으며"가 즉시 거짓이 
- **[TRUE·fine-print]** Passwords are stored one-way hashed; Doppia never keeps your plaintext password.
  - 근거: `src/auth/password.js:4-12 — "비밀번호를 scrypt로 해시한다", `crypto.scryptSync(String(password), salt, 64)`, 포맷 `scrypt$<salt-hex>$<hash-hex>`. 고지 정합: public/privacy.html:231, public/terms.h`
- **[PARTIAL·fine-print]** Private Mode keeps a creation out of the public Community feed.
  - 근거: `src/generate/generate.route.js:88 생성 시 visibility 결정 / :633 사후 토글 / src/generate/result.repository.js findCommunity `WHERE gr.visibility = 'public'` — private면 피드 쿼리에서 제외되는 것 맞음. 그`
  - ⚠️ **Private ≠ 비공개다.** private 결과물의 실제 파일은 /images/<uuid>에서 **무인증**으로 누구에게나 서빙된다(index.js:49). UUID라 추측은 어렵지만 접근제어는 0 = security by obscurity. 
- **[TRUE·support]** Private Mode is available on paid plans.
  - 근거: `src/lib/entitlements.js:13-17 — free `privateMode:false`, starter/standard/pro/premium 전부 `privateMode:true`. src/generate/generate.route.js:58 `canUsePrivate()`가 entitlementsFor(.`
  - ⚠️ privateMode는 **코드로 실제 강제되는 유일한 플랜 권한**이다(주어진 확정 사실 재확인 완료: requirePlan() 호출 0건, `commercial` 플래그는 src/ 어디서도 읽히지 않음). 즉 유료 플랜의 기능적 차별점 중 진짜인 
- **[TRUE·fine-print]** Every page — including the landing — links to the Terms, Privacy Policy, AI Use Policy, and Refund Policy, plus full business registration details.
  - 근거: `public/js/biz-footer.js:58-61 — `<a href="/terms">이용약관</a>`, `<a href="/terms#ai-use-policy">AI 이용정책</a>`, `<a href="/privacy">개인정보처리방침</a>`, `<a href="/refund">취소·환불 및 청약철회</a>`. `
  - ⚠️ 전자상거래법 제10조 표시사항 베이스라인은 충족. 다만 biz-footer.js:7에 "⚠️ TODO(확인 필요): 통신판매업신고번호 / 대표전화 / 이메일 실제값 입력" 주석이 남아있는데, 값 자체는 terms.html:288-292와 일치하므로 T
- **[TRUE·support]** Doppia publishes an Acceptable Use Policy banning deepfakes, non-consented likenesses of real people, and sexual content.
  - 근거: `public/terms.html:122-127 `<section id="ai-use-policy">` 제7조 + `.aup` 박스: "① 성인물·음란물 ② 타인의 얼굴을 무단으로 합성·변형한 딥페이크 ③ 연예인·정치인 등 실존 유명인의 초상·음성을 동의 없이 이용·합성 ④ 아동·청소년 ... 엄격히 금지". 위반 시 제1`
  - ⚠️ 브랜드 세이프티 축에서 유효. 단 **집행 코드는 확인 못 함** — 신고 기반 테이크다운은 존재(generate.route.js:660 `/results/:idx/report`, result.repository.js `taken_down` 컬럼)하나
- **[TRUE·fine-print]** Payment and contract records are retained for 5 years as required by Korean e-commerce law; access logs for 3 months.
  - 근거: `public/privacy.html:126-130 법정 보존 표 — 계약/청약철회 기록 5년, 대금결제·재화공급 기록 5년, 소비자 불만·분쟁처리 3년, 표시·광고 6개월(전자상거래법), 접속 로그 3개월(통신비밀보호법). terms.html:238 (제16조②)과 정합.`
  - ⚠️ 이건 법정 의무라 '혜택'이 아니라 FAQ 정확성 항목. 단 이 표는 **결제 기록**에만 적용되고, 이미지 데이터 보관에 대한 실제 기간 약속은 privacy.html:116뿐인데 그게 코드로 거짓(위 항목) — 즉 '이미지 얼마나 보관하나?'는 현
- **[TRUE·fine-print]** Uploaded face images are processed only to generate content — never for identity verification or biometric matching.
  - 근거: `public/privacy.html:66 "얼굴 이미지는 개인영상정보에 해당하며, 회사는 이를 **본인 인증·식별(생체인식) 목적이 아니라** 오직 이용자가 요청한 콘텐츠 생성 목적으로만 처리합니다." 코드 정합: 생체인식·얼굴매칭 라이브러리/경로 근거 0건(src/ 전수 grep에서 face-recognition/emb`
  - ⚠️ 제품 사진 중심 타깃(스마트스토어 셀러·DTC)에겐 얼굴 이미지 자체가 부차적이라 세일즈 가치는 낮다. 다만 privacy.html:49·64-66·194-201이 서비스 핵심을 여전히 "얼굴 사진(셀카)"으로 기술하고 있어 **제품 사진 중심 피벗과

### ad_video
- **[TRUE·headline]** Doppia writes the ad for you: Claude drafts the scenes, the narration, the on-screen line, the post caption and the hashtags from your product photo and a one-line brief.
  - 근거: `src/ugc/ugcScript.service.js:100-106 (client.messages.create with env.CLAUDE_MODEL_SCRIPT, structured json_schema output); src/config/index.js:10 (CLAUDE_MODEL_SCRIPT default 'clau`
  - ⚠️ The photo is attached as a real vision block (ugcScript.service.js:88-98), so the copy is grounded in the actual product — but only if the u
- **[TRUE·headline]** See the whole script before you spend a single credit. Rewrite any line, then generate.
  - 근거: `src/ugc/ugcVideo.service.js:250-260 generateScript() — no charge, no DB write, returns {script, nClips, cost}; the only charge in the whole flow is src/ugc/ugcVideo.service.js:282-`
  - ⚠️ Also means /ugc/script is an unmetered, unrate-limited Claude-Opus-with-vision call reachable at 0 credits — an internal abuse hole, not a c
- **[TRUE·headline]** AI voice-over in 8 voices, with speed control and pause marks you can drop into the script.
  - 근거: `public/studio.html:950-959 (8 <option> voice IDs, 5F/3M); public/studio.html:961-967 (Speed 0.85/1/1.1); src/ugc/audio/tts.service.js:74-87 (ElevenLabs POST /v1/text-to-speech); sr`
  - ⚠️ Falls back to OpenAI TTS if ELEVENLABS_API_KEY is unset (tts.service.js:20-24, :90-101), which silently loses the timestamp-based caption sy
- **[TRUE·support]** Background music is composed for each ad — instrumental only, licence-cleared, matched to the mood.
  - 근거: `src/ugc/audio/music.service.js:32-56 (ElevenLabs POST /v1/music, force_instrumental:true, model_id=env.ELEVENLABS_MUSIC_MODEL); src/config/index.js:92 (default 'music_v2'); src/ugc`
  - ⚠️ 'Licence-cleared' rests on ElevenLabs' music_v2 claim (music.service.js:5-6 comment), not on anything in our code.
- **[TRUE·support]** 9:16, 1:1 and 16:9 — pick the frame before you generate.
  - 근거: `src/ugc/renderPlan.js:79 ASPECT_DIMS {'9:16':1080x1920,'1:1':1080x1080,'16:9':1920x1080}; src/generate/generate.route.js:1004 (server whitelist, defaults 9:16); public/studio.html:`
- **[PARTIAL·fine-print]** Choose 3-6 scenes and 3s or 5s per scene — or let the AI decide.
  - 근거: `public/studio.html:885-891 (Count: Auto/3/4/5/6), :893-899 (Length/scene: Auto/3s/5s), :905 (hidden inputs both default '0'=Auto); public/studio.html:4735-4736 (only appended when `
  - ⚠️ Two things break it. (1) Default is Auto → Claude picks the count with no cap on first render (generate.route.js:943 clamps only a user-supp
- **[TRUE·headline]** An ad video costs 625 credits per 5-second scene (1,250 per 10-second scene).
  - 근거: `src/credits/credit.service.js:17-20 VIDEO_CREDIT = {5:[625,1250], 10:[1250,2500]} (custom/template); :36-39 videoCost(); src/ugc/ugcVideo.service.js:120-124 estimateCost() sums vid`
  - ⚠️ So: 3 scenes = 1,875 / 4 = 2,500 / 5 = 3,125 / 6 = 3,750 credits. The 'pro' mode argument is dead — videoCost ignores it (credit.service.js:
- **[TRUE·headline]** Starter ($19 / ◈7,300 a month) gets you about 2 five-scene ad videos a month.
  - 근거: `src/pricing/pricing.config.js:26 (starter cr: 7300); src/credits/credit.service.js:17-20 (625/5s scene) → 5x625 = 3,125; 7300/3125 = 2. Same arithmetic already lives in the landing`
  - ⚠️ Scene count is AI-chosen by default, so it is 2 only for a 5-scene ad; a 3-scene ad = ~3/mo, and voice-driven 10s scenes = 1,250/scene → ~1/
- **[PARTIAL·fine-print]** Free (◈1,500) cannot make an ad video.
  - 근거: `src/credits/credit.service.js:7 SIGNUP_BONUS = 1500, and no recurring monthly grant exists for free (monthlyCredits is read only at subscription.service.js:63 and :121, i.e. on pur`
  - ⚠️ Directly contradicts docs/AdVideo_실원가_검증_2026-07-16.md:119 ('Ad Video → 0편 … 1,500cr로는 구조적으로 진입 불가'). There is no structural block; it is a 
- **[TRUE·support]** Upload up to 5 photos of the same product from different angles — the AI reads them together to keep your actual product intact across every shot.
  - 근거: `src/generate/generate.route.js:905 UGC_MAX_PRODUCT_IMAGES = 5, enforced at :922 and on the multer field at :933/:994; src/ugc/ugcScript.service.js:88-98 (all images attached as bas`
  - ⚠️ In tension with the site-wide 'one photo' hook. Frame as 'one is enough — more is better', not as a correction of the hero.
- **[TRUE·headline]** Fix any scene after the video is made: tell it what to change in plain language, add scenes, reorder, delete — and flip back to any earlier take.
  - 근거: `src/ugc/ugcVideo.service.js:642-645 reRender (job-serialised via chainRun :615-624); :735-741 (per-scene natural-language instruction → refineScene, which routes it to image vs mot`
  - ⚠️ Scene re-generation is billed per scene at the same 625/1,250 rate (ugcVideo.service.js:717-719).
- **[TRUE·support]** Changing the music, the voice, the speed or the narration on a finished ad costs nothing.
  - 근거: `src/ugc/ugcVideo.service.js:482-602 tryReComposite — re-mixes from the persisted silent/preview base with no clip work; returns cost:0 at :595. Music/voice/speed/narration override`
  - ⚠️ Internally each free music swap fires a real ElevenLabs Music call (ugcVideo.service.js:568) with no meter and no limit.
- **[TRUE·support]** Write the ad in English or Korean — with copy rules that keep it from sounding machine-translated.
  - 근거: `public/studio.html:917-923 (Ad language segment, default 'en'); src/generate/generate.route.js:945 + :1006 (language piped through); src/ugc/prompts/ugcScript.builder.js:91-96 (Kor`
  - ⚠️ A prompt instruction, not an enforced check — validateScriptSafety (ugcScript.service.js:49-59) only scans BLOCKED_TERMS, not tone.
- **[TRUE·support]** The shot list adapts to what you sell — cosmetics, apparel, nail and accessories each get their own shot palette, lighting style and music lean, detected from your photo.
  - 근거: `src/ugc/categories.js:13-42 PLAYBOOKS (4 categories, each with shots[]/style/format/music); src/ugc/prompts/ugcScript.builder.js:63 (getPlaybook), :80-88 (explicit playbook when ca`
  - ⚠️ 4 categories only, and the category picker was removed from the UI (studio.html comment above :849) → it always runs in auto-detect menu mod

### templates_catalog
- **[TRUE·support]** Seven official template families ship: Product Cut, Product Hero, Model Cut, Jewelry Product Cut, Jewelry Worn Cut, Jewelry On Model, Jewelry Hero.
  - 근거: `public/js/themes.js:16 DEFAULT_OFFICIAL_RECIPES = exactly those 7 ids. src/db/migrate.js:1046-1053 inserts exactly those 7 into marketplace_templates at price_credits=0 ('기본 제공(무료)`
  - ⚠️ This is the entire official catalog a prod user can reach. Do not sell the number 7 as a headline — sell the 45 looks it expands to (next cl
- **[TRUE·headline]** Every official template returns 4 photos per run, starting at ◈2.
  - 근거: `All 7 officials have config.output.count=4 and aspect_ratio 4:5 — verified by loading src/recipes/recipeStore.js and reading getById() for each id. Costs: product-cut ◈2, product-h`
  - ⚠️ The landing states this nowhere. Studio itself already sells it — studio.html:2438 badge title="4 photos for the same cost — best value per 
- **[TRUE·headline]** 7 template families expand to 45 selectable preset looks — you pick the family, then the exact cut.
  - 근거: `public/js/recipes.generated.js cards carry a cuts[] array per parent: product-cut 2, model-cut 0, product-hero 19, jewelry-product-cut 4, jewelry-worn-cut 4, jewelry-on-model 4, je`
  - ⚠️ 38 of these are 'cuts'/presets inside a family, not standalone templates. Say 'looks' or 'presets', never 'templates', or it becomes the sam
- **[TRUE·headline]** Product Hero ships 19 preset styles for cosmetics — Dewy Glass, Liquid Splash, Noir Gold, Silk Drape, Gloss Mirror and 14 more.
  - 근거: `src/recipes/seeds/recipes.producthero.v2.js = 20 recipes: 1 parent (product-hero) + 19 children with config.parent_id. Named children verified via recipeStore.listSeed(): Dewy Glas`
  - ⚠️ None. This is the single deepest, best-evidenced asset in the catalog and the landing sells it as scattered anonymous 'Sample ·' tiles.
- **[TRUE·headline]** One product photo is the only thing you upload. No second angle, no reference sheet.
  - 근거: `All 170 cards have config.subject.min_refs=1 — verified by iterating recipeStore.list() and reading subject.min_refs on each (distribution: 116 product_composite, 32 on_model_tryon`
  - ⚠️ 22 identity_lock cards need a FACE not a product — but all 22 (influencer 7 + headshot 7 + general 8) are unreachable in prod, so the claim 
- **[TRUE·headline]** Pick from 80 house models and 40 backgrounds — no casting, no location, no model release.
  - 근거: `public/js/models.roster.js:1 header 'window.MODELS(80)'; 80 entries counted, 80 matching .jpg files in public/img/models/. public/js/backgrounds.roster.js:1 header 'window.BACKGROU`
  - ⚠️ Only 2 of the 7 officials use the picker (Model Cut, Jewelry On Model). 80×40=3,200 combinations is arithmetically true but reads as inflati
- **[TRUE·support]** Jewelry Worn Cut shows your piece worn — on hand, neck, ears or wrist — with no model face in frame.
  - 근거: `public/js/taxonomy.js:67 defines the Worn Cut content type as '착용컷(부위 파라미터·얼굴 배제)' = worn cut, body-part parameter, face excluded — explicitly contrasted at :68 with On Model '(모델 `
  - ⚠️ Face-free is a prompt/crop construction, not a verified guarantee — the seed carries meta.flags ['experimental','needs_human_review']. Sell 
- **[TRUE·support]** Ghost mannequin — the hollow-body apparel shot — is a shipped cut, not a roadmap item.
  - 근거: `src/recipes/seeds/recipes.productcut.v2.js child 'Ghost Mannequin Cut' (config.parent_id='product-cut', credit_cost 2, count 4), one of only two surviving cuts under Product Cut. T`
  - ⚠️ Seed meta.flags=['experimental','needs_human_review'] with render_notes 'Hollow-body invisible-mannequin effect is high AI-risk — verify no 
- **[PARTIAL·fine-print]** Every template is written to hold your product identical to the reference — color, print, seams, hardware and neckline, with no morph or drift.
  - 근거: `The instruction is real and specific: recipes.productcut.v2.js look.extra_positive = 'the exact same garment locked across all shots — color, print, seams, hardware and neckline id`
  - ⚠️ Sellable ONLY as authored intent ('every template is written to hold your product true'), never as outcome. The landing's :641 'Product stay
- **[TRUE·headline]** Templates, not prompts — the template path requires no prompt writing.
  - 근거: `public/studio.html:2062 sets the template-mode hint to 'No prompt needed — just choose a look.' The prompt is assembled server-side from recipe config by recipeResolver.js:152-171 `
  - ⚠️ True as scoped to the template path. NOT true as the absolute the landing states at :589 ('There's no prompt box in Doppia') — Custom mode i

## 2. 현재 랜딩의 거짓 (105) — 절대 재사용 금지
- [fatal] L728 `✓ No watermark  (Starter tier feature bullet)` → Nothing watermarks anything. src/lib/watermark.js:12 applyWatermark() has zero callers; src/generate/generate.route.js:385 hardcodes `watermarked: fal
- [fatal] L746 `✓ Commercial license included  (listed as a Premium-only differentiator)` → Two problems. (1) Today it is technically true but ONLY as contract, not code: public/terms.html:210 restricts Free·Starter·Standard·Pro to non-commer
- [fatal] L717 `Monthly credits with your plan, top up anytime. Bigger plans cost less per credit.` → Two of three clauses are false. (a) 'Monthly credits' — no monthly grant exists; src/subscription/subscription.service.js:121-123 grants monthlyCredit
- [fatal] L720 `$0 /mo — Free (and $19/$49/$99/$199 /mo at lines 726, 732, 738, 744)` → '/mo' asserts recurring monthly billing that does not exist. src/subscription/subscription.service.js:99 `activatePlan(userId, plan, nowMs, months = 3
- [fatal] L727 `◈ 7,300 credits / month (and lines 733 ◈20,300, 739 ◈44,000, 745 ◈95,000)` → Credits are not granted per month. src/subscription/subscription.service.js:121 `const credits = (PLANS[plan].monthlyCredits || 0) * m;` → :123 addCre
- [fatal] L840 `'or N ad videos' (Starter 2 · Standard 6 · Pro 14 · Premium 30)` → Ad Video cannot be generated: public/studio.html:2927 `if(state.mode==='ugc'){btn.disabled=true;...s.textContent='UGC ads — coming soon.';return;}` is
- [fatal] L609 `<a class="fs vid" id="advideo" href="/studio?mode=ugc">Create an ad video</a> — the first, 'Flagship · New' CTA (badge a` → Routes users into a dead end. public/js/flags.js:20 `ugc:{ launch:false }` hides the in-app entry (studio.html:811), but this deep link bypasses it: s
- [fatal] L691 `Download, or publish straight to Instagram & TikTok.` → Credit-adjacent but flagged for completeness: no publishing integration exists (0 tiktok references in public/studio.html). Noted here because it sits
- [fatal] L560 `◈ 1,500 free credits · No credit card · Cancel anytime` → 'Cancel anytime' advertises a recurring contract that does not exist. No cancel endpoint exists in src/subscription/subscription.route.js (only GET /,
- [fatal] L759 `◈ 1,500 free credits · No credit card · Cancel anytime` → Same falsehood as line 560, repeated on the closing CTA band — the two highest-intent moments on the page. Same disproof: subscription.route.js (no ca
- [fatal] L510 `Background Remover — Automatic on every upload` → No background removal exists anywhere in the codebase. No bg-removal dependency in package.json (no rembg/remove-bg/u2net/imgly). sharp is required on
- [fatal] L554 `✓ Background auto-removed` → Same as line 510 — zero implementing code. Worse, this is a hero trust chip: it sits in the first screenful next to three claims that are true or argu
- [fatal] L689 `One product photo — any angle. We remove the background and lock the product so it stays true across every frame.` → 'We remove the background' is false — no code performs any removal (no dependency; sharp unused on the generation path; no alpha). This is the most da
- [fatal] L691 `Download, or publish straight to Instagram & TikTok.` → There is no publishing integration of any kind. grep for graph.instagram / graph.facebook / open.tiktokapis / publish-instagram across src/, public/js
- [fatal] L609 `Entire Ad Video flagship card (:608-627): badge '✦ Flagship · New', h3 'Ad Video', 'Turn a product photo into a narrated` → The product is switched off in prod and the CTA dead-ends. public/js/flags.js ugc:{launch:false,label:'hidden'} ('UGC Ads 모드 — 준비 중') and reels:{launc
- [fatal] L691 `Download, or publish straight to Instagram & TikTok.` → TikTok has zero integration: all 28 'tiktok' hits in src/ are LLM prompt prose (ugcScript.builder.js:66) or recipe rationale text; src/publishing/ has
- [fatal] L554 `✓ Product locked across frames` → The guard system built for this is wired to nothing. `grep -rn 'guards' src/ --include=*.js | grep -v seeds/` = 0 hits — recipeResolver.js, imagePromp
- [fatal] L642 `✓ Product stays true in every frame` → Same as :554 — config.guards[] has 0 consumers outside src/recipes/seeds/ (verified by grep). 'every frame' is an absolute quantifier over generative 
- [fatal] L689 `One product photo — any angle. We remove the background and lock the product so it stays true across every frame.` → Two false claims in one sentence. (1) Background removal does not exist — the widest grep (remove.bg|removebg|rembg|clipdrop|photoroom|birefnet|u2net|
- [fatal] L708 `No reshoots — product stays true, every frame` → Same unwired guard layer (product.guards.v2.js:22 '미반영 시 today 효과 0'; 0 consumers of guards outside seeds). Worse than :554/:642 because 'No reshoots'
- [fatal] L750 `Commercial license is included with Premium — and Studio offers it right when you publish.` → The second half is invented. public/studio.html has 0 hits for 'commercial' and 0 hits for 'publish' — there is no in-Studio license offer and no publ
- [fatal] L722 `Free: '✓ Solo lookbook' · '✓ 2 concurrent slots'` → Both fabricated. 'lookbook' is a comment-only stub — src/ugc/profiles/index.js:12 "// 'product-editorial', 'lookbook' — 후속(스텁)"; no solo/group distinc
- [fatal] L728 `Starter: '✓ Ad Video reels' · '✓ No watermark' · '✓ Group lookbook' · '✓ 3 concurrent slots'` → All four fail. 'Ad Video reels' — flags.js ugc:false AND reels:false; studio.html:2927 'UGC ads — coming soon.' 'No watermark' — generate.route.js:385
- [fatal] L734 `Standard: '✓ Edit results' · '✓ 4 concurrent slots'` → 'Edit results' — the only edit path is generate.route.js:288 openai.images.edit, reachable only via gpt-image / gpt-image-hd, both enabled:false in sr
- [fatal] L740 `Pro: '✓ Concept cut' · '✓ 5 concurrent slots'` → 'Concept cut' exists only as a marketing string — src/pricing/pricing.config.js:30 and public/js/pricing.js:21 line:'Concept cut · 5 slots · ◈44,000/m
- [fatal] L746 `Premium: '✓ 6 concurrent slots'` → No concurrency limiter exists anywhere (see :722). Premium's 'Commercial license included' bullet is currently sound (entitlements.js:17), but the slo
- [fatal] L510 `Background Remover — Automatic on every upload` → This feature does not exist. Widest-net grep (remove.bg|removebg|rembg|clipdrop|photoroom|birefnet|u2net|background.?remov) across all *.js/*.html/*.j
- [fatal] L613 `The Ad Video flagship card's reel player (#reelStage → /img/reels/r00-r12.mp4, POOL at :861), presented as what Ad Video` → ffprobe on all 13 files: 720x1280, 2.375-2.417s, ZERO audio streams. Real output is a multi-scene concat at 1080x1920 (src/ugc/renderPlan.js:79 + src/
- [fatal] L654 `Every image below is a real Doppia output. Pick a template — the lighting, composition and consistency are baked in.` → Two of the 16 tiles are not template outputs at all. :668 uses /img/models/f-east_asian-2.jpg and :677 uses /img/models/f-hispanic_latino-2.jpg — thes
- [fatal] L671 `Sample · Hanger Cut (tile, data-cat="apparel", /img/productcut/hanger-cut.png)` → No hanger-cut recipe exists. src/recipes/seeds/recipes.productcut.v2.js:6 records the deletion: '(2026-07-08 옷걸이·오브제·디테일 컷 제거 — 배경색 지정 지원 컷만 유지.)' = h
- [fatal] L722 `Free 카드 불릿: '✓ Solo lookbook' · '✓ 2 concurrent slots'` → src/lib/entitlements.js:12-16 PLANS의 필드는 monthlyCredits·watermarkFree·hd·commercial·privateMode 5개뿐. grep 'slots|lookbook|conceptCut|editResults' src/
- [fatal] L728 `Starter 카드 불릿: '✓ No watermark' · '✓ Group lookbook' · '✓ 3 concurrent slots'` → ①'No watermark' — src/generate/generate.route.js:385가 플랜 무관 `watermarked: false` 하드코딩(파일 전체 watermarked 히트 1건). Free도 워터마크가 없으므로 유료 혜택으로 파는 건 거짓. ②'Gr
- [fatal] L734 `Standard 카드 불릿: '✓ Edit results' · '✓ 4 concurrent slots'` → entitlements.js:14에 editResults·slots 축 없음(grep 0건). 문서 §⛔ (L108)이 명시: laongen은 '수정하기'를 STANDARD부터 여는 명시적 기능으로 파는데, 우리 :734는 같은 자리·같은 티어에 같은 혜택을 코드 근거
- [fatal] L740 `Pro 카드 불릿: '✓ Concept cut' · '✓ 5 concurrent slots'` → entitlements.js:15에 대응 축 없음(grep 0건). 'Concept cut'은 문서 §⛔ (L106) '호리즌컷 — Doppia 코드에 대응 기능 없음'과 같은 계열의 무근거 기능명.
- [fatal] L746 `Premium 카드 불릿: '✓ 6 concurrent slots'` → entitlements.js:16에 slots 축 없음(grep 0건). 같은 줄의 'Commercial license included'는 entitlements.js:16 commercial:true로 현재는 참이나, 승인된 starter/standard/pro의 c
- [fatal] L691 `#how 3단계: 'Generate & post … Download, or publish straight to Instagram & TikTok.'` → grep -c 'tiktok' public/studio.html = 0건. 게시 연동이 코드에 존재하지 않는다. 구조적으로 최악의 배치 — #how는 '내가 뭘 해야 하나'(노동량)를 닫는 섹션이고 laongen의 워크플로우 섹션에 대응하는 자리인데, 그 마지막 단계가
- [serious] L581 `<span>Runway</span> — inside the strip headed "The AI engines behind Doppia"` → Disproved four ways: src/tools/registry.js:85 enabled:false (excluded from the catalog by generate.route.js:542-543 enabledOnly:true, so unselectable)
- [serious] L581 `<span>MiniMax</span> — inside the strip headed "The AI engines behind Doppia"` → src/tools/registry.js:89 states MiniMax is not yet registered ('#9 어댑터 웨이브에서 검증 후 등록') and it has no TOOLS entry at all; prod .env MINIMAX_API_KEY emp
- [serious] L579 `<p>The AI engines behind Doppia</p>` → The header asserts the list is the complete, current engine set. It is neither: 2 of 7 cannot run (registry.js:85,:89 + empty keys + privacy.html:189)
- [serious] L722 `✓ Solo lookbook · ✓ 2 concurrent slots  (Free tier bullets)` → Neither exists in code. PLANS (src/lib/entitlements.js:12-16) has exactly five fields — monthlyCredits, watermarkFree, hd, commercial, privateMode. No
- [serious] L728 `✓ Group lookbook · ✓ 3 concurrent slots  (Starter tier bullets)` → Same as line 722: no slot field in PLANS (src/lib/entitlements.js:12-16), no concurrency limiter in src/, and generate.route.js:91 caps batch at 8 for
- [serious] L734 `✓ Edit results · ✓ 4 concurrent slots  (Standard tier bullets)` → No slot field in PLANS (src/lib/entitlements.js:12-16). 'Edit results' is not plan-gated: the edit/refine surfaces (src/generate/generate.route.js:954
- [serious] L740 `✓ Concept cut · ✓ 5 concurrent slots  (Pro tier bullets)` → No slot field in PLANS (src/lib/entitlements.js:12-16). 'Concept cut' appears nowhere in src/ — not in PLANS, not in src/tools/registry.js, not in any
- [serious] L746 `✓ 6 concurrent slots  (Premium tier bullet)` → No slot/concurrency field exists in PLANS (src/lib/entitlements.js:12-16) or anywhere in src/. generate.route.js:91 caps batch size at 8 for Premium a
- [serious] L840 `LP-CONV converter: var PRO=300,FLASH=90,VIDEO=3125 → renders '≈ 5–16 photos' (free), '≈ 24–81 photos or 2 ad videos' (st` → The upper bound divides by the Flash rate (◈90, credit.service.js:11), but Flash is hidden in prod: public/js/flags.js:25 `'model-flash':{ launch:fals
- [serious] L560 `◈ 1,500 free credits · No credit card · Cancel anytime (repeated at line 759)` → 'Cancel anytime' implies a recurring subscription to cancel; none exists (subscription.service.js:99 prepaid pass, entitlements.js:24 '자동재청구 없음'), and
- [serious] L717 `Monthly credits with your plan, top up anytime.` → 'Monthly credits' is false: src/subscription/subscription.service.js:121 grants `monthlyCredits * m` in ONE addCredits call at :123, with no scheduler
- [serious] L726 `$19 /mo  (identical pattern at lines 720, 732, 738, 744)` → '/mo' misstates the billing period. The only contract is a 3-month prepaid block: subscription.route.js:29 (`months = 3`), billing.html:428 (`months:3
- [serious] L727 `◈ 7,300 credits / month  (identical pattern at lines 733, 739, 745)` → Presents a lump sum as a monthly delivery. subscription.service.js:121-123 grants the entire term's credits at activation in a single call. Even publi
- [serious] L747 `Choose Premium (and the four sibling plan CTAs)` → The entire 5-tier table is a funnel to a dead end. The CTA routes to /billing, where Subscribe calls POST /api/subscription/upgrade → src/subscription
- [serious] L691 `Get a full set of photos or a ready-to-post reel in seconds.` → False for reels. Video is an asynchronous polled job: src/generate/videoJob.service.js:20 `const MAX_ATTEMPTS = 48; // 48 * 15s = 12분 후 타임아웃` — a sing
- [serious] L640 `✓ Cutout, editorial hero & on-model templates` → No template named or categorised 'Cutout' exists. Full category census across src/recipes/seeds/ yields: Reel(46), Hero(32), OnModelEditorial(24), Lif
- [serious] L638 `Choose a ready-made template and get catalog-ready shots — cutouts, editorial heroes and on-model looks — from a single ` → 'cutouts' is unfounded for the same reason as line 640 — no Cutout category exists, and no cutout/alpha output is produced (output is an opaque PNG, g
- [serious] L581 `The AI engines behind Doppia: Nano Banana · Gemini · Kling · MiniMax · Runway · ElevenLabs · Claude` → Two of seven are not behind Doppia. Runway — src/tools/registry.js:83-85 enabled:false. MiniMax — no TOOLS entry at all; registry.js:88 says '추가 툴(min
- [serious] L668 `Gallery tile: background /img/models/f-east_asian-2.jpg, labeled 'Apparel' / 'Sample · On-model'` → This is a house-model casting headshot, not a product output. public/js/models.roster.js identifies f-east_asian-2 as roster model 'Aoi'; scripts/buil
- [serious] L677 `Gallery tile: background /img/models/f-hispanic_latino-2.jpg, labeled 'Apparel' / 'Sample · On-model'` → Identical defect to :668 — models.roster.js identifies this as roster model 'Beatriz'; scripts/build_model_roster.js:112 produces a plain casting port
- [serious] L654 `Every image below is a real Doppia output.` → Falsified by its own grid: 2 of 16 tiles (:668, :677) are model-roster casting headshots from scripts/build_model_roster.js:112, not template outputs.
- [serious] L561 `Every photo and video here is a real Doppia sample output.` → Same universal-quantifier failure as :654, page-wide: the gallery contains 2 roster headshots (:668,:677 via models.roster.js / build_model_roster.js:
- [serious] L568 `Hero reel Instagram overlay: '9,999+' likes, '999+' comments, '999+' saves, '@doppia.ai' with a 'Follow' button, 'Origin` → Fabricated social proof. These are hardcoded strings in the markup with no data source — no analytics, no API, no engagement store anywhere in src/. T
- [serious] L691 `Get a full set of photos or a ready-to-post reel in seconds.` → src/videos/videoGeneration.service.js:37 sets maxWaitMs = 600_000 (10 minutes) with intervalMs 5_000, throwing 'Video generation timed out' at :52. do
- [serious] L552 `Upload one photo, tap a ready-made template — and get studio-grade product & model photos and a narrated ad video. No pr` → The 'and a narrated ad video' half sells the flag-off product: flags.js ugc:{launch:false} + reels:{launch:false}; studio.html:2888/:2927 return 'comi
- [serious] L560 `◈ 1,500 free credits · No credit card · Cancel anytime` → 'Cancel anytime' has no referent. src/subscription/subscription.route.js exposes only GET / (:7), POST /offer/start (:14), POST /upgrade (:27) — no ca
- [serious] L759 `◈ 1,500 free credits · No credit card · Cancel anytime` → Duplicate of :560 in the CTA band — same defect: no cancel route in src/subscription/subscription.route.js, no auto-renewal per entitlements.js:26.
- [serious] L709 `Photos + video — from the same shot` → The 'video' half is flag-hidden: flags.js ugc:{launch:false} and reels:{launch:false}; studio.html:2927 'UGC ads — coming soon.' Appears in the sectio
- [serious] L606 `Turn one product shot into a narrated ad video, or a full set of studio photos — the same photo, no reshoots.` → 'narrated ad video' is unreachable (flags.js ugc:false/reels:false; studio.html:2888,:2927), and 'no reshoots' repeats the unwired product-lock guaran
- [serious] L697 `Official templates by category: Apparel · Cosmetics · Nail · Accessories — Nail links live to /studio?mode=product&verti` → No nail vertical exists — `ls src/recipes/seeds/ | grep -ci nail` = 0 across all 28 seed files; the only 'nail' strings in src/ are anatomical prose i
- [serious] L7 `<title>Doppia — One product photo. Studio photos & ad videos.</title>` → 'ad videos' is in the title tag — the highest-visibility surface on the page (SERP, tab, share cards) — for a mode blocked by flags.js ugc:{launch:fal
- [serious] L8 `<meta name="description">…Doppia generates studio-grade product & model photos and narrated ad videos for Reels & TikTok` → Two defects: 'narrated ad videos' is flag-off (flags.js ugc:false, studio.html:2927), and 'for Reels & TikTok' implies the publishing that does not ex
- [serious] L589 `There's no prompt box in Doppia — you pick a ready-made template and it renders.` → A prompt box exists and is not flag-hidden: public/studio.html:811 `<button id="modeCustom" onclick="setMode('custom')">Custom</button>` carries no da
- [serious] L668 `<a class="gtile" data-cat="apparel" ... style="background-image:url('/img/models/f-east_asian-2.jpg')"> Apparel / "Sampl` → This is not an on-model output — it is the model picker's own INPUT thumbnail. /img/models/f-east_asian-2.jpg is roster entry 'Aoi' (public/js/models.
- [serious] L677 `<a class="gtile" data-cat="apparel" ... style="background-image:url('/img/models/f-hispanic_latino-2.jpg')"> Apparel / "` → Identical defect. /img/models/f-hispanic_latino-2.jpg is roster entry 'Beatriz' (Brazilian, 24) wearing 'a black crew-neck t-shirt' from the roster's 
- [serious] L691 `Get a full set of photos or a ready-to-post reel in seconds. Download, or publish straight to Instagram & TikTok.` → 게시 연동이 없다. public/studio.html에 tiktok|instagram|publish 매치 **0건**(grep -c = 0). 발행 모듈 src/publishing/*는 존재하나 src/index.js:145-148에서 레거시 `/heyhoai/acco
- [serious] L552 `Upload one photo, tap a ready-made template — and get studio-grade product & model photos and a narrated ad video. No pr` → Ad Video has no templates and cannot start without a written brief. src/ugc/ugcVideo.service.js:255 throws 400 if concept is missing; public/studio.ht
- [serious] L690 `Choose a photo template, or switch to Ad Video for a narrated short. No prompt writing, no settings to fiddle with.` → Both halves fail for Ad Video. 'No prompt writing' — a concept brief is mandatory (ugcVideo.service.js:255); only half-rescued by 'Suggest a concept f
- [serious] L728 `Starter tier bullet: '✓ Ad Video reels' (implying Ad Video is a Starter-and-up entitlement).` → No plan gate on Ad Video exists anywhere. src/lib/entitlements.js:10-17 PLANS has no video field; requirePlan() (entitlements.js:53-66) has zero call 
- [serious] L8 `<meta name="description">: Drop in one product photo, pick a template, and Doppia generates studio-grade product & model` → Same defect as :552, in the SERP snippet. 'pick a template … narrated ad videos … No prompts' — Ad Video has no template and requires a brief (ugcVide
- [serious] L550 `Hero pill: 'Template-based · zero prompts'` → The page's own #1 flagship, placed 60 lines below (landing.html:602 comment: '핵심 주력 2개(플래그십): Ad Video 먼저'), is neither template-based nor prompt-free
- [serious] L689 `One product photo — any angle. We remove the background and lock the product so it stays true across every frame.` → Neither mechanism exists. (1) Background removal: grep -rniE 'removeBackground|remove_background|bgRemov|backgroundRemoval' across the repo (excluding
- [serious] L641 `✓ Product stays true in every frame` → Stated as an outcome guarantee; only prompt text backs it. recipeResolver.js:154-168 composes look.extra_positive/extra_negative into each job and sto
- [serious] L640 `✓ Cutout, editorial hero & on-model templates` → There is no cutout template. Zero of the 208 seed objects contain 'cutout' in name (verified via recipeStore.listSeed); the only /\bcut\b/ names are V
- [serious] L674 `Sample · Product Cut (tile, /img/productcut/product-cut.png)` → Byte-identical duplicate of the :665 tile. md5(public/img/productcut/product-cut.png) = md5(public/img/productcut/ghost-mannequin-cut.png) = 9be7d6720
- [serious] L589 `There's no prompt box in Doppia — you pick a ready-made template and it renders.` → A prompt box ships, unflagged and on the main toolbar. public/studio.html:816 renders <button id="customCta" onclick="toggleCustom()">Custom</button> 
- [serious] L750 `'Commercial license is included with Premium — and Studio offers it right when you publish.'` → grep -c 'commercial' public/studio.html = 0건. Studio에 라이선스 제공/업셀 표면이 없다. 문서 §직답 (L7) 및 §Now-1 (L42-43)이 지목한 '없는 기능 업셀'.
- [serious] L717 `pricing 서브헤드: 'Monthly credits with your plan, top up anytime.'` → 바로 아래 :721 Free 카드가 '1,500 credits to try'인데 같은 시야의 서브헤드가 'Monthly credits'라 'Free도 매월 1,500'으로 읽힌다. billing.html:143은 '1,500 credits (one-time)'로 다르게
- [serious] L560 `히어로 리스크 해소문: '◈ 1,500 free credits · No credit card · Cancel anytime'` → 해지할 자동결제가 존재하지 않는다 — subscription.service.js activatePlan(userId, plan, nowMs, months = 3)은 선불 기간권이고 주석이 '자동재청구 없음(eximbay 정기결제 오픈 전)'이라 명시(문서 §Next-2
- [serious] L668 `#templates Apparel 탭 타일: f-east_asian-2.jpg (:677 f-hispanic_latino-2.jpg) — 'Sample · On-model'로 노출` → models.roster.js의 로스터 헤드샷을 '실제 산출물 샘플'로 제시. :654 'Every image below is a real Doppia output' 선언 바로 아래의 반례(문서 §Now-1, L42-43). 구조적 악화 요인: 방문자가 제품을 검증하러
- [serious] L554 `히어로 bchip: '✓ Product locked across frames' (동류 :642 · :689 · :708)` → 코드가 뒷받침하지 않는 절대보장문. 문서 §Now-3 (L50-51): 초기 버티컬인 주얼리·뷰티에서 가장 먼저 깨지는 축이라 최악의 조합. laongen 구조 대비의 함의: laongen 가격/기능 축은 '결제 후 그 자리에서 참·거짓이 확인되는 이산값'인데, 'lo
- [serious] L568 `.rslot.sns 가짜 소셜지표: '9,999+' 좋아요 · '999+' 댓글 · '999+' 저장 · '@doppia.ai Follow' · 'Original audio · Doppia'` → 실제 소셜 계정 지표가 아닌 장식. 문서 §⛔ (L102): Doppia 트랙션은 0(라이브 계정 생성물 118개). laongen이 '900+ 브랜드·1,388,118 프로젝트'라는 실데이터로 이기는 자리를, 우리는 가짜 지표 UI로 흉내내고 있다 — 구조적으로 같은
- [minor] L581 `<span>Nano&nbsp;Banana</span><span>Gemini</span> — listed as two separate engines` → One engine, one vendor, one key. nanoBanana.provider.js:1 imports @google/genai and :11 authenticates with GEMINI_API_KEY; registry.js:34,41 both decl
- [minor] L577 `<!-- ENGINE STRIP (honest capability proof) -->` → Source comment claiming honesty on a strip that is 3/7 wrong (landing.html:581 vs registry.js:85,:89 and privacy.html:189). Not user-visible, but it i
- [minor] L580 `<div class="logos"> with CSS .logos{opacity:.6} and .logos span{color:var(--dim)} (landing.html:154-155)` → Not a falsehood but a self-inflicted wound worth fixing in the same pass: the four claims that ARE true (Gemini, Kling, ElevenLabs, Claude) are render
- [minor] L717 `Monthly credits with your plan, top up anytime. Bigger plans cost less per credit.` → Half right. 'Top up anytime' is TRUE (src/pricing/pricing.config.js:43-48 packs, src/billing/billing.route.js:46,:189) and 'bigger plans cost less per
- [minor] L746 `Commercial license is included with Premium — and Studio offers it right when you publish.` → TRUE today (src/lib/entitlements.js:16 premium commercial:true vs :13-15 others false) but INVERTS under this workstream's approved starter/standard/p
- [minor] L717 `Monthly credits with your plan, top up anytime. Bigger plans cost less per credit.` → Misleading on both halves. 'Monthly credits with your plan' implies recurring monthly delivery, but entitlements.js:26 says '자동재청구 없음' and :27-30 expi
- [minor] L750 `<a href="/business" data-flag="business">Enterprise & agencies →</a>` → Dead copy in prod. public/js/flags.js business:{launch:false,label:'hidden'} → flags.js apply() sets el.style.display='none', and PAGE_FLAG maps '/bus
- [minor] L506 `Nav mega-link: 'Templates — Ready-made looks — no prompts'` → Accurate for the template path (recipeResolver.js:152-165), but the href is '#templates' — an on-page anchor to the sample gallery, not a template bro
- [minor] L509 `Nav mega-link: 'Ad Video — Narrated shorts for Reels' → /studio?mode=ugc` → Same dead-end as the flagship card: flags.js ugc:{launch:false}; studio.html:2888 toasts 'UGC mode is coming soon.' and :2927 disables the generate bu
- [minor] L774 `Footer 'Product' column: <a href="#advideo">Ad Video</a>` → Lists the flag-off mode (flags.js ugc:false) as a shipped product line in the footer nav.
- [minor] L771 `Footer: 'Studio-grade photos and ad videos from one product photo. No prompts, no studio.'` → 'ad videos' — flags.js ugc:{launch:false}, studio.html:2927 'UGC ads — coming soon.' Fourth restatement of the same unshipped promise (after :7, :8, :
- [minor] L616 `Ad Video badge: '✦ Flagship · New'` → 'New' implies recently shipped. flags.js labels it '준비 중' (in preparation) and reels 'v1.1로 연기' (deferred to v1.1). 'Flagship' ranks it first among tw
- [minor] L750 `Selling products or running ads? Commercial license is included with Premium — and Studio offers it right when you publi` → 두 겹. ① "Studio offers it right when you publish" — publish 개념 자체가 studio.html에 없음(publish 매치 0건). 결제 시점 상업 라이선스 업셀 UI 근거 0건. ② 이 문장은 **Premium에만** 상업 
- [minor] L746 `Everything in Pro / Commercial license included / 6 concurrent slots` → "Commercial license included"(Premium)는 terms.html:211·entitlements.js:17 `commercial:true`와 정합 = 참. 다만 **코드 강제는 0**(`commercial` 플래그가 src/ 어디서도 read되
- [minor] L840 `Credit-conversion script: var PRO=300,FLASH=90,VIDEO=3125 → renders 'or N ad videos' on each paid tier.` → VIDEO=3125 assumes 5 scenes x 625 (credit.service.js:17-20), but 5 scenes is not a code default — public/studio.html:905 ships #ugcSceneCount='0' (Aut
- [minor] L660 `Nail · soon (disabled gallery tab; same at :524 'Nail — Coming soon' and :597 disabled chip)` → The page contradicts itself and the code. At :694-696, under the heading 'Official templates by category', Nail is a live, enabled link: <a href="/stu
- [minor] L570 `<div class="mq-card"><span class="t">Product cut</span><img src="/img/productcut/styled-object-cut.png"> (hero marquee, ` → styled-object-cut is a deleted recipe, same removal as hanger-cut: src/recipes/seeds/recipes.productcut.v2.js:6 '(2026-07-08 옷걸이·오브제·디테일 컷 제거)'. No re

## 3. 미개발 자산 (62) — 코드엔 있는데 안 파는 것
- **The four-engine pipeline told as a sequence, with each engine's actual job attributed** (랜딩 언급 0회) — This is the whole ballgame and the page gives it seven undifferentiated words at opacity .6. 'One photo in. Claude writes the script, Gemini renders every scene, Kling brings it to
- **Named model tiers, not just vendor names** (랜딩 언급 0회) — 'Claude' is a logo. 'Claude Opus writes your script' is a spend decision the buyer can price — we put our most expensive model on the script and the prompt-enhance path (config/ind
- **Two independent audio engines (ElevenLabs voice + ElevenLabs Music v2 + Kling video-to-audio SFX)** (랜딩 언급 0회) — The landing's Ad Video card says only 'AI voice-over & music' (landing.html ~:606) — flat feature-listing. The real story is three separately generated audio layers and a license-c
- **ElevenLabs with-timestamps drives caption sync AND scene re-timing** (랜딩 언급 0회) — Explains WHY the video feels cut-to-the-voice rather than voice-pasted-over-video — a quality difference viewers feel and cannot name. Fine-print/technical-page material, not headl
- **Private Mode — the only code-enforced paid feature in the entire product, and the landing never once mentions it** (랜딩 언급 0회) — Every fabricated bullet in the pricing table (slots, lookbooks, edit, concept cut, no-watermark) was invented to fill space that this real, enforced, five-call-site feature could h
- **The public feed publishes each Free user's email local-part as their public handle** (랜딩 언급 0회) — Turns Private Mode from a soft feature into an urgent one, and makes the pitch concrete rather than abstract. A seller signing up as `brandname@company.com` is publishing `@brandna
- **Automatic refund on failed generations — full refund if nothing renders, per-image partial refund otherwise** (랜딩 언급 0회) — 'You only pay for what actually renders' directly answers the #1 objection to credit-based AI pricing — fear of burning money on garbage output. It is real, it is on every generati
- **Team credit pools with role-based permissions** (랜딩 언급 0회) — Marketing agencies are one of the three named target segments, and shared-pool-plus-roles is exactly what an agency evaluates on. It is built and enforced, yet `grep -oic team publ
- **Automatic per-photo refund on generation failure** (랜딩 언급 0회) — The #1 objection for a commercial seller evaluating AI generation is 'I pay for garbage output.' The product already answers it, granularly (5 photos requested, 2 fail → you are ch
- **Atomic, fully auditable credit ledger** (랜딩 언급 0회) — Korean smartstore/Cafe24 sellers and agencies expense this and need per-item receipts; agencies re-bill clients. 'Every credit, itemized, with a running balance and the render it p
- **No overage, no surprise bill, no forced auto-top-up** (랜딩 언급 0회) — Directly inverts the dominant fear of usage-based AI pricing ('it silently ran up $400'). The hard ceiling is a feature, currently framed as a missing one. Copy: 'You can't overspe
- **Custom prompts cost half of templates (◈150 vs ◈300)** (랜딩 언급 0회) — Reframes the free tier from a thin '5 photos' to '10 custom photos or 5 template photos' — doubling the perceived trial without changing a single number, and it's the one lever tha
- **Volume discount curve is real and monotonic** (랜딩 언급 1회) — Currently a trailing clause on landing.html:717 next to two falsehoods, so it reads as filler. It's one of the few pricing claims that survives verification — it deserves to be the
- **No auto-renewal, by construction — the anti-dark-pattern trust pitch** (랜딩 언급 0회) — This is the headline finding. The landing fabricates 'Cancel anytime' to claim a feature it lacks — while the thing it ACTUALLY has (no auto-renewal, nothing to cancel, no card on 
- **Admin-issued company trial accounts — a fully built, enforced B2B pilot rail** (랜딩 언급 0회) — The brief's target is 대행사 + DTC brands — all commercial. While self-serve 'Subscribe' returns 501, this pilot rail is built, mounted, and actually gated at generation time. It is t
- **Failed generations auto-refund credits** (랜딩 언급 0회) — Directly answers the #1 objection to credit-metered AI tools: 'do I pay for garbage?' It's implemented, multi-path, and needs no ask from the user. For sellers evaluating cost-per-
- **Credits never expire** (랜딩 언급 0회) — Pairs with the prepaid-pass framing into a coherent 'no pressure' story: buy a term, keep what you don't burn. Strong for seasonal sellers (명절/시즌 catalogs) whose volume is spiky an
- **Fair upgrade math — remaining term carries over, downgrades blocked rather than silently applied** (랜딩 언급 0회) — 'Upgrade mid-term and lose nothing' is a real, working fairness guarantee most competitors handle badly. Currently unreachable behind the 501, so it's a launch-day asset rather tha
- **Ten aspect ratios for PHOTOS — and the landing never states a single one of them** (랜딩 언급 0회) — This is the exact problem the target buyer has. A Korean SmartStore/Cafe24/Shopify seller needs 1:1 for the listing thumbnail, 4:5 for the IG feed, 9:16 for Reels, and taller/wider
- **4:5 is the default — a correct, deliberate Instagram-feed decision nobody is told about** (랜딩 언급 0회) — Defaults are the cheapest possible trust signal: it proves the product was built by someone who knows that 4:5 is the largest ratio the IG feed will accept. Competitors default to 
- **Ratio is a per-generation control, not a per-template property — one upload holds one look across every placement** (랜딩 언급 0회) — This is the mechanism that makes the 'One product photo becomes a week of content' headline (landing.html:551) literally true instead of aspirational — and the headline currently h
- **The product already speaks the seller's platform language in-app; the landing doesn't** (랜딩 언급 0회) — The landing's job is to promise what the product delivers; here the product is already more fluent than the landing. Lifting this exact vocabulary onto the landing costs nothing, i
- **Ad Video's exact, enforced spec — 1080×1920, 30fps, H.264 MP4 — the hardest provable number in the codebase** (랜딩 언급 0회) — Unlike photo pixel dims (a client-side estimate, studio.html:1886-1887), these are dimensions our own ffmpeg enforces — we can state them as a guarantee and be right. Agencies and 
- **Up to 8 images per run, defaulting to 4 — the arithmetic behind 'a week of content'** (랜딩 언급 0회) — The hero headline promises 'a week of content' with no supporting number anywhere on the page. 8 images × 10 ratios from one upload is the actual math, and it is verifiable. Quanti
- **2K output (~1824×2288 at 4:5) on the default engine, available to everyone including Free** (랜딩 언급 0회) — Detail-page hero images are where Korean sellers actually need pixels, and 2K clears that bar on the default engine — no upsell, no configuration. Two cautions: state it as 'up to 
- **privateMode — the only entitlement actually enforced in the entire codebase** (랜딩 언급 0회) — This is the ONLY thing a paid plan actually buys besides credits — and it is the single most commercially relevant fact on the page for the target audience. Korean smartstore/Cafe2
- **KRW-native pricing with VAT included, at a pegged FX rate** (랜딩 언급 0회) — The stated target is Korean smartstore/Cafe24 sellers, and Korean SaaS buyers price-shop in VAT-inclusive won — a '$19/mo' headline reads as a foreign tool with an unknown final in
- **80 house models × 40 backgrounds = 3,200 on-model combinations, no casting, no release forms** (랜딩 언급 1회) — A DTC brand shooting one on-model apparel set pays for casting, model day rates, a studio, and usage rights — and gets one ethnicity in one location. This is the single largest con
- **10 export aspect ratios with 4:5 (Instagram feed) as the default** (랜딩 언급 0회) — Sellers need the same SKU at different crops for a Cafe24 product grid, an IG feed post, and a Naver listing — and re-cropping is the tedious part of the job Doppia claims to remov
- **Claude Opus 4.8 as the ad-script writer (top-tier model on the creative step)** (랜딩 언급 1회) — Deliberately spending the most expensive model on the step that determines creative quality, while using a cheaper one for captions, is a real quality signal competitors flatten in
- **The 7-guard product-fidelity taxonomy — designed, documented, and wired to nothing** (랜딩 언급 4회) — This is the asset that would let Doppia legitimately make the fidelity claim the landing already makes four times (:554, :642, :689, :708). The analysis is done and the taxonomy is
- **The 80-model roster itself, with its 8 ethnic groups / 40 nationalities / both genders** (랜딩 언급 2회) — The landing never states a single number. '80' and '40 nationalities' are verifiable, concrete, and instantly differentiating — and they are the rare Doppia claim that survives adv
- **The 40-background scene library** (랜딩 언급 1회) — 40 finished thumbnails (4.6MB) are sitting in the repo, unused. For a Korean seller the pitch 'studio cut today, Seoul café scene tomorrow, snowy street for the winter campaign — s
- **Korean and East Asian models for the actual target buyer** (랜딩 언급 0회) — The buyer is a Korean 스마트스토어/카페24 seller. That Korean models exist AND that Black/European/Latino/South Asian models exist for the same product is the export story (sell domestical
- **The roster is the one spine shared by Photos AND Ad Video** (랜딩 언급 0회) — Directly relevant to the open Ad-Video-vs-Photos question. The roster is the only asset that doesn't force a choice — it's the shared vertebra. Evidence-based proposal (not a decis
- **120 finished thumbnails already shipped in the repo** (랜딩 언급 0회) — An on-model configurator strip (model row + background row, mirroring studio.html:2633/:2661 labels) can be built from assets that already exist — no new generation, no new cost, n
- **PIPA 제28조의8 완전 준수 국외이전 고지표 (벤더·연락처·국가·항목·목적·시기·보유기간 7칸)** (랜딩 언급 0회) — 대행사·브랜드 구매 심사(vendor security review)에서 가장 먼저 요구하는 문서다. 국내 AI SaaS 경쟁사 상당수가 '해외 AI API 씁니다' 수준으로 뭉개는데 Doppia는 벤더별 연락처까지 있다. 이건 이미 만들어져 있고, 랜딩이 0번 언급한다. 'Ask us where your data goes
- **AWS 서울 리전 데이터 레지던시 + 미디어가 국외 오브젝트 스토리지로 안 나감** (랜딩 언급 0회) — 한국 셀러·대행사에게 '데이터가 한국에 있다'는 구매 심사 통과 요소다. 이미 참인데 랜딩이 0번 말한다. 'Your data lives on AWS Seoul.' 한 줄이면 됨. (⚠️ prod .env 1회 확인 후 사용)
- **업로드물에 대한 좁은 라이선스 + 학습 미사용 명문화, 그리고 실제로 학습 코드 경로가 0** (랜딩 언급 0회) — '내 신제품 사진이 AI 학습에 들어가나?'는 브랜드가 반드시 묻는 질문이고, Doppia는 약관·방침·코드 3중으로 '아니오'라 답할 수 있다. 대부분의 경쟁사가 학습 유보 조항을 넣어두는 것과 대비된다. 랜딩 0번 언급. **단 주어를 Doppia로 한정**해야 함(수탁자 정책은 위임 상태 — 이게 진짜 갭이고, 벤더
- **Photos 경로 = 미국 벤더만 / Ad Video 경로 = 중국(Kling) 추가 — 프라이버시 축의 구조적 비대칭** (랜딩 언급 0회) — 과제가 물은 'Photos vs Ad Video 중 어느 쪽이 구조적으로 앞서는가'에 대한 근거 기반 답. 한국 엔터프라이즈·대행사 구매 심사에서 중국 이전은 단독 탈락 사유가 되는 일이 흔하다. Photos는 그 리스크가 0 → **프라이버시 축에서는 Photos가 명백히 앞선다**(제안일 뿐, 결정 아님). 둘 다 살
- **카드정보 미보유 (PG 위임) — 3개 법적 페이지에 일관되게 명문화** (랜딩 언급 0회) — 결제 신뢰 축. 특히 신생 SaaS에 카드 넣기 꺼리는 소상공인 셀러에게 유효. 'We never see your card number.' 랜딩 0번 언급.
- **AUP(딥페이크·유명인 초상·성적 콘텐츠 금지) + 전 페이지 딥링크 노출** (랜딩 언급 0회) — 브랜드 세이프티. 대행사가 클라이언트에게 툴을 제안할 때 'AI 툴 쓴다니 딥페이크 리스크는?' 방어에 쓰인다. 이미 있고 딥링크까지 걸려 있는데 랜딩 0번 언급.
- **개인정보 보호책임자 지정 + 권익침해 구제기관 4곳 안내 + 사업자 정보 전 페이지 자동 주입** (랜딩 언급 0회) — 전자상거래법 제10조 표시사항 베이스라인이 이미 충족됨 = PG 심사·법인 고객 심사의 최저선을 통과한 상태. 실명 대표자·유선 고객센터가 있는 건 '유령 AI 서비스' 대비 신뢰 요소.
- **The script is free, and you approve it line by line before any credit is spent** (랜딩 언급 0회) — The #1 objection to AI video for a seller spending ₩31,000/mo on ~2 videos is 'I'll burn my credits on garbage'. The product already solved it — a full free preview with unlimited 
- **Post-render scene surgery with version history — redo one scene from a plain-language note, add/reorder/delete scenes, f** (랜딩 언급 0회) — This is the actual moat versus every one-shot generator, and it is one of the most heavily engineered surfaces in the repo. The landing's only 'edit' word is the fabricated Standar
- **8 Korean-native voices + speed + [pause] marks, and copy rules that ban translationese** (랜딩 언급 0회) — Target is Korean 스마트스토어/카페24 sellers. 'AI voice-over' is a commodity claim every competitor makes. '8 Korean voices, and copy that doesn't read like it was translated' is a localis
- **Up to 5 angles of the same product, read together** (랜딩 언급 0회) — Answers the seller's real fear — 'will it still be MY product?' — with a mechanism instead of an adjective. The landing asserts the outcome ('Product locked across frames', :554) b
- **The AI writes the brief for you from the photo** (랜딩 언급 0회) — This is the mechanism that makes 'no prompt writing' nearly-true for Ad Video. Because the landing claims the benefit (:690) without ever showing the mechanism, the claim currently
- **Shot palettes per product category, auto-detected from the photo** (랜딩 언급 0회) — Explains why the output isn't generic — the code deliberately treats palettes as a menu 'so two ads should not look identical' (categories.js:4-5). Cheap credibility for a page who
- **4 photos per run, from ◈2 — the unit of value** (랜딩 언급 0회) — The landing never once says how many images a run produces. A smartstore seller's whole mental model is 'how many usable shots per SKU, for how much' — that is the purchase decisio
- **Product Hero's 19 preset cosmetics styles (and the 38-cut depth behind 7 families)** (랜딩 언급 0회) — The landing scatters Noir Gold, Dewy Glass, Gloss Mirror, Silk Drape, Sunlit Pop and Liquid Splash across the wall as anonymous 'Sample ·' tiles, never revealing they are six of ni
- **80 house models × 40 backgrounds, fully wired** (랜딩 언급 1회) — One passing bullet at :642 ('Pick a house model & background — no casting') with no numbers, while the page burns two gallery tiles showing roster portraits mislabeled as outputs. 
- **Jewelry Worn Cut — worn shots with no face in frame** (랜딩 언급 0회) — A whole shipped family, invisible on the landing. It answers a real objection nobody else answers: sellers want worn/scale shots but don't want an AI face on their PDP (identity ri
- **Ghost mannequin — a named SKU sellers already shop for** (랜딩 언급 1회) — Appears only as a tile caption ('Sample · Ghost Mannequin') and a mega-nav subtitle. Ghost mannequin is a term apparel sellers actively search and pay studios for — it is a named, 
- **FAQ 슬롯 자체 (pricing </section> :751 과 CTA 밴드 :754 사이) — 우리가 0을 가진 유일한 표면** (랜딩 언급 0회) — 차용 가능한 구조 중 유일하게 '노력 대비 격차 해소'가 선형이 아니라 계단이다. 0→1이라 만드는 즉시 much_better가 사라진다. 더 중요한 건 부수효과: FAQ는 조건절의 배수구라, 이게 생기면 가격 카드가 다시 날조로 채워질 구조적 유인이 사라진다(claims 8번). ⚠️ 의존성: 상업권 문항은 승인된 st
- **가격 그리드 위의 산문 층 (상품 형태를 닫는 자리) — laongen의 '구독vs충전형 문단' 슬롯** (랜딩 언급 1회) — 카드로 비교 불가능한 축(상품 형태·기간·정책)이 갈 곳이 여기뿐이다. 이 층이 없어서 형태 질문이 '/mo'(:726)와 'Cancel anytime'(:560,:759)으로 새어 3중 충돌을 만들었다(문서 §Next-2, L72). 슬롯은 이미 DOM에 있다(:717) — 신설이 아니라 용도 교정. ⚠️ 종속: 과금 
- **#templates 탭 구조 (:651-681, .gtab 5개 + 타일 16장) = laongen 탭 데모의 우리 버전, 이미 존재** (랜딩 언급 3회) — laongen이 이 차원에서 이기는 구조를 우리가 이미 소유하고 있다. 차용해야 할 것은 laongen의 탭이 아니라 laongen의 절제 — #demo(:586-600)·#products(:603-650) 삭제. 삭제만으로 ①스크롤이 반복→전진으로 바뀌고 ②pricing 도달률이 오르고 ③죽은 CSS(.demo-src·
- **On-model 컨피규레이터 스트립 — 모델 80 + 배경 40 로스터 (laongen '탭 7개 = 골라 쓰는 스튜디오'에 대한 우리의 유일한 동급 카드)** (랜딩 언급 0회) — claims 11번의 원리('탭은 조작을 리허설한다')를 우리 자산으로 실행하는 유일한 수단. 갤러리는 '결과가 좋다'만 말하지만 모델·배경 픽커는 '네가 고른다'까지 말한다 — 그게 구매 판단이다. 80명이 인종·국적·연령까지 구조화돼 있는데 랜딩 언급 0회. ⚠️ 선행 필수: :668·:677 로스터 헤드샷을 'Sam
- **카드 내장 크레딧 환산 (LP-CONV :839-840) — laongen보다 구조적으로 앞선 우리 자산** (랜딩 언급 5회) — 이 목록에서 유일하게 '베낄 게 아니라 지킬 것'. laongen 카드는 크레딧 수를 주고 의미를 FAQ로 미루지만 우리 카드는 의미를 카드 안에서 준다 — 비교 시점과 이해 시점이 일치한다. 지금 안 읽히는 이유는 환산이 나빠서가 아니라 같은 카드에 날조 불릿 3~4개가 붙어 시선을 나눠 갖기 때문. 날조 제거 = 환산
- **히어로 마퀴의 깊이 재구성 — 착용컷 자산이 이미 레포에 있음** (랜딩 언급 0회) — claims 2·3번의 원리를 자산 0으로 실행. 12장 6라벨(폭) → 착용컷 축 반복(깊이)로 바꾸면 :552가 약속한 'product & model photos'가 화면에서 처음으로 지켜지고, 사람과 제품이 한 프레임에 공존해 용도가 즉시 읽힌다. ⚠️ 짝이 되는 선행조건: 122px 카드에선 착용컷을 넣어도 제품이
- **nav를 페이지 목차로 전환 — rail.js가 이미 앱 내비를 담당하므로 중복 제거** (랜딩 언급 3회) — nav 자체가 설득 완료 전 이탈 장치다(파일 전체 /studio 링크 47개). 앱 내비가 이미 다른 파일에 있으므로 landing.html의 메가메뉴는 순 손실 — 제거에 기능 비용이 0이다. ⚠️ 순서: #faq 앵커를 넣으려면 FAQ 섹션 신설이 선행(지금은 죽은 앵커).
- **엔진 공개 스트립 (:578-583) — laongen에 없는 검증 가능한 신뢰 신호** (랜딩 언급 1회) — 트랙션 수치 슬롯(laongen의 900+ 브랜드)이 우리에겐 영구 공백인데, 그 자리를 메울 수 있는 유일한 정직한 신뢰 자산. 대행사·기술 구매자에겐 검증 가능한 신호다(각 엔진명을 독자가 직접 검색해 확인 가능 = 사회적 증거와 달리 우리 데이터가 필요 없음). 현재는 히어로와 #demo 사이의 장식 스트립으로 소비

## 4. 도메인별 핵심 발견 (SURPRISES) — 가장 중요

### engines
FIVE surprises, two of them serious.

(1) **The doc that framed this task is itself wrong — on both counts.** docs/laongen_대비_랜딩비교_2026-07-16.md:22 says the strip is at "landing.html:604-610" and calls it "우리 페이지에서 몇 안 되는 '100% 참인 주장'". The strip is at landing.html:577-583 (the wordmarks are one line, :581), and it is **3/7 wrong**, not 100% true: Runway and MiniMax cannot run, and Nano Banana/Gemini are the same engine. The one section the internal review certified as clean is a section nobody re-checked — because landing.html:577's own source comment says "(honest capability proof)". A label asserting honesty substituted for verifying it.

(2) **The landing contradicts our own privacy policy, and the privacy policy is the legally-filed one.** public/privacy.html:189 states in writing that Runway and MiniMax are 예비(폴백) processors and "**현재는 이용하고 있지 않으며**" (we do not currently use them). landing.html:581 advertises both as "The AI engines behind Doppia". This is not a marketing overreach — it is two pages of the same site making opposite factual claims about named third parties, and per docs/PG심사_준비_체크리스트_2026-07-13.md the privacy doc is under active PG review. A reviewer opening both tabs sees it immediately. It is also plain 표시광고법 exposure (거짓·과장 광고). **The good news: privacy.html is the accurate one — the fix is deleting two words from the landing, not amending a legal filing.**

(3) **Nano Banana IS Gemini.** nanoBanana.provider.js:1 imports @google/genai and :11 authenticates with GEMINI_API_KEY; adminRefine.route.js:78 literally calls gemini-3-pro-image-preview "Nano Banana Pro". The strip pads a 5-engine stack into 7 — in front of the technical/agency buyer this strip exists to impress, i.e. the one reader guaranteed to catch it.

(4) **Kling is a single point of failure with zero live redundancy.** videoGeneration.service.js:132-145 implements a careful fallback chain ['kling','minimax','runway'] — and in prod both MINIMAX_API_KEY and RUNWAY_API_KEY are empty, so the chain has exactly one link. REPLICATE_API_TOKEN and FAL_API_KEY are empty too. Every video path (clipPipeline.service.js:97 and videoJob.service.js:89) terminates at api.klingai.com. Never s

### entitlements_enforcement
Four things worth flagging.

(1) THE DOCS/COMMENTS ARE THE CONTAMINATION SOURCE, NOT JUST THE LANDING. src/tools/registry.js:36 carries the comment "2K=Pro 전용" — meaning the Nano Banana **Pro model**, since generate.route.js:106 gates 2K on `model === 'pro'` and never reads a plan. Someone read "Pro" as the **Pro plan** and shipped it into pricing.config.js:29 ("Edit · 2K · 4 slots") and the landing. A false pricing claim was manufactured by a naming collision in a code comment. This also means fixing landing.html alone will NOT fix it: billing/studio read PRICING from src/pricing/pricing.config.js, so the lie regenerates from the config.

(2) SUBSCRIPTION.SERVICE.JS:120 IS A COMMENT DESCRIBING CODE THAT DOES NOT EXIST. It claims "만료 시 잔여 소멸" (leftover credits expire at pass expiry). grep across src/credits/ and src/teams/ finds no expiry job, no sweep, and no plan_renews_at reference in either directory. Credits are a plain balance that survives expiry. Per the instruction to distrust docs: this one is an intention written in the past tense.

(3) THE PRIVATEMODE GATE FAILS **OPEN**, NOT CLOSED. generate.route.js:59 catches a DB error in canUsePrivate() and returns `false` — which at :88 means the result is published PUBLIC. The log line even says "defaulting to public". So a transient DB blip publishes a paying subscriber's private result to the Community feed, irreversibly (the feed is read by other users before :633 could un-publish it). This is the one axis the landing *should* sell, and it has a real bug behind it. It does not block the copy — the entitlement is genuine — but it argues for phrasing the claim as the plan grant ("Private Mode is included on every paid plan") rather than an absolute ("your results are never public"). Worth a bug ticket independent of the landing work.

(4) STRUCTURAL ANSWER TO "HOW MANY AXES CAN THE PRICING TABLE HONESTLY SELL?" — **Two, plus one contractual.** Enforced in code: **monthlyCredits** (granted subscription.service.js:121, gated team.credit.js:90 → 402) and **privateMode** (generate.route.js:58 + five call sites). Contractual only, legitimate but must be worded as a license: **commercial** (terms.html:210, no code 

### credit_economy
FIVE surprises, the first of which reframes the whole domain.

(1) **NO ONE CAN GIVE DOPPIA MONEY TODAY — both paths are hard-blocked.** Plans: src/subscription/subscription.route.js:30-36 returns HTTP 501 comingSoon to every non-admin, and it is the only route reaching activatePlan(). Packs: src/billing/billing.route.js:65-67 returns HTTP 503 unless LEMONSQUEEZY_API_KEY + LEMONSQUEEZY_STORE_ID are configured (billing.route.js:39) — I enumerated .env key names and there is NO payment provider key of any kind (no Lemon Squeezy, no Eximbay, no KCP, no PortOne; the file holds only PORT/NODE_ENV/model keys/DB/JWT/admin/provider API keys). Consequence: **SIGNUP_BONUS 1,500, granted once at account creation, is the ONLY credit any real user will ever hold.** The entire 5-tier table, the annual toggle, the KRW peg, the 50%-off first-month offer, the volume curve — all of it is display over a closed till. Every "/mo" on the landing is not just semantically wrong; it advertises a transaction the software refuses. This dwarfs the conversion-math bug I was sent to confirm.

(2) **The docs are wrong in three distinct ways, and I had to overrule them.** (a) docs/생성원가_마진_분석_2026-07-06.md:19-20 still describes the PRE-30x credit model ("이미지=장수×1 + 모델가산(flash 0/pro 1), 영상={5s:8,10s:16}") — flatly contradicted by credit.service.js:10-20 (flash [45,90] / pro [150,300] / 5s [625,1250]). The same file's §6 (lines 55-88) uses the current model, so the document contradicts itself. (b) The same doc cites "generate.route L1004·1037" for the isTemplate:false Ad Video charge; the real lines are **1020 and 1043** — claim correct, references stale. (c) The doc's §6-4 table (Free=0 ad videos, Starter=2, Standard=6, Pro=14, Premium=30) reproduces exactly against code, and its claim that landing uses Flash as the upper bound is CONFIRMED (1500/90=16 → "≈ 5–16 photos"). So docs were right on the arithmetic and wrong on the cost model — I verified every load-bearing number against source.

(3) **"만료 시 잔여 소멸" is a comment describing code that was never written.** src/subscription/subscription.service.js:120 asserts leftover credits are destroyed at pass expiry. I grepped for any credit_balance z

### billing_subscription
여섯 가지. **① refund.html이 이 레포에서 가장 정직한 과금 문서다** — 보통 법무 페이지가 뒤처지는데 여기선 반대다. refund.html:119가 '현재 서비스의 구독 정기결제 기능은 가맹점 심사 절차 진행에 따라 순차적으로 제공되며... 정기결제 개시 전 선불형 기간권으로 제공되는 경우에는...'라고 코드와 정확히 일치하게 명시하고, :85는 '기간분 크레딧이 일괄 지급'까지 맞게 서술한다. 즉 랜딩의 'Cancel anytime'은 **자사 약관과 정면 모순**이다. 이건 마케팅 과장이 아니라 refund.html:73이 스스로 인용한 전자상거래법 §17③(표시·광고와 다른 재화 = 3개월 청약철회) 트리거다. 거짓말이 스스로 법적 노출을 만들어낸다.

**② NHN KCP는 반려됐는데 코드는 아직도 한국 방문자 전원에게 'NHN KCP'를 결제사로 출력한다.** docs/결제창_개발명세_포트원_2026-07-14.md:14 = '~~NHN KCP~~ **반려됨**(디지털콘텐츠 충전업종 업력 2년 미만 계약불가) → 나이스페이먼츠 + 토스페이먼츠 병렬 신청 중'. 그런데 public/js/pricing.js:67과 pricing.config.js:49는 07-13자라 여전히 KCP를 박아둔다. **내 과업 브리핑의 전제('KRW → NHN KCP') 자체가 stale이다.** 그리고 KCP/PortOne 구현은 애초에 0줄(주석 2개뿐).

**③ 통화 토글은 결제에 아무 영향이 없다.** pricing.config.js:49가 '표시통화=결제통화=PG'라고 단언하지만, billing.html:383-396 buyPack()은 통화와 무관하게 Eximbay-우선/LS-폴백을 고르고, eximbay.service.js:36은 `currency: 'USD'`를 하드코딩한다. ₩31,000을 보고 결제한 한국 사용자는 달러로 청구된다. 표시가와 청구가가 통화 단위부터 다르다.

**④ JSDoc이 코드를 3배 틀리게 설명한다.** subscription.service.js:96 `@param {number} [months=1]` vs :99 `activatePlan(userId, plan, nowMs, months = 3)`. 문서만 읽고 1개월권 가격을 매기면 3개월치를 청구하게 된다. (반면 docs/원화_빌링키_설계_2026-07-13.md:69는 `months=3`을 정확히 적었다 — 같은 레포 안에서 문서끼리 충돌.)

**⑤ subscription.service.js:120 주석 '만료 시 잔여 소멸'은 어디에도 구현돼 있지 않고**, refund.html:99는 정반대('회원 자격 유지 중 유효')를 약속한다. 실제 코드 = 소멸 없음. 즉 주석이 틀렸고, 우연히 사용자에게 유리한 방향이다.

**⑥ 앱이 사적으로 인정하는 걸 랜딩이 공개적으로 부인한다.** billing.html:262가 'Manage subscription' 섹션 전체를 `display:none`으로 숨기며 남긴 주석 = '단발성 이용권 → 관리할 정기결제 없음(일시정지/취소 무의미)'. 제품은 해지할 게 없다는 걸 알고 UI를 지웠는데, 랜딩은 같은 화면 두 곳(560·759)에서 'Cancel anytime'을 판다. 덤으로 24h 50% 카운트다운(subscription.service.js:12-16, :40-55)은 offerPrice()를 계산해 표시하지만 activatePlan(:99-127)이 그 값을 절대 읽지 않는다 — 어떤 코드도 이행할 수 없는 할인의 실시간 타이머다.

**결론 — 코드가 지지하는 건 (B) 선불 기간권이다. 논쟁의 여지가 없다.** 근거 5중: activatePlan(months=3, subscription.service.js:99) · plan_renews_at=만료일이지 갱신일이 아님(:115/:118) · 만료 시 자동 강등만 있고 재청구 없음(entitlements.js:26-29) · 해지 엔드포인트 부재 · 빌링키/스케줄러/상태머신 전무(설계doc:11 '그린필드'). (A) 자동갱신을 지지하는 코드는 **단 한 줄도 없다.**

단, 더 중요한 사실이 (A)/(B)를 앞선다: **비관리자는 아무것도 살 수 없다**(route.js:30-36 → 501). 따라서 지금 정직한 가격 섹션은 과금 '조건'을 파는 게 아니라 과금 '상태'를 밝히는 것이다. 쓸 수 있는 문장: ① "Start with ◈1,500 free 

### output_formats
SIX findings, ordered by how much they should change the plan.

(1) THE BACKGROUND REMOVER IS A TRANSLATION ACCIDENT, NOT AN EXAGGERATION. I expected to find a weak or partial implementation. There is none — no dependency, no code, no alpha. I traced the origin: recipe categories are 'Product Cut' / 'ProductCut' / 'Worn Cut' (src/recipes/seeds/recipes.accessories.v2.js:37-39 and the category census). 제품컷 is Korean product-photography jargon for a *clean catalog shot*; 누끼 is the word for a cutout/alpha. Someone rendered 'Cut' → 'Cutout' → then promoted it into a named feature, 'Background Remover — Automatic on every upload' (landing.html:510). The codebase says the exact opposite of the sell: recipes.jewelry.v2.js:829-830 instruct the model to AVOID a cutout look ('never a flat dead cutout void', extra_negative: 'cutout void'). This means it cannot be fixed by softening the copy — 'cutout' must be deleted at landing.html:510, 554, 638, 640, plus home.html:152/165/166 and the Korean strings at i18n.js:180/191 that already carry the lie into a second language.

(2) DOCS/MEMORY WERE WRONG ON VIDEO RESOLUTION. The project memory records '씬영상 릴720' (720-class reels). Grepping 720 across src/ugc/, src/videos/ and videoJob.service.js returns ZERO hits. The real values are 1080-class everywhere: renderPlan.js:79 (1080×1920 / 1080×1080 / 1920×1080) and clipPipeline.service.js:13-14 (REELS_W=1080, REELS_H=1920). Had I trusted the note, I would have under-sold the strongest spec in the product by a full tier. Flagging per instruction 3.

(3) 2K IS THE WATERMARK BUG A SECOND TIME. entitlements.js:12-16 carries an `hd` flag per plan that is read NOWHERE — generate.route.js touches entitlements exactly once, at :58, for privateMode. 2K is gated purely on model==='pro' (:106), so a Free user on the default engine gets 2K. This is structurally identical to watermarkFree/generate.route.js:385: a field that exists, looks like a tier lever, and enforces nothing. Anyone reading entitlements.js to write pricing copy will make the same mistake twice. Treat `hd` as unsellable exactly like watermark.

(4) THE UI'S PIXEL NUMBERS ARE A GUESS, AND THE CODE ADMITS IT. studio.html:1888-1894

### landing_falsehood_sweep
FIVE surprises, ordered by consequence.

**1. The page's #1 flagship is switched off in production — and the page already knows how to hide it.** This is the biggest finding of the sweep and it is not in any doc. `public/js/flags.js` carries TWO independent kill switches: `ugc:{launch:false, label:'hidden'}` ('UGC Ads 모드 — 준비 중') and `reels:{launch:false}` ('Kling 영상 — v1.1로 연기'). Behind them, `public/studio.html:2888` toasts `'UGC mode is coming soon.'` on upload and `:2927` disables the generate button with `'UGC ads — coming soon.'`; `:1513` stamps `m.soon=true` on every ugc template. `flags.js` PAGE_FLAG has no `/studio` entry, so `/studio?mode=ugc` — the exact URL the landing links from the flagship CTA (:609), the nav (:509) and the footer (:774) — loads and then refuses. Meanwhile the landing carries exactly one `data-flag` (`business` at :750), proving it knows the mechanism and simply never applied it here. So the page ranks Ad Video FIRST, badges it '✦ Flagship · New', and puts 'ad videos' in the `<title>` (:7) and meta description (:8) — for the only one of its two products that cannot run. Photos is the reverse: `registry.js:35,42` both `enabled:true`, `:91` `DEFAULT_IMAGE_TOOL='nano-banana-pro'`, no flag. The card order is exactly inverted relative to what ships.

**2. `docs/` and code comments are wrong in BOTH directions — the audit found the code more honest than its comments in one place and far less honest in another.** Understating: `src/ugc/assembler/ffmpeg.assembler.js:7` claims 'music/vo/presenter → 미배선(있어도 무시)', and `src/ugc/assembler/tracks.js` declares PENDING stubs that `throw new Error('music track renderer not wired (v2)')` / `('vo track renderer not wired (v3)')`. Both are stale: `tracks.js` is orphaned (`grep -rn "require.*tracks" src/` = 0 requires), and the real assembler at `:20`, `:223-255`, `:483-500` performs genuine `amix` of VO + music with `aloop`, `adelay` scene-sync and ducking (music drops to 0.30 under VO). Audio is fully shipped and its own comments deny it. Overstating: `product.guards.v2.js` reads like a finished system and is dead data (next item). Neither would have been caught by trusting docs — this is why rule 3

### models_backgrounds
SEVEN surprises, several of which contradict the docs.

1) ⛔ DOCS ARE WRONG — the filter claim. docs/laongen_대비_랜딩비교_2026-07-16.md:93 asserts "80명 로스터는 인종·국적·연령까지 구조화돼 있고 studio.html:2618-2670이 이미 필터를 구현했는데" — implying ethnicity/nationality/age filters are implemented. FALSE. studio.html:2621 filters on m.gender ONLY, and :2632-2634 renders exactly three chips (All/Women/Men). Ethnicity, descent and age exist in the data but have NO filter UI anywhere. The doc's proposed copy ('80 house models · 40 backgrounds · no casting') is fine; its premise that the picker is richly filterable is not. Anyone building the configurator strip from that doc would ship a filter row that doesn't exist.

2) The two model-picker templates are flagged experimental + needs_human_review and ship as held:true (launch-hold, scripts/export_recipe_cards.js:95) — they only render because public/js/themes.js:16 lists them in DEFAULT_OFFICIAL_RECIPES, which forces held=false at studio.html:1523. The flagship on-model capability is one line in themes.js away from showing an "On hold" badge (studio.html:2462).

3) The Photos path does NOT use the good identity code. src/images/providers/nanoBanana.provider.js:35 has a kind-aware refClause ('Image N is an AI-generated fictional model (not a real person) — keep the SAME face'), but src/generate/generate.route.js never requires that provider — it inlines its own Gemini call. Only the Ad Video path (clipPipeline.service.js:78) gets refClause. Worse, the route's identity instruction is skipped entirely when enhance is OFF (generate.route.js:311).

4) A latent bug: generate.route.js:316 tells the model "The first image is the main character reference" — but for Model Cut the FIRST reference is the PRODUCT (studio.html:4526 sets characterId from the subject; the model is appended LAST at :4546). The route has no product-vs-person role distinction, so it labels the garment as the "main character".

5) Background thumbnails are not backgrounds. scripts/build_bg_roster.js:73 generates each preview as "full-body fashion lookbook photograph of a fashion model wearing plain neutral basics, {scene}…" — every one of the 40 contains an unrelated AI model. Com

### privacy_data
가장 큰 놀라움은 **문서가 코드보다 훨씬 정직하다**는 것 — 이 도메인은 랜딩이 과장하는 게 아니라 **아예 아무 말도 안 한다**(landing.html에 privacy/your data/secure/training 매치 전부 0건). 반면 privacy.html·terms.html은 국외이전 7칸 표·AUP·보호책임자까지 갖춘 상당한 자산이다. 즉 이 도메인의 문제는 '날조를 지우는 것'이 아니라 **이미 참인 자산을 하나도 안 팔고 있는 것 + 방침이 코드보다 앞서 약속해버린 것**이다.

**docs·주석이 틀린 지점 (요청하신 항목):**
1. **존재하지 않는 cleanup cron.** 코드 주석 4곳이 실재하는 것처럼 참조한다 — generate.route.js:30 \"cleanup cron이 오래된 tmp를 지워도\", :151 \"cleanup cron이 tmp/images 삭제해도 레퍼런스 유지\", character.controller.js:8·173 \"R2 영속화(cleanup cron 삭제 대비)\", ugcVideo.service.js:364 \"재배포/cleanup 후 redo 대비\". 그런데 scripts/에 clean|cron|reap|prune|purge 파일 **0개**, package.json에 cron/agenda/bull 의존성 **0건**, 스케줄러는 publishing 전용(index.js:220-221)뿐. R2 폴백은 **한 번도 작성되지 않은 cron을 방어하려고** 지어졌고, 실제로는 **아무것도 삭제되지 않는다**. docs/씬재생성_실패케이스_분석.md:80이 \"F11(cleanup cron)\"을 미완 항목으로 적어둔 게 유일한 정직한 흔적이다.
2. **삭제 약속 3개가 전부 코드 0.** privacy.html:115(탈퇴 30일 내 파기)·:116(1회성 업로드 처리 후 파기)·:200(탈퇴 시 얼굴 이미지 파기) 셋 다 배선이 없다. 결정적으로 user.repository.js:76 주석이 \"status='deleted' (로그인 차단, **FK 캐스케이드 회피**)\"라고 **의도적 미삭제를 자백**한다. 방침이 약속한 것과 코드가 의도한 것이 정반대다.
3. **탈퇴자 데이터가 공개 피드에 영구 잔류.** result.repository.js findCommunity에 `u.status <> 'deleted'` 필터가 없고, 크리에이터 핸들이 `split_part(u.email,'@',1)` = **이메일 로컬파트**다. 탈퇴해도 공개 크리에이션과 이메일 기반 핸들이 계속 노출된다. 이 '이메일→공개 핸들' 사실은 privacy.html·terms.html **어디에도 고지가 없다**(grep 0건). 미고지 개인정보 공개.
4. **Private ≠ 비공개.** index.js:49 `/images/:file`에 requireAuth가 없다(requireAuth는 :184부터 /api/generate에만). private 결과물 파일도 URL만 알면 무인증 열람되고 `Cache-Control: public, immutable, 30일`이 붙는다. privateMode는 '피드에서 빼기'일 뿐 접근제어가 아니다. 그런데 studio.html:1268은 이미 \"Only you can see and use it\"이라고 쓰고 있다.
5. **terms.html:216이 코드와 정면충돌.** \"회원이 공개(public)로 **설정한**\" + \"회원은 **언제든지** 공개 여부를 변경\" — Free는 설정한 적이 없고(generate.route.js:88 강제) 변경도 불가(:633이 조용히 public 강등)다. 그리고 Free 강제공개 사실은 약관·방침 어디에도 고지가 없다. **타깃이 전원 상업 이용자**라는 점에서 이게 이 도메인 최악의 항목이다 — 셀러의 미출시 제품컷이 경쟁 셀러가 보는 피드로 나간다.
6. **라이선스 등급은 순수 페이퍼.** `commercial` 플래그가 src/ 어디서도 read되지 않는다(매치는 models/roster.v1.js의 무관한 \"commercial model build\" 문자열뿐). requirePlan() 호출 0건도 재확인. 역설적으로 이건 **좋은 소식** — 승인된 starter/standard/pro 상업 개방은 entitlements.js + **terms.html:210 표**만 고치면 끝이고 강제 로직을 건드릴 게 없다

### ad_video
**1. The flagship's demo is not the flagship's product (ffprobe, 13/13).** public/img/reels/r00-r12.mp4 — every one is 720x1280, 2.375-2.417s, **zero audio streams**. The card they sit in (landing.html:618) sells "a narrated short — scenes, voice and music". Real output = 1080x1920 multi-scene concat with a muxed AAC track (ffmpeg.assembler.js:434-436, :505-510). We are selling narration with silence, scenes with a single shot, and 1080p with 720p. Nobody caught this because the players are `muted` (landing.html:613), so the missing audio track is invisible on the page.

**2. The kill/keep ledger still says Ad Video isn't launched — and is dead code.** public/js/flags.js:22: `ugc: { launch: false, label: 'hidden' }, // UGC Ads 모드 — 준비 중`. But nothing enforces it: rail.js:27's Ad Video item has no `f:` property so it gets no data-flag (rail.js:38), PAGE_FLAG (flags.js:29) has no /studio entry so the landing's `/studio?mode=ugc` link is never gated, and the only `data-flag="ugc"` element (studio.html:811) lives inside a `display:none` container anyway. Ad Video is fully live via rail.js:27 → bootFromUrl (studio.html:5055-5066), and it is the landing's #1 flagship. The launch ledger and the landing disagree about whether this product exists.

**3. docs/kling_영상파이프라인_감사_2026-07-06.md is stale in the way that matters most.** It says the hardening is "미푸시" — it landed (fetchWithTimeout at videoJob.service.js:34, reentrancy guard :364, finalizing recovery :299-301). But **every one of those fixes is in videoJob.service.js — the *shared* Kling poller for Studio reels. Ad Video does not use it.** Ad Video has its own poller (clipPipeline.service.js:23-32) with no fetch timeout, no retry, and a bare `fetch` in kling.provider.js:74/:104. None of the 2026-07-06 hardening reached the surface the landing calls flagship. Reading that doc and concluding "the Kling problems are fixed" is exactly backwards for this domain.

**4. The one crash path the reaper can't reach is the only one that spends real money.** reapStaleProcessing (ugcVideo.service.js:997-1001) requires `script IS NOT NULL`, but `script` is written **only on success** (:383-387) — the INSERT at :287-294 has no sc

### templates_catalog
FOUR different template counts live in this repo and every one of them is wrong as a sales claim. CLAUDE.md says '11섹션·78개'. docs/템플릿_v2_발굴선별강화_결과.md:3 says '9개 카탈로그 × 6±개 = 60개'. studio.html:1454 and migrate.js:1076 both say '93종'. docs/섹션명령서/_STATUS.md and recipes.generated.js:11 say 170/16 — and 170/16 is the only figure that matches the code (recipeStore.list() returns exactly 170 parents over 16 sections; 208 objects total incl. 38 parent_id children). recipes.generated.js:6 even preserves the intent to shrink: '템플릿이 170→~93 재export하면'. So the docs are not merely stale, they disagree with each other by 3x. Every doc-sourced number must be treated as hostile.

THE REAL SHAPE: 170 exist, 7 ship. studio.html:1460 OFFICIALS_ONLY=true collapses the entire seed catalog to whatever is official/owned, and themes.js:16 + migrate.js:1046-1053 make that exactly 7 free recipe families (+38 cuts = 45 looks). 163 built templates — including some of the heaviest work in the repo — are unreachable in prod. Nobody is lying about 170 on the landing today (it claims no count at all), but any instinct to 'sell the library' must die here.

THE BURIED GRAVEYARD: themes.js:8 restricts CAT_THEMES.Shopping to beauty/fashion/general/productcut/nail-base/nail-template/accessories, and isThemeVisible (themes.js:31-35) returns false for everything else — so jewelry(34) + home(27) + coffee(24) + pet(12) + food(8) + tech(7) = 112 cards are invisible in Studio AND Library, everywhere, permanently. The single largest seed file is recipes.jewelry.v2.js at 313KB / 34 cards with lavishly engineered prompts — 100% dead. The jewelry that actually ships is a separate, later 4-family 'accessories' set. Add flags.js influencer:false (kills influencer 7 + headshot 7 + general 8) and reels:false (kills all 39 reel cards via studio.html:2460) and almost nothing survives. Of the 170 FE cards, only 30 carry held===false, and even those must clear the official gate.

GUARDS ARE THEATRE: config.guards[] (single_sku, label_lock, form_lock, count_lock, scale_cue, reflection_control) is declared on 118 cards and consumed by zero lines — grep -c 'guards' recipeResolver.js = 0. recipes.general.v2.js:40 files 

### laongen_structure
【1】🔴 최대 발견 — slots 사다리는 '복사 의혹'이 아니라 완전일치다. 문서 §⛔ (L112)는 laongen이 FREE 2 / STARTER 3 / STANDARD 4 / PRO 5 / PREMIUM 6을 판다고 적고 '이 숫자열이 laongen에서 그대로 넘어온 흔적일 가능성을 시사한다'며 조심스럽게 의심만 한다. 코드 실측 결과 public/landing.html:722(2)·728(3)·734(4)·740(5)·746(6) = 5티어 전부, 티어명 순서까지 완전일치다. 우연 확률 ≈0. 게다가 :734 'Edit results'가 Standard에 붙은 것도 문서 §⛔(L108)가 말한 laongen '수정하기 = STANDARD부터'와 티어 위치까지 일치한다. 결론: 우리 가격표는 laongen 가격표를 축째로 베낀 뒤 entitlements.js와 대조하지 않은 산물이다. design 단계 함의 — 이 5개 불릿은 '틀린 사실'이 아니라 '남의 제품 명세'다. 하나씩 검증할 게 아니라 통째로 폐기하고 entitlements.js에서 다시 세워야 한다.

【2】🔴 문서 자체가 내부모순 — 크레딧 환산을 FAQ로 옮기라는 제안은 우리 우위를 버리는 것이다. §Doppia가나은점-2 (L24)는 laongen이 '크레딧이 뭘 사는지'를 FAQ로 미루는 반면 우리 LP-CONV(:839-840)는 실단가로 카드 안에서 라이브 환산한다며 이를 우리 우위로 정확히 판정한다. 그런데 §Next-3 (L76)의 FAQ 문항 ④는 '크레딧은 어떻게 차감되나요 — 템플릿 300/장…'을 FAQ로 신설하라고 한다. 그대로 하면 laongen의 약점을 수입하는 꼴이다. 올바른 구조: 카드=환산 유지(우리가 더 나음), FAQ=단가표(카드에 안 들어가는 산문). 둘 다지 이동이 아니다. ⚠️ design 단계에서 §Next-3을 문자 그대로 따르면 안 됨.

【3】문서에 laongen 섹션 순서가 없다. 이 도메인의 1번 질문('섹션 순서와 각 섹션이 닫는 질문')에 대해 문서가 실제로 제공하는 것은 nav 라벨 5개(L85), FAQ 위치(L32/76), 히어로 내용(L34/120), 최종 CTA 문구(L102)가 전부다. '서비스'·'갤러리'·'워크플로우' 각 섹션의 내부 구성은 한 줄도 문서화되지 않았다. 49에이전트가 8차원을 비교했는데 정작 비교 대상의 섹션 목록이 산출물에 없다 — 워크플로우의 구조적 누락. claims[0]의 순서는 추론이며, design 단계에서 laongen 원 사이트 재확인이 필요하다.

【4】우리는 이미 laongen의 핵심 구조를 갖고 있고, 그 위에 중복을 얹어 스스로 무력화했다. #templates(:651-681)는 이미 탭 5개(All·Cosmetics·Accessories·Apparel·Nail soon) + 타일 16장으로 laongen의 '탭 단일 데모'와 같은 형태다. #demo(:586-600)는 .dchip 4개로 같은 카테고리 축의 같은 인터랙션을 반복한다 — 근사 중복이 아니라 정확한 중복. 즉 이 차원의 격차는 '자산 부족'도 '설계 부재'도 아니라 순수한 삭제 미실행이다. #demo·#products 삭제만으로 우리는 이 차원에서 laongen과 구조적 동급이 된다. 신규 생성 0.

【5】문서 파일 자체가 꼬리에서 손상돼 있다. L124·L125(복사오염으로 기각된 제안 2건)가 문장 중간에서 잘려 있다('…각 열 = 122.' / '…explore.h'). 기각 사유의 결론부가 유실됐으므로, 이 두 제안(히어로 대형 정지 3장 재구성 / '실제 결과물 피드' 노출)을 '기각됨'으로만 알고 지나가면 안 된다. 특히 전자는 §Next-6(마퀴 확대)와 실질적으로 같은 처방인데 한쪽은 기각, 한쪽은 채택으로 남아 있어 상태가 모호하다.

【6】#how가 워크플로우 섹션인데 그 마지막 단계가 거짓이다(:691 Instagram & TikTok). laongen 구조에서 워크플로우는 '내 노동량이 얼마인가'를 닫는 자리이고, 그 섹션의 결말은 곧 제품 약속의 결말이다. 우리는 그 결말 자리에 studio.html에 grep 0건인 기능을 놓았다. 구조 슬롯은 옳고 내용이 거짓인 패턴이 :560(히어로 리스크 해소)·:568(사회적 증거)·:722-746(가격 축)에서 반복된다 — 이 랜딩의 병은 구조 설계가 아니라, 옳은 구조 슬롯을 근거 없이 채운 것이다. 이는 좋은 소식이다: 골격을 다시 짤 필요가 없다.
