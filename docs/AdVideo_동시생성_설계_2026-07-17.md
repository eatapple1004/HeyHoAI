# Ad Video 동시 생성(2번) — 설계 정본 · 2026-07-17

> **이 문서 = 다음 세션 진입점.** 아래 "검증된 사실"은 이미 코드/DB로 확인했으니 다시 파지 말 것.

## ✅ 진행 상태 (2026-07-17) — **A~H 8단계 착지 + 설계 개정 완료. 푸시·배포됨.**

`upstream/main`에 푸시 + **prod 배포됨**(사용자가 pull + pm2 restart, 2026-07-17).
프론트는 curl로 대조 확인(prod `studio.html` = 로컬 HEAD).
⚠️ **백엔드는 밖에서 확인 불가** — `/health`가 상태·타임스탬프만 주고 버전·업타임이 없다. 확실히 하려면
prod에서 `"rose-nude"` 들어간 컨셉으로 대본 1회(통과=최신, `422 Blocked term: "nude"`=pm2 restart 필요).

> 🔴 **배포 상태는 이 문서를 믿지 말고 매번 curl로 확인할 것** — "미배포"라는 옛 기록이 실제로 틀렸던 적이 있다.

> **§3 "규칙"·"배치"는 실측 후 개정됐다** — 원안의 "렌더는 오븐으로 + 자동 스크롤 + 자동 승격"은 기각.
> 사유·근거는 §3의 "❌ 폐기된 규칙" 참조. 되돌리려면 거기 적힌 충돌부터 풀 것.

| 커밋 | 단계 | 내용 |
|---|---|---|
| `53b7c9c` | **A** | 축 분리 — `isUgc`(정체성) / `_bench`(위치) |
| `efc056c` | **F** | 편집 부활 — 피드 [✎ Edit video] → 조리대. 신규 `GET /ugc/jobs/by-result/:idx` + `/ugc/jobs`에 `editable[]` |
| `62f267b` | **D** | 저장 시 result idx를 배치에 기록(배치 ↔ DB 행 연결) |
| `7f5e6cb` | **C** | 공용 힌트를 조리대 전용으로 + "saved to your gallery" 거짓말 제거 |
| `6e3d000` | **B** | 경과 타이머 배치별(`data-batch`) |
| `dfaee59` | **E** | 오븐 카드가 Ad Video 모양(9:16+경과+씬) · CSS 3개 언스코프 |
| `e6453f3` | **H** | **N개 개시** — 벽 3개(splice·genActive 가드·`.rendering` Edit 숨김) 제거 |
| `1eaf6ff` | **G** | 조리대 교체 시 자동 저장 · 완성 시 자동 승격(조리대 빈 경우) |

**작업 중 발견해 같이 고친 기존 결함**(설계 목록엔 없던 것):
- `ugcAutoCommit`이 `b.isUgc`를 봐서 **피드에서 되불러 편집한 것이 이탈 시 조용히 유실**되던 것 → `b.ugc.jobId` 판정
- `_saved`가 "갤러리에 있다"와 "저장할 게 없다"를 겸해 **되불러 편집하면 적용 버튼이 사라지던** 것 → dirty 우선
- 대본 없는 옛 잡에 [Edit]가 뜨던 것(로컬 5개 중 4개) → `editable[]`로 차단
- ugc 모드 밖(`/studio`)에서 [Edit] 누르면 `#scriptStep`이 숨겨져 **영상이 통째로 사라지던** 것 → 모드 게이트
- 완성 알림이 "saved to your gallery"라 옆 스트립("Not saved yet")과 **한 화면에서 정면 모순**이던 것

### ✔ 결정됨 — 원안의 "렌더=오븐" 기각, 현재 동작 확정 (`053cb32`)

실측으로 원안을 검토한 결과 **(b) 현재 동작 유지**로 확정. 근거는 §3 "❌ 폐기된 규칙"에 있다.
도달 불가였던 `ugcBenchFree()`·자동 승격 2곳을 제거했다(사용자 동작 변화 0 — 실행된 적 없는 코드).

⚠️ 교훈: 커밋 `1eaf6ff`의 "1개생성 자동승격 검증"은 **테스트에서 `ugcScript=null`을 손으로 넣어**
실사용에서 못 일어나는 상태를 통과시킨 **잘못된 검증**이었다. 전제를 손으로 만들면 죽은 코드도 통과한다.

### ✔ 결정됨 — 오븐 편집 기각, 편집은 조리대 전용 (`bcce627`, 2026-07-17)

사용자 제보 "어떻게 오븐에서 편집이 되는거야?"에서 출발. **오븐 카드가 조리대와 같은 함수**
(`ugcSceneStripHtml`)를 불러 씬 편집·재생성·음악·자막까지 통째로 편집기를 갖고 있었다.

**아무도 설계하지 않았다 — 우리가 깨웠다:**
| 시점 | 커밋 | |
|---|---|---|
| 7/9 | `e66ff47`(개발자) | 씬 스트립을 **피드용**으로 작성 — 그땐 결과가 피드에 살았다. **살아 있던 코드** |
| 7/9 (같은 날) | `e732ac0`(개발자) | 결과를 조리대로 옮기며 `if(b.isUgc)return` 추가 → 피드 스트립은 **그날 죽은 코드** |
| 7/17 | `53b7c9c`(우리 A) | 가드가 `if(b._bench)return`이 되며 **8일 만에 되살아남** |

**기각 사유**: ① 오븐엔 레이어드 플레이어가 없다(`#ugcInline` 전용) → 같은 버튼인데 "고치면 즉시 보임"이
"Apply→재굽기 왕복"이 된다. **같아 보이는 것이 같게 동작하지 않는 게 없는 것보다 나쁘다.**
② 표면이 둘이면 양쪽 검증이 필요한데 실제로 안 됐다 — `ugcRefreshStrip`이 `#ugcInline` 스코프라 오븐에선
no-op이었고 **씬 재생성 스피너·Add scene 플레이스홀더가 조용히 죽어 있었다**(크레딧은 나가는데 무반응 →
재클릭 유도). **B가 경과타이머에서 푼 것과 같은 부류를 바로 옆에서 놓친 것.**
③ 오븐 카드엔 이미 조리대로 가는 한 클릭 경로가 있다(`[↑ View up top]`·`[✎ Edit video]`).

**같이 한 일**: 스트립 안에 있던 `[Save & finish]`가 사라지므로 오븐 카드에 독립 `.rb-save`를 붙였다.
`ugcApplyBtn`은 조리대=`.ugc-apply` / 오븐=`.rb-save` 폴백(없으면 `btn=null`로 "Saving…"·중복클릭 방지 소실).
**E(렌더 중 로딩 스트립)는 유지** — 그건 편집기가 아니라 진행 표시다.

⚠️ **되살리려면** 위 ①②를 먼저 풀 것(레이어드 플레이어를 오븐에도 두고, 스코프 셀렉터를 전부 `[data-job]`으로).

### 착지한 UI 플로우 (사용자 시점)

```
빈 화면        좌: 입력칸(펼침)          우: "How this works"        아래: 과거 피드
 ↓ [Write script]
대본 검토      좌: 영수증 카드 + [Edit]   우: 대본(줄마다 탭 수정)     [Rewrite] [Generate ◈N]
 ↓ [Generate]  ← 크레딧 즉시 차감
렌더 중        좌: 영수증                우: 9:16 로딩 + 경과초 + 씬카드
 ↓ 완성
완성           좌: 영수증                우: 영상 + 씬 편집 스트립 + [Save & finish]
 ↓ [Edit] → 컨셉 수정 → [Write script]   ← **이 순간** 이전 영상 자동 저장 + 오븐으로 내려감
2개째          우: 새 대본                아래: 1번이 계속 렌더/완성(같은 모양·자기 경과초)
```
- 피드 카드 `[✎ Edit video]` → 조리대로(이전 조리대 것은 자동 저장 후 내려감). 렌더 중이면 거절.
- 실패·타임아웃 → 로딩 카드 치우고 대본을 조리대로 복원 → 재시도 가능.

### 재연결(새로고침 생존) — H 후속으로 마저 고침
- **경과초가 0부터 다시 세던 것**(`70b7a38`) — t0를 `Date.now()`(다시 붙은 순간)로 잡아 3분 걸린 렌더가
  "5초째"라고 했다. 오래 걸릴수록 자주 새로고침하니 **가장 필요할 때 가장 크게 틀리는 숫자**였다.
  → 기준을 `created_at`(잡 시작)으로. 백엔드가 이미 주고 있던 값이다(`listActiveJobs` 443행).
- **1개만 되붙어 나머지가 증발하던 것**(`1f246b7`) — 백엔드는 `LIMIT 20`을 주는데 `list[0]`만 썼다
  (원 주석에 "N개는 피드 전환(2단계) 후"라 예고돼 있던 그것). → 전부 붙인다: 최신 1개 조리대, 나머지 오븐.
  잡 하나 붙이는 로직 = `ugcReattachJob()`. 힌트·영수증·`.rendering`은 조리대 것만 건드린다.

### ✔ 영어 모드인데 씬 설명만 한국어로 나오던 것 (`79d8e50`, prod 제보)

`ugcScript.builder.js:90` — 지시는 `in ${langName}`으로 맞게 갈라져 있는데 **바로 뒤 e.g.가 한국어로 박혀**
있어서 영어 모드에서도 **예시가 지시를 이겼다.** 89행(spoken·cta·caption)은 언어를 타서 영어였으니
**한 화면에서 두 언어가 어긋나** 보였다. `18b216f`(컨셉 제안)와 **글자 그대로 같은 재발** —
`${langName}`을 쓰는 자리엔 예시도 같이 갈라야 한다.

검증(실호출): en 2회 = 씬 summary 전부 영어 · ko 3회 = 전부 한국어(회귀 없음) ·
ko 프롬프트는 수정 전후 **바이트 동일**함을 대조로 확인.

> 곁다리로 알게 된 것: 씬 `summary`를 나오게 하는 건 프롬프트가 아니라 **`output_config` 스키마 강제**다
> (스키마 없이 부르면 두 언어 모두 summary를 통째로 생략). 그리고 **ko 4회 중 1회 summary가 빈 문자열**로
> 온 적이 있다 — 재현 안 됨·우리 수정과 무관(바이트 동일). 걸리면 씬 설명이 빈칸으로 보이니 언젠가 볼 것.

### ✔ 금지어 워드리스트 6벌 전부 제거 (`8f5254f`·`600068b`, 2026-07-17) — **다시 넣지 말 것**

발단: prod에서 `"creamy rose-nude bullet"` 컨셉이 **422 `Unsafe script: Blocked term: "nude"`**로 죽었다.
`'nude'`는 립스틱 셰이드 이름인데 금지어였고, 검사가 `.includes()` **부분문자열**이라 그대로 걸렸다.

같은 부류가 코드베이스에 **6벌** 복사돼 있었고, 막은 것의 대부분이 위반이 아니라 상거래였다:

| 금지어 | 실제로 막던 멀쩡한 말 |
|---|---|
| `nude` | rose-nude, nude tone — **화장품 셰이드** |
| `strip` | **pinstripe·striped** (줄무늬 옷) |
| `kill` | **skill** ("skilled craftsmanship") |
| `take off` | **"take off the cap"** (립스틱 뚜껑 열기) |
| `loli` | **lollipop** · `grinding` → 커피 · `intimate` → 조명 용어 · `school` → old-school |
| `child`·`teen`·`minor` | children · teenager · minority |
| `underwear`·`lingerie` | **속옷 브랜드는 광고 자체가 불가** |

**제거 근거**: 실제 게이트는 이미지·영상 생성 레벨이다(Gemini·Kling 자체 정책) — 위반물은 우리가 안 막아도
거기서 거절된다. 반면 이 목록들이 훑던 건 대부분 **우리가 방금 Claude로 만든 우리 자신의 출력**이고,
프롬프트가 이미 "no banned/adult content"를 지시한다. 게다가 워드리스트는 우회가 자명해서
(`'child'` 옆 `'kid'`는 통과) 방어가 아니라 연출에 가까웠다.

**유지한 것**: `character.validator`의 **나이 하한(23)** — 워드리스트가 아니라 구조화된 필드 판정이라
오탐이 원천적으로 없다. `BANNED_HASHTAGS`(f4f·like4like) — 안전이 아니라 IG 노출 위생이라 근거가 다르다.

🔴 **되살리려면 부분문자열 목록 말고 구조화된 판정으로.** 워드리스트는 "children's **dress**"(옷 이름)와
"**a child** wearing a dress"(사람)를 구분하지 못한다. 그게 애초에 아동복 광고를 막던 이유다.

### ✔ 착용컷 속 인물을 제품으로 베끼던 것 (`7e1f8dc`, 2026-07-17)

착용컷은 정상 제품컷이다(귀걸이는 귀에, 옷은 몸에 — accessories 3패밀리 중 하나가 **Worn Cut**).
그런데 `refClauses`가 `"Image N is the EXACT product — keep its **identity** ... unchanged"`라고만 해서
착용컷이면 **그 사람 얼굴까지 그대로 그리라는 말**로 읽혔다. 판매자가 계약한 모델이지 우리가 계약한 게
아닌데 그 얼굴을 새 장면에 재현하고 있었다(**초상권**).

→ 제품 사진 속 사람은 **마네킹**이라고 명시. 업로드본에선 제품만, 사람은 **로스터에서만** 온다.
   막는 게 아니라 범위를 말해주는 것 = 착용컷은 계속 쓰고 거절·오탐 0.

**같이 고침**: 인물 배제 네거티브(`human face, people, hands`)가 **모델 씬에도** 걸려 한 요청 안에서
정반대를 말하던 것. 이제 인물 레퍼런스가 실린 씬만 해제(`hasPersonRef`). 제품 씬 네거티브는 **문자열까지 동일**.

**실측 검증**(착용컷 1장 → 두 씬, Kling 미사용): 제품 씬 = **귀걸이만**(원본 인물 흔적 0) ·
모델 씬 = **로스터 모델이 착용**(원본 백인·갈색머리 정체성 유출 0).
⚠️ 단 **네거티브 버그의 가시적 피해는 A/B 2회(얼굴·손)로 반증됐다** — Gemini가 포지티브를 우선해
모순을 흡수한다. 이 수정의 값은 품질 개선이 아니라 **모순 제거**(네거티브를 엄격히 지키는 모델이 오면 깨질 지뢰).

### ✔ 제품 사진 작업 (2026-07-17 후반) — **롤백 안 함. 종결됨.** (판정은 이 절 끝)

사용자 제보: "다중 레퍼런스가 작동을 잘 안 하는 듯". 파이프라인 4단계를 추적한 결과
**Kling까지 갈 것도 없이 ③ 스타트 프레임에서 어긋나** 있었다 — 프롬프트가 장마다 따로
`Image N is the EXACT product`라고 해서 **"이게 그 제품"을 N번 선언** = 모델이 제품이 여럿이라고 읽었다.
코드 주석·화면은 "동일 제품 다각도"라는데 **정작 모델에게만 그 말을 안 하고 있었다.**

| 커밋 | 내용 |
|---|---|
| `81c81b7` | refClauses로 **묶어서** 말함(1장=기존 문구 유지) + 번호↔이미지 어긋남 수정 + 컨셉이 첫 장만 보던 것 + 임시파일 누수 |
| `cca1c01` | "각도"로 못박던 것 → **상태**(뚜껑 닫힘/열림)·**클로즈업**도 포함. 씬이 상태 변화를 쓸 수 있게 열어줌 |
| `1159b62` | "제품 하나"의 뜻 = 물리적 개수가 아니라 **사진들이 같은 대상에 합의하는가**. 세트도 하나로 침 + 안내 ⓘ |

**규칙(확정)**: ✅ 같은 제품의 각도/상태/클로즈업 · ✅ **세트 컷 × 여러 각도**(대상=세트) ·
🔴 사진마다 대상이 바뀌는 것 · ⚠️ 회색지대=세트 전체 + 그중 하나 클로즈업(대상이 모순).
**여러 제품 미지원** — `scene.subject`가 product|model뿐이라 "몇 번 제품"을 지목할 수단이 없고 모든 씬에
모든 레퍼런스가 들어간다. 지원하려면 스키마+대본 프롬프트+렌더 라우팅+UI(Product 1/2 그룹)가 같이 움직여야 한다.
→ 결론: **필요 없다고 판단** — 세트는 한 컷으로 되고, 개별은 **N개 동시 생성**(오늘 연 것)으로 각각 만드는 게 더 좋다.

### ✔ 결론 — **롤백 안 함. 화면 문구만 고쳤다** (2026-07-17, 사용자 실사용 판정)

**사용자 판정: "사진 많이 넣을수록 좋아지지 않아. 1장이든 여러 장이든 퀄리티는 랜덤임."**
→ 이 절이 기다리던 검증(1장 vs 2장 비교)은 **불필요해졌다.** 답이 나왔다.

**그래서 한 일**: 화면이 `more angles, states or close-ups = **better accuracy**`라고 **초대**하고 있었다.
그게 거짓말이므로 그 주장만 걷었다 → `N/5 — locked into every scene · every photo must show the same product`.
**여러 장 허용은 유지**(`UGC_MAX_REF=5`) — 안 좋아질 뿐 해가 없다. 주장만 걷고 기능은 안 막았다.

🔴 **3커밋(`81c81b7`·`cca1c01`·`1159b62`)은 롤백하지 않는다** — 원래 롤백 조건이 "안 좋아지면"이었는데,
그 셋이 고친 건 **"2장 넣으면 제품이 둘로 읽히던 것"**이다. 되돌리면 좋아지는 게 아니라 **혼종이 다시 나온다.**
"안 좋아진다"와 "되돌리면 나아진다"는 다른 말이다.

> ⚠️ 참고 — 원래 적혀 있던 *"합성 이미지는 모델이 제품으로 안 읽어 무의미"*는 **틀린 사유였다**(2026-07-17 실측).
> Gemini는 AI 생성 이미지도 제품으로 아주 잘 읽는다 — `7e1f8dc` 검증에서 AI가 만든 귀걸이 사진에서 귀걸이만
> 정확히 추출했다. 합성이 무의미했던 진짜 이유는 **AI가 만든 2번째 각도는 1번째에서 추론한 것이라 새 정보가 0**이라
> 원리상 개선을 보여줄 수 없기 때문이다. (이제 사용자 판정으로 답이 나왔으니 이 검증 자체가 닫혔다.)
→ **실제 제품의 다각도/상태 사진 2장으로 1장 vs 2장 스타트 프레임 비교**가 다음 할 일.
   안 좋아지면 `UGC_MAX_REF`를 1로 내리는 게 정직하다(지금은 "넣을수록 좋다"고 초대 중).
   롤백하려면 `git revert 1159b62 cca1c01 81c81b7`(단, `15cb3e0` 사진 필수 가드는 별개라 유지).

### 🧒 아동복 — 현재 가능/불가 (2026-07-17 토의 결론)

| | 상태 |
|---|---|
| 아동복 **제품컷** | ✅ **지금 됨.** 실측: 3씬 전부 옷만 나옴(리넨 위 평면·원단 매크로·행거에 스커트 퍼진 컷). `productcut` 시드에 **ghost mannequin**(hollow-body)도 있다 — 아동복 이커머스의 표준 포맷 |
| 아동복 **모델샷** | 🔴 **불가.** 금지돼서가 아니라 **로스터에 아이가 없어서**(80명 전원 22~29세). 막는 코드는 없다 |
| 성인 일반 의류 모델샷 | ✅ 됨 |

⚠️ **아동복에 `With a model`을 고르면 지금 사고가 난다** — 대본이 `"A child aged 5-7 wearing…"`을 쓰는데
레퍼런스는 22세 모델이다. 프롬프트와 레퍼런스가 모순 → **◈3,125 선차감 후** 성인이 아기옷 입은 컷 또는 벤더 거절.
→ **미해결.** 고치려면 워드리스트가 아니라 빌더의 `usesModel` 가이드에 사실을 알려주면 된다:
   *"The model reference is an ADULT (roster 22–29). We cannot cast a child. If the product is for children,
   keep every scene product-only."* (오탐 0 · 크레딧 낭비 0)

**아동 로스터를 하기로 하면 전제조건**:
1. **벤더 정책 확인이 먼저** — Gemini·Kling이 미성년자 생성을 허용하는지. 안 되면 나머지가 무의미.
   (⚠️ 이전 세션이 "제한하는 걸로 알려져 있다"고 적은 건 **확인 안 한 추측이었다**. 경쟁사 라오젠은 아동 얼굴
   로스터를 공개 운영 중 = 어떤 스택은 허용한다는 뜻)
2. `7e1f8dc`(착용컷 인물보호)가 **이미 그 바닥을 깔았다** — 사람은 로스터에서만 온다.
3. 아동 로스터는 별도 네임스페이스(`/img/models/kids/`)로. 현 정규식 `[\w-]+`가 슬래시를 안 받아 **자동으로 막히니**,
   그 정규식 확장 지점이 곧 강제 지점이 된다(`generate.route.js:1006`).
4. 아동 선택 시 자유 텍스트 금지 = 씬 고정 템플릿(`kids-catalog` 프로파일 신설). 카탈로그는 자유 연출이 필요 없다.

**참고(검증된 사실)**: 인물 레퍼런스는 `/^\/img\/models\/[\w-]+\.(jpe?g|png|webp)$/` 정규식으로 **서버가 이미
로스터만 통과**시킨다. 고객 업로드(`productImage`)는 전부 `kind='product'`. 단 `multer`에 **fileFilter가 없어**
업로드 필드 자체는 아무 이미지나 받는다 — "제품 사진"은 **필드 이름이지 통제가 아니다**(지키는 건 Claude 비전과 Gemini).

### 남은 것
- **배포** — 코드는 푸시됨, prod 미반영. `git pull` + **`pm2 restart`**(라우트 신규) + CF Purge(`studio.html`).
  ⚠️ 배포하면 이전 미배포분(`a648ee5` 재연결·배지·환불)도 같이 나간다.
  ⚠️ **prod 배포 상태 재확인 필요** — 문서 §1은 "`7f1b9bf`까지만 라이브"라는데, 사용자가 prod에서 새로고침
  후 렌더에 다시 붙는 것(=`a648ee5`)과 레일 배지(=`358609e`)를 실제로 보고 있다. §1이 낡았을 가능성.
- ~~크레딧 N배 선차감 확인 단계~~ → **✔ 결정: 확인 단계 없이 그대로 간다**(2026-07-17 사용자 결정).
  사실관계는 남겨둔다: 과금은 `ugcVideo.service.js:283` = 렌더 **전** 선차감, 환불은 **실패 시에만**(390행).
  5씬 영상 1개=◈3,125(5초 씬 ◈625×5) → 4개 동시=**◈12,500 즉시**(◈20,300/월 티어의 61%).
  Generate 버튼에 단가(`◈ N`)가 표시되는 것이 유일한 고지. "마음이 바뀌었다"·"결과가 별로다"는 환불 대상 아님.
- **실 렌더 2발 동시 미검증** — 크레딧이 나가서 상태기계로만 검증했다. 실제 Kling 2발은 승인 후.
- **로컬 테스트 잔재** — `doppia_local`에 `generation_results` 46·47(7/9 draft 2개를 검증하며 커밋). 정리 여부 결정.
- D의 "피드 재조회 중복 제거 필터"는 **넣지 않았다** — `loadStudioResults(reset)`을 아무도 안 부르고 무한스크롤은
  과거 페이지만 읽어 방금 저장한 행이 다시 들어올 길이 없다(없는 경우를 위한 죽은 코드). D의 실체=정체성 연결.

### 좀비 파일·에러 감사 (완료 — 새로 생기는 것 없음)
- 추가한 백엔드는 SELECT 2개뿐 = **파일 쓰기 코드 0건**. 세션 중 `tmp/images`에 새 파일 0개.
- F/G/H가 굽는 빈도를 늘리지만 모든 굽기는 `script._render.composites`에 기록되고
  `sweepStaleComposites` → `pruneComposites` → `delCompositeFiles`가 24h 후 **디스크에서 실제 삭제**.
  F가 새로 연 고아(저장본 재굽기)도 스윕 쿼리에 `result_idx` 조건이 없어 범위 안.
- 스위퍼가 남기는 `ugc_jobs.result_url`과 갤러리가 보는 `generation_results.file_path`는 **같은 filename**(663~668행)
  → 살아있는 갤러리 파일을 지울 위험 없음.
- G의 `await` 없는 자동 저장은 중복 굽기를 안 낸다 — 개발자의 `UGC_NS_BUSY[jobId]` 잡별 직렬화가 큐로 돌리고,
  큐에서 나온 건 `_dirty=false`로 조기 반환(3485행).
- 브라우저: Edit 5회 왕복에도 비디오 6개 고정·오디오/레이어드 누수 0·에러 0. 서버 로그 실에러 0.
- **기존 문제(우리 탓 아님)**: 로컬 Ad Video 결과가 34·35·36·37 포함 **전부 404** — 7/9 잡의 mp4가 로컬에 없고
  `/images`는 오브젝트 스토리지가 없으면 404(메모리 "미디어 스토리지 버그" 그대로).

## 0. 어디서 작업하나

| | |
|---|---|
| 워크트리 | `~/doppia-adui` (브랜치 `feat/advideo-ui`, upstream/main 추적) |
| 미리보기 | `http://localhost:3002/studio?mode=ugc` — `cd ~/doppia-adui && node src/index.js` |
| 로컬 DB | `doppia_local` (`.env`의 DATABASE_URL이 로컬로 박혀 있음. **prod RDS 아님**) |
| ⚠️ 미커밋 | `src/db/client.js` = 메인 세션의 로컬 SSL 우회. **절대 커밋하지 말 것**(그쪽이 미푸시로 남긴 것) |
| ⚠️ 안 끄는 것 | `:3001`(메인 세션·prod DB)·`:8098`·`:8097`·`:8096`·`:8077`·`:8000`·`:4179` |

**푸시 규칙(확인됨)**: `origin`=사용자 포크(main은 5주 전 죽은 브랜치), **`upstream`=개발자 레포 = 살아있는 main**.
팀 관행은 upstream/main **직접 푸시**(랜딩 커밋들이 전부 non-merge). `CLAUDE.md`의 "main 직접 push 금지"는 PR #8 시절 기준이라 낡음.

## 1. 오늘 이미 배포/푸시된 것 (upstream/main)

```
00d0f2c fix(ugc): 렌더 중 서버가 죽으면 과금된 채 유실되던 잡을 회수 + 환불
358609e feat(ugc): 레일 Ad Video 배지 (렌더 중 + 완성 미저장)
0cbb954 fix(ugc): 재연결이 고아 잡을 물고 늘어지지 않게 30분 상한
a648ee5 feat(ugc): 진행 중 렌더에 다시 붙기 (새로고침 생존)
56ebe45 fix(ugc): 진행 중엔 왼쪽을 영수증으로 (렌더 중 대본 재작성이 영상 지우던 사고 차단)
3ccbed5 fix(ugc): 씬 길이를 1단계로 되돌림 + 씬 요약행 아이콘 flex 버그
7f1b9bf feat(ugc): Ad Video Step 1 — 노브를 요약줄로 접고 여정·구제책 노출
```
**prod 배포 상태**: `7f1b9bf`까지만 라이브. `a648ee5` 이후(재연결·배지·환불)는 **미배포**.
배포 시 `git pull` + `pm2 restart`(백엔드 라우트 신규) + **CF Purge**(`rail.js` + rail 버전 바뀐 HTML 10개).

## 2. 검증된 사실 (다시 파지 말 것)

### 데이터 흐름 — 완성 ≠ 라이브러리
```
렌더 중    ugc_jobs status='processing', script=NULL, result_idx=NULL
  ↓ 완성   status='succeeded', result_url 있음, result_idx=NULL   ← draft. 어디에도 안 보임
  ↓ 저장   Save & finish 또는 이탈 시 sendBeacon → generation_results 행 생성 → result_idx 채움
           → 이제야 크리에이션 피드 · Library · home 에 뜸 (셋 다 /api/generate/results 단일 소스)
```
- **자동 커밋 스위퍼 없음**(`sweepStaleCommits`는 존재하지 않음 — `sweepStaleComposites`와 혼동 주의)
- 편집 후 반영: `if (row.result_idx)` 가드 → `UPDATE generation_results ... WHERE idx=result_idx`(3곳). **draft면 스킵**
- prod 실측: succeeded 89 → 저장됨 83 / draft 6(13h~180h 방치). generation_results 연결 = 정확히 83

### 🔴 피드의 과거 Ad Video는 편집 불가
> ⚠️ **아래 "피드 씬 스트립 조건"은 `bcce627`로 없어졌다**(편집=조리대 전용). 지금 피드는 렌더 중 로딩
> 스트립만 그린다. 아래 코드는 F가 왜 필요했는지를 설명하는 **당시 기록**으로만 읽을 것.
```js
// 히스토리 배치 생성 — b.ugc 가 없다
b = { key, items, failed, promptText, templateSource, templateName, model, w, h }
// 피드 씬 스트립 조건 (※ bcce627로 제거됨 — 당시 코드)
const ugcStrip = (!b.loading && b.ugc && b.ugc.scenes.length) ? ugcSceneStripHtml(b) : ''
```
`b.ugc`를 채우는 곳은 **`ugcFillPendingFromJob` 단 하나**. → DB 히스토리엔 절대 안 붙음.
**= 페이지를 떠나면 그 영상은 영원히 편집 불가.** (F가 고칠 기존 결함)

### 전역 상태 감사 — N개 안전성 (완료)
| 전역 | 성격 | N개 안전? |
|---|---|---|
| `UGC_LAYERED{}` · `UGC_REASM_*` · `UGC_NS_*` · `UGC_EDIT_REFS` | **jobId 키 맵** | ✅ 이미 배치별 |
| `UGC_CAP_EDITING` | 편집 중 jobId 1개 | ✅ 조리대 개념 그 자체 |
| `ugcScript` · `ugcProductFiles` · `ugcSelectedModelImg` · `_ugcPreviewAudio` | 조리대 입력 | ✅ 조리대=1개라 맞음 |
| `UGC_DRAG` · `UGC_CAP_DRAG` · `UGC_TL_DRAG` · `UGC_TL_SAVE_TIMER` | 조리대 편집 | ✅ |
| `UGC_CAPTIONS` | **기능 플래그**(config 게이트) | ✅ 배치 상태 아님 |
| `UGC_MAX_REF` · `UGC_VOICE_OPTS` · `UGC_CAP_MAX_*` | 상수 | ✅ |

배치별 함수는 전부 jobId를 받음: `ugcToggleAdv(jobId,n)` · `ugcAddScene(jobId)` · `ugcRefreshStrip(jobId)` ·
`ugcReasmNext(jobId)` · `ugcReflectEdit(jobId)` · `ugcSyncCapAt(jobId,ms)` · `ugcFindBatch(jobId)`
**→ 개발자가 이미 배치별로 짜놨다. 남은 전역은 전부 "조리대 것"이라 설계와 일치.**

### 워크벤치 vs 피드
| | `#ugcInline` (조리대 우측) | 크리에이션 피드 |
|---|---|---|
| 렌더 함수 | `renderUgcInline` | `batchRowHtml` |
| 영상 | **레이어드 플레이어**(자막 오버레이·오디오 2트랙) — `useLayered`/`ugcMakePlayer`가 **여기에만** 있음 | 구운 영상 카드 |
| CSS | **41개 규칙이 `#ugcInline` 스코프**. 로딩에 필요한 건 3개(`ugc-inline-vidwrap`·`loadvid`·`loadcap`) | 전역 |

### 이을 열쇠
`히스토리 배치 items[].idx` === `ugc_jobs.result_idx` (990행 `updateJob(id,{result_idx: savedResult.idx})`)
→ 어느 피드 카드가 그 잡인지 특정 가능(D·F에 필요)

## 3. 설계 (확정)

> **오븐엔 여러 개. 조리대엔 하나. 새 걸 올리면 이전 건 저장되어 내려간다.**

### 축 분리 — 설계의 심장
```js
isUgc  = "이건 Ad Video 배치다"     → 피드에서 배지·씬 스트립
_bench = "지금 조리대에 올라와 있다"  → 피드에서 제외, 워크벤치로
```
지금 `isUgc` 하나가 두 뜻을 겸해서(`if(b.isUgc)return` = 피드 제외 + 워크벤치행) N개가 불가능했다.

### 배치 (2026-07-17 개정 — 착지한 실제 동작)
| 상태 | 어디 |
|---|---|
| 입력·대본 검토 | 조리대 |
| **렌더 중 (가장 최근 1개)** | **조리대** — 큰 9:16 + 경과초 + 씬 카드 |
| **렌더 중 (그 이전 것들)** | **오븐(피드)** — 새 대본을 쓴 순간 내려간 것들. 모양·경과초 동일(E·B) |
| 완성·미저장 | 조리대(가장 최근) 또는 오븐 `[Save & finish][✎ Edit video]` + 레일 배지 카운트 |
| 저장됨 | 오븐 `[✎ Edit video]` = Library = home |
| **편집 1개** | 조리대(씬 스트립·자막·오디오 **그대로 유지**) |

### 규칙 (2026-07-17 개정 — 아래 "폐기된 규칙" 참조)
- **조리대 = 내가 가장 최근에 손댄 것 하나.** 렌더도 조리대에서 시작해 조리대에서 끝난다.
- **조리대가 내려가는 시점은 새 대본을 쓸 때 딱 한 번**(`ugcRenderScriptPreview`) — 그때 **자동 저장 후** 오븐으로.
  미리 안 비우는 이유: 다음 영상은 새 컨셉+새 대본이 있어야 하고(같은 대본 재사용 = 똑같은 영상 2개),
  그 `[Write script]` 순간이 곧 조리대가 필요해지는 순간이라 정확히 그때 비면 된다.
- **동시에 두 곳에 뜨지 않음**: 편집은 `_dirty`로 클라에만 쌓여 저장 전엔 DB와 다름 → 두 곳에 띄우면 **같은 영상의 두 버전**이 한 화면에 (정합성 문제)
- **오븐에서 완성된 건 오븐에서 기다린다** — 저장/편집 버튼이 카드에 있고 레일 배지가 센다.
- **화면은 최신순 한 줄**: 조리대(가장 최근) → 오븐(그 이전) → 과거. 화면 점프 0.

#### ❌ 폐기된 규칙 (원안 → 실측 후 기각, 2026-07-17)
> 원안: *"Generate 누르면 잡이 오븐으로 떨어지고 조리대 비워짐 + 오븐으로 자동 스크롤 1회"* ·
> *"완성 시 조리대가 비어 있으면 자동 승격"* · *"렌더 중인 건 조리대에 안 올림"*

**기각 사유 — 두 규칙이 서로 충돌하고, 흔한 경우(1개 생성)에 손해.** 1440×900 실측:
- 지금(렌더=조리대): Generate 직후 영상이 **눈앞**. 스크롤 **0**.
- 원안(렌더=오븐): 카드 상단 **y=841**(뷰포트 900 경계 밖) → 자동 스크롤 필요 → 완성되면 자동 승격으로
  **다시 위로** = 사용자는 아래를 보는데 카드가 증발. **막으려던 "어디 갔지"가 반대 방향으로 재발.**
- 자동 승격의 존재 이유가 원안 스스로의 말로 *"1개만 생성 = 지금과 동일한 경험"* → **같게 만들려고 내렸다 올리는** 왕복.
- "조리대를 미리 비운다"가 되찾아주는 것이 없다(위 규칙 2의 이유).
- 렌더 중 왼쪽은 영수증 카드뿐이라 이미 빈 공간이 큰데, 오른쪽까지 "How this works"로 비우면 Step 1 전체가 죽은 공간.

→ 구현 `053cb32`에서 `ugcBenchFree()`·자동 승격 2곳 제거(**애초에 실행된 적 없는 코드**였다).
   같은 걸 다시 넣지 않도록 `renderStudioResults` 축 주석에 근거를 남겼다.
   **원안으로 되돌리려면** 위 충돌(자동 스크롤 ↔ 자동 승격)을 먼저 풀어야 한다.

## 4. 작업 목록 (순서대로 · 각 단계 독립 커밋)

| | 내용 | 위험 | 개발자 영역? |
|---|---|---|---|
| **A** | `if(b.isUgc)return` → `if(b._bench)return` · `renderUgcInline(...find(b=>b._bench))` | 🟢 동작 무변화(준비) | 아니오 |
| **F** | **편집 부활** — 피드 카드 `[편집]` → `getJob(id)` → `ugcFillPendingFromJob` → `_bench=true` → 조리대. 카드는 자동 숨김(A 덕분) | 🟢 재료 다 있음 | 살짝 |
| **D** | 저장 후 중복 제거 — `items[].idx === job.result_idx` 매칭 | 🟡 | 아니오 |
| **C** | `#ugcHint` 17곳 충돌 → 오븐 카드가 스스로 진행 표시(`ugc-scenes-hint` 기존 활용), 힌트는 조리대 전용 | 🟢 | 아니오 |
| **B** | 경과 타이머 배치별로 — `querySelectorAll('#ugcInline .rb-elapsed')`(전역+스코프) → `[data-job="X"] .rb-elapsed` | 🟡 | 아니오 |
| **E** | 피드 로딩에 Ad Video 모양 — CSS 3개 언스코프 + `batchRowHtml` 로딩 분기 | 🟡 | 🔴 **예** |
| **H** | `splice` all isUgc 제거 · `genActive>0` 가드 제거 → **N개 개시** | 🟢 | 아니오 |
| **G** | 조리대 교체 시 자동 저장 · 완성 시 자동 승격(조리대 빈 경우) | 🟡 | 아니오 |

**H가 마지막인 게 중요** — A~E 검증 후에 N개를 풀어야 깨졌을 때 원인이 명확.

### 이미 반영된 것 (워크트리에 미커밋)
- `batchRowHtml`의 `const isUgc=!!b.isUgc||/^ugc/i.test(b.model)` — 인플라이트 배치가 `model:'Reel'`이라
  `/^ugc/`에 안 걸려 피드에서 **Custom 배지로 오표기**되던 것. (지뢰1, 해결)

## 5. 🔴 절차 (오늘 배운 것 — 반드시)

1. **매 단계 인라인 `<script>` 추출 후 `node --check`**
   오늘 `ugcRestoreActiveJobs`에서 `let pending`(API응답)과 `const pending`(배치)이 같은 스코프에 겹쳐
   `SyntaxError: Identifier 'pending' has already been declared` → **studio.html 인라인 블록 전체가 파싱 실패**해
   `bootFromUrl`·`STUDIO_BATCHES` 등 모든 함수가 사라졌다. **`node --check public/studio.html`은 HTML이라 통과한다.**
   더 위험했던 건 `rail.js`는 별 파일이라 **배지가 정상으로 보였다**는 것.
   ```bash
   python3 - <<'PY'
   import re,subprocess,tempfile,os
   s=open('public/studio.html').read(); bad=0
   for m in re.finditer(r'<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>', s, re.S):
       c=m.group(1)
       if not c.strip(): continue
       with tempfile.NamedTemporaryFile('w',suffix='.js',delete=False) as f: f.write(c); p=f.name
       r=subprocess.run(['node','--check',p],capture_output=True,text=True); os.unlink(p)
       if r.returncode!=0: bad+=1; print("🔴 "+r.stderr.strip().split('\n')[3][:90])
   print("✅ OK" if not bad else f"오류 {bad}")
   PY
   ```
2. **매 단계 개발자 식별자 개수 대조** — `ugcToggleAdv`=3 · `.ugc-adv`=24 · `.ugc-sum`=12
   (오늘 `ugcToggleAdv(jobId,n)`= "✎ Edit scene" 토글을 무인자 함수로 덮어쓸 뻔했다. 개명으로 회피)
   ⚠️ **E 단계엔 이 방식이 안 통한다** — 같은 함수 안을 고치므로 개수로는 안 잡힘
3. **매 단계 브라우저 실검증** — 클래스 유무가 아니라 실제 동작
4. **커밋은 단계별로** — 뭉치면 되돌리기 불가

## 6. 지뢰

- **개발자**: 마지막 PR 머지 `4dbfe28`(#131) = 11시간 전, 지금 조용. 하지만 **로컬에서 뭘 만드는지는 안 보인다.**
  최근 PR 4개가 전부 씬 편집(`Add scene 진행 표시`·`씬 재생성 스피너`·`Edit scene 레퍼런스`) = **E가 만질 곳**
- **소유 규칙**: `template.html`은 미접촉(개발자 소유 의심, rail.js `v=8`로 남겨둠 — 하위호환이라 배지만 안 뜸)
- **theme.css 미접촉** — 20개 페이지가 쓰므로 캐시 버전 올리기가 위험. 배지 스타일은 rail.js 인라인으로 해결함
- **크레딧 N배 선차감** (미결) — 4개 쏘면 4배가 즉시. 확인 단계 필요한지 사용자 결정 대기
- **배지가 조리대 것도 셈** (미결) — `result_idx IS NULL` 기준이라 편집 중인 것도 셈. 거짓말은 아니라 방치 예정

## 7. 별건으로 남은 것

- **최초 렌더 크래시 회수는 부팅 1회뿐** — `reapCrashedRenders`가 부팅 때만 돈다(나이 판단이 불가능해서:
  `runPipeline`이 중간에 `updateJob`을 안 불러 렌더 내내 `updated_at`이 멈춰 있음). 프로세스가 안 죽고
  파이프라인만 행이 걸리면 안 잡힌다
- **draft 6개 방치**(prod, 13h~180h) — 24h 넘어 배지에도 안 잡힘. 수동 복구 여부 결정 필요
- **대본 패널 1534px 스프롤** — 씬 설명이 한 줄 1422px. max-width 필요하나 개발자 레이아웃 전반
- **"hover it for what it means"** — 아이콘이 실패했다는 자백. 아이콘을 고쳐야 함
- **i18n**: Step 1 신규 문구가 사전에 없음(`Sound`·`Voiceover`·`Concept`·`Write it for me`·`How this works` 등).
  `'Count': '수량'` 오역 그대로(씬 개수인데 물건 수량). 번역 세션 담당
